# framework: `.agents/` is committed on purpose (docs, plans, state, skills, helpers travel with the repo).
# Comment lines are dropped when this snippet is appended; each path below is added only if absent.
{{VAULT_PATH}}/
.claude/settings.local.json
.knowledge-quiz/
__pycache__/
.agents-migration-backup/
