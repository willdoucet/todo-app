---
plan_kind: "feature"
plan_mode: "feature"
registry_key: "plan:prod-launch-release"
parent_epic: "v1-productionization"
milestone: "M8"
ui_scope: true
risk_tags: ["infra", "security", "auth", "data", "migration"]
ship_parts: ["PR1a", "PR1b", "PR2"]
workflow_status: "partially-shipped"
review_status: ["ceo-reviewed", "eng-reviewed", "adversarial-reviewed", "design-reviewed", "impl-reviewed", "final-reviewed"]
implementation_status: "partially-shipped"
shipped_parts: [{"part": "PR1a", "pr": "https://github.com/willdoucet/todo-app/pull/60", "shipped_at": "2026-09-23T16:07:56Z", "commit": "6f0a608"}]
reason: "PR1a shipped as #60 (not merged). Before merging it: the two Vercel settings (auto-assign off, done; system env vars exposed, already on); optionally the read-only smoke groups from this branch. After merge, before the first deploy: confirm the Staged build carries its commit, confirm/enable a restore point (step 0a), fly scale count web=1 (step 1); then Between the PRs steps 2-6 by RUNBOOK. Before PR1b's /execute-plan: merge master into prod-launch-release (PR1a's squash), and never change ship_parts."
reason_by: "ship"
reason_at: "2026-09-23T16:24:00Z"
updated_at: "2026-09-23T16:24:00Z"
pr: "https://github.com/willdoucet/todo-app/pull/60"
---
# Design: M8 — `prod-launch-release`

Generated by /office-hours on 2026-09-11
Branch: prod-launch-release
Status: APPROVED (2026-09-11)
Mode: Startup (pure engineering/infra)

## Inherited constraints

From epic `v1-productionization`, milestone M8: Launch release: manual runbook, smoke and
rollback checklists, password-rotation CLI, backup-restore drill.
Depends on: M7 (shipped 2026-09-11, PR #44). Dependency satisfied.

### Locked decisions (from the epic, verbatim)

1. Frontend: Vercel SPA at `mealy.dev`
2. Backend: Fly.io with `web`, `worker`, and `beat` process groups at `api.mealy.dev`
3. Database: Fly managed Postgres
4. Broker and result backend: Upstash Redis over TLS (`rediss://`)
5. File storage: Cloudflare R2, single provider, no S3 fallback
6. Tenancy: single-tenant per household, one deployment per family
7. Auth: first-party email and password, JWT access token plus `__Host-refresh` HttpOnly cookie
8. Merge strategy: squash-and-merge on `master`; direct push blocked by branch protection
9. Sequencing rule: infra unknowns are discovered before auth code depends on them

### Done when (from the milestone, verbatim)

- A manual release runbook is committed, smoke and rollback checklists exist, the operator
  password-rotation CLI works over `fly ssh console`, a backup-restore dry-run has been executed
  against a scratch Postgres, and the two criteria below are met.
- **Process-group counts are reconciled, not merely checked.** `fly scale show` must agree with
  `fly.toml` and the docs. Production runs `web=2` today, not the `web=1` this milestone
  assumed (`worker=1` and `beat=1` are correct). Nothing pins the count: `fly.toml` sets
  `min_machines_running = 1`, which is a floor, not a cap. M8 either scales `web` back to 1, or
  keeps 2 deliberately and updates `fly.toml`, TECH_STACK, and this criterion to match. Each web
  VM is 1024 MB (sized for argon2), so running 2 doubles that spend; a second machine also
  widens the window in which a restart leaves two machines briefly holding different secret
  values, which is what made the 2026-09-11 rollout hard to diagnose. `beat=1` is the invariant
  that must not change — a second beat double-fires the iCloud sync schedule.
- **The production host gate records which check rejected a request.** One log line per
  rejection naming the failed check (host vs origin-verify, absent vs mismatched), never the
  secret value, and with the 421 response body unchanged so the two failure modes stay
  indistinguishable to a caller. They are currently indistinguishable to the operator as well:
  during the origin-lock rollout, a drifted secret and a mis-deployed Cloudflare rule looked
  identical from outside, and telling them apart cost a production experiment. This is one line
  on an existing gate for operability — not the broader structured-logging story deferred
  below.

## Problem Statement

Production has been live since 2026-05-01 and has taken 22 merges to `master` since — 12 of
them touching `backend/`, and because Vercel auto-deploys on merge every one of the 22 was a
frontend production release. None of them ran against a written procedure — every one was
improvised from memory and from whichever milestone runbook happened to exist at the time. M8 is the milestone that turns "the operator
remembers how this works" into an artifact that survives the operator forgetting.

The specific failure this milestone exists to prevent is not data loss. It is the **silent
failure that nobody is looking for** — which is precisely the shape of both production incidents
this deployment has had.

## Demand Evidence

Not hypothetical. Six pieces of evidence, all from this repo's own record:

1. **The 103-day silent worker outage (2026-09-11).** The Celery `worker` machine had been
   stopped since at least the end of May. iCloud sync, the soft-delete purge, and the
   abandoned-upload sweep never ran; 32,136 jobs queued in Upstash. Nothing surfaced it — web
   stayed healthy, beat kept queueing, and the M7 deploy updated the stopped machine without
   starting it. It was found only because an M7 runbook step happened to look at worker logs
   for an unrelated reason.
2. **The M7 runbook's abandoned-upload sweep check was left open and nothing tracked it.** Its
   execution log says so in as many words: "Still open: the sweep's worker `succeeded` line."
   (The `fly status` box in the same runbook is also unchecked, but corrected during spec
   review: `git log -S` places that check's introduction in `651ff27` / PR #49, *after* the
   runbook's only execution in `3f14222` / PR #45. It has never had an opportunity to be run,
   so it is not evidence of anything.)
3. **The origin-lock rollout cost a production experiment (2026-09-11).** A drifted secret and a
   mis-deployed Cloudflare rule produce an identical 421 from outside. Telling them apart
   required changing production to see what happened.
4. **Nothing states the intended `web` count anywhere.** Corrected during spec review: `fly.toml`
   has no per-group machine count at all — `[processes]` maps names to commands, `[[vm]]` maps
   sizes to groups, and `min_machines_running = 1` is a floor on one service, not a cap.
   TECH_STACK names the three process groups without counts. Production runs 2. Nobody noticed
   until the M8 criterion was written. This is why item 7 below needs a new file to pin against:
   there is no existing place to put the number.
5. **Backups have never been restored.** Fly Postgres snapshots exist; no restore has ever been
   performed. By the standard definition, the backups are untested and therefore unproven.
6. **The locked deploy order is inverted by construction.** The parent plan's CEO review locked
   "backend first, then frontend." TECH_STACK records Vercel as git-integration auto-deploy, and
   Fly is deployed by hand — so the frontend ships on merge and the backend whenever someone
   runs `fly deploy`. The M7 execution log shows exactly this: PR #44 merged 10:48 PDT, Fly
   cutover 10:49 PDT. Every release since M2 has violated the rule, and no release has noticed.

7. **The production database was moved to a new volume and its entire snapshot history went
   with it — and nothing surfaced that either.** Found 2026-09-12 while executing The Assignment
   (below). The machine `6835444b795398` was created `2026-09-11T23:31:01Z` in zone `d2a4`; the
   previous volume sits in `pending_destroy` in zone `76a8`. Data survived (`/auth/status`
   returns `{"account_exists":true}`), but a moved volume starts a fresh snapshot timeline, so
   `fly volumes snapshots list` returned **"No snapshots available"** — production had **no
   restore point at all**, and `fly pg backup` (continuous/point-in-time backups) was found
   disabled. This is the milestone's own thesis happening in real time: not data loss, but a
   silent change to the recovery posture that no check was watching. It is also the single
   strongest argument that the drill in item 3 is worth the effort — a drill run the day before
   would have had nothing to restore from and would have said so.

## Status Quo

Today a release is: merge the PR (Vercel auto-deploys the frontend), then `cd backend && fly
deploy -a mealy-app-prod` from memory, then look at the app in a browser. Health verification is
`/healthz` returning 200, which proves only that one uvicorn process is alive. There is no
rollback procedure written down, no smoke checklist outside the two milestone-specific runbooks
(`r2-cutover-runbook.md`, `cloudflare-state.md`), no way to change the household password except
by hand-editing a hash, and no evidence any backup is restorable.

What it costs: the 103-day outage is the direct price already paid. The next one is unbounded —
nothing in the current procedure would catch a dead `beat`, a drifted secret, a Cloudflare rule
someone disabled, or a migration that half-applied.

## Target User & Narrowest Wedge

**The user is the operator, six months from now, who has forgotten everything.** Not a team, not
an on-call rotation — one person, by design, on a deployment whose whole premise is
one-household-one-instance.

This inverts the standard restore-drill advice, which universally says "train at least two
people; single-person ownership is a dangerous gap." That mitigation is structurally unavailable
here. The compensating control is different: the drill's value is not in cross-training, it is
in the **observed numbers written into the artifact** — how long the restore actually took, what
the row counts were, how old the snapshot was. The document, not the operator's memory, is what
has to survive.

**Narrowest wedge:** a release procedure the operator can follow cold, whose mechanical checks
fail loudly on their own rather than relying on the operator to remember to look.

## Constraints

- **Single-tenant, single household.** One operator, one account. Multi-user, invite flows and
  public signup are PRD non-goals; nothing here should anticipate them.
- **Command policy.** App commands run through Docker Compose (`backend/`); `.agents/bin/`
  helpers are host-side. `fly` and `vercel` are operator-side CLIs, run from the host.
- **Never run unfiltered `env` / `printenv`** on any machine (LESSONS.md). Every check in every
  artifact this plan produces must filter on the remote side. Names only, existence checks, or a
  single redacted variable.
- **The secret is never written down.** `ORIGIN_VERIFY_SECRET` appears in no document, no script
  output, no log line, no transcript.
- **`beat=1` is invariant.** A second beat double-fires the iCloud sync schedule.
- **The new backend must serve the previous frontend bundle unchanged.** The parent plan states
  the deploy-order rule as a pair: backend first, *and* "backend supports both old and new
  clients at any given moment." Turning off Vercel auto-promotion (item 8) makes that window
  operator-controlled rather than seconds long, so this constraint gets stricter, not looser.
- **Migrations must be backward-compatible** (`fly.toml [deploy] release_command`): a
  `release_command` failure leaves the previous image serving, so the new migration must not
  break old app code.
- **Shallow `/healthz` only** (LESSONS Decisions, 2026-04-23). Deep DB+Redis probes on a
  single-instance deployment turn a 5-second dependency flap into total downtime. This plan adds
  liveness checks to the *runbook*, never to the health endpoint.
- **Runbook checks must be runnable, and meaningful, where they sit** (LESSONS.md, 2026-09-11).
  A check must fail when the thing it guards is broken, and must be able to pass at the point in
  the sequence where it is written.
- **This repository is public.** GitHub disables scheduled workflows on public repos after 60
  days with no repository activity (commits, PRs — workflow *runs* do not count). The daily
  `ops-check.yml` watchdog dies silently on that timer, which is the 103-day outage in
  miniature. Monthly releases reset the clock; a 50-day gap since the last commit is a
  documented re-arm, not a surprise. Adversarial review: 60-day trap named.
- **Deferred to v1.1 by the epic, and out of scope here:** `deploy.yml` release workflow, Vercel
  PR previews, `visual-tests` as a required check, Sentry, structured JSON logging beyond the
  single host-gate line, app-layer rate limiting, password-rotation CLI as `workflow_dispatch`.

## Design Source of Truth

- **Approach:** HTML mockups
- **Mockups:**
  - BackgroundJobsSection + BackgroundJobsAlert (item 15a) → ./mockups/background-jobs-card-option-a.html
- **Design system pin SHA:** 628891f08a93549a472a78ce8b7dfcf81baf735c
- **Captured at:** 20260921-220717

The mockup is the full Settings page; its bottom-right panel switches fourteen fixtures across
the 15a states, dark mode, and a 375 px phone width, and shows what the status region would
announce. That panel is mockup chrome, not part of the design. Adversarial review (scoped, item
15a, 2026-09-21): where the state table below and this mockup disagree, the table wins.
Design re-review (scoped, item 15a, 2026-09-21): the mockup had fallen behind the table on
exactly those points, and it has been brought back. Its one Waiting fixture (two syncs run, two
sweeps not) still read "Waiting for the first runs", the case the adversarial pass found. State
2 had only the "may still be running" line. A never-run job past its grace would have rendered
"last ran null min ago", and its row text was keyed on `write_error` alone, so a fresh row that
carried it turned red. Now: two state 5 fixtures (no job has run; "Waiting on 2 jobs"), three
state 2 fixtures (some jobs fresh; all stale and flagged; all stale with one unflagged), state 3
"Nothing has run yet", state 4 with never-run rows overdue, and row text from the row's own
values (item 15a, "what a row says"). All fourteen were rendered and read back against the
table, at desktop and 375 px. Layout and styles are unchanged, so 8C stands. The pin moved
from `c0f7f30` because #55 added a Recipes-grid breakpoint note to `FRONTEND_GUIDELINES.md`. No
token and nothing on Settings changed. The two text-only items — the iCloud "Last synced" dot
and gray change (5A, 6A) and the page-header wrapper — are specified in item 15a's tables and
also render in the mockup as page context. Design review 8D folded three corrections back into
15a from the mockup: the top alert is inline text rather than a flex row, its link never wraps
mid-phrase, and the headline dot stays on the first line when the headline wraps.

## Premises

Agreed by the user, 2026-09-11:

1. M8's deliverable is a runbook that has been **executed** at least once, not one that has been
   written. Everything else in M8 is subordinate to that.
2. A release is not "green" on web health alone. The smoke checklist must prove `worker` and
   `beat` actually **ran a job** — not that `fly status` says `started`. Anything less
   reproduces the 103-day outage.
3. The password-rotation CLI is the only meaningful new application code in M8, plus one log
   line on the host gate. The rest is documentation, operator procedure, and executed drills.
   M8 is therefore unusually doc-heavy for a plan-tier milestone; reviewers should expect that.
4. `web=2` is drift to be **decided**, not a bug to be reflexively fixed. Either number can be
   right; what is wrong today is that nothing pins it and three sources disagree.
5. The parent plan's open item 8 (`prod-contract-freeze-plan-20260421-182714.md` § Open
   Questions, item 8) — "plan the v1.1 scope during M8, not after launch" — is this plan's
   responsibility. It lands here as a written scope contract or is explicitly dropped with a
   reason.
6. The two Cloudflare drift-detection TODOs both name "the M8 runbook" as their home. They get
   promoted here or explicitly declared out of scope. Not silently ignored.

## Landscape findings

Searched with category terms only; the product, domain, and proprietary concepts never left the
session.

**Conventional wisdom.** Runbooks fail from length, not from missing edge cases; the good ones
are short, imperative, one failure mode each, with exact commands and expected output, explicit
stop-and-escalate points, and a **last-tested date**. Mixing runbook types (deploy, diagnose,
remediate, maintain) into one document is named as the most common failure mode in growing
teams. A completed backup job is not proof of recovery — teams "trust the wrong signal." On
rollback, the field has largely settled on forward-only migrations with compensating migrations,
especially for single-instance deployments.

**Where it is sound, and we build on it:** forward-fix over rollback, already locked
independently by the parent plan's adversarial review on 2026-04-22. Split by runbook type. Put
a last-tested date on every artifact.

**Where it is wrong here, and why:**

1. "Train at least two people" is structurally unavailable on a single-operator deployment. The
   compensating control is the written observed numbers, not cross-training. → recorded under
   LESSONS Decisions.
2. "Write shorter" is the wrong correction for this repo. `cloudflare-state.md` and
   `r2-cutover-runbook.md` are long and heavily annotated, and those annotations demonstrably
   paid for themselves during the 2026-09-11 rollout. The advice that applies is **split by
   type, not by length**.
3. Rollback is not one doctrine here, it is two: Vercel is genuinely one-command reversible;
   Fly plus Postgres is not. The parent plan's "roll the frontend back first" rule implies this
   but never says it.
4. The industry's canonical backup story is not this repo's actual incident. Both 2026 failures
   were **liveness** failures nobody was looking for, not data loss.

## Approaches Considered

### Approach A: One runbook, manual throughout

- Summary: a single `infra/release-runbook.md` holding the deploy sequence, smoke checklist,
  rollback checklist, SPOF diagnostics and execution log. Password-rotation CLI. Host-gate
  rejection log line. Scale decision pinned in `fly.toml`. Execute one real release and one
  restore drill against it.
- Effort: M (human: ~2 days / AI-assisted: ~3-4 h) + operator execution ~2-3 h, human only
- Risk: Low-Med
- Completeness: 7/10
- Pros: smallest surface; no new credentials; ships fastest; meets every M8 done-when literally.
- Cons: one document mixing four runbook types is the exact failure the field names; every check
  stays human discipline — and the M7 runbook's sweep check was left open with nothing tracking
  it, which is the evidence that discipline alone did not hold here.
- Reuses: `r2-cutover-runbook.md` structure and execution-log table; `ROLLOUT.md`'s
  `pg_dump` → restore-probe → count-verify drill; `Secret Rotation Guide.md`'s rotation-order
  format.

### Approach B: Runbook set split by type + an executable release gate + drift detection

- Summary: three documents, one per runbook type — release, incident diagnostics, and the
  backup-restore drill with its execution log. Plus `infra/release-smoke.py`, which mechanically
  asserts the checks a human skips and exits non-zero. Plus the Cloudflare drift script both
  TODOs ask for. Plus the CLI, the gate log line, the scale decision, the executed release, the
  executed drill, and the v1.1 scope contract.
- Effort: L (human: ~5-6 days / AI-assisted: ~10-13 h) + operator execution ~3-4 h, human only.
  Re-estimated upward after spec review: three documents, two scripts, a CLI with tests, five
  gate-reason branches with tests, a new scale-expectation file, doc_map rules, and
  `fly.toml`/TECH_STACK/epic edits across two PRs. PR1 lands as two pull requests, 1a (docs,
  scripts, config) and 1b (application code and UI) — Eng review 0: decided, see Recommended
  Approach.
- Risk: Med — scope, and one new read-only Cloudflare API token
- Completeness: 10/10
- Pros: turns the two checks that actually failed in 2026 into assertions that cannot be
  silently skipped; closes both Cloudflare drift TODOs at the home they name; splitting by type
  keeps the release doc short enough to read under pressure while the diagnostics stay as deep
  as this repo's precedent.
- Cons: largest M8, realistically two PRs; a Cloudflare API token is a new credential surface
  launch does not strictly require; some checks (browser same-site, stale-tab chunk load) can
  never be scripted and stay manual.
- Reuses: everything A reuses, plus `cloudflare-state.md` as the drift script's diff target and
  `slice6-rate-limit-burst-test.sh` as the shell-script precedent.

### Approach C: Script-first — the script IS the runbook, markdown is its log

- Summary: invert the artifact. Write the executable verification first and keep the markdown
  thin: an ordered command list where the script performs every assertion, and prose only where
  a script cannot go. No Cloudflare drift automation.
- Effort: M-L (human: ~3 days / AI-assisted: ~5-6 h) + operator execution ~3 h, human only
- Risk: Med
- Completeness: 9/10
- Pros: directly serves this repo's own lesson that runbook checks must be runnable where they
  sit; both 2026 failures were unrun checks, not badly written ones.
- Cons: the operator still has to remember to run the script, so the discipline problem moves
  rather than disappearing; the rationale that made `cloudflare-state.md` pay off has no natural
  home in a script; heavy automation for a roughly monthly procedure is hard to justify alone.
- Reuses: same as B minus the Cloudflare API surface.

## Recommended Approach

**Approach B, shipped as two PRs on `prod-launch-release`** — chosen by the user, 2026-09-11.

The two-PR split is structural, not stylistic: premise 1 says the deliverable is an *executed*
runbook, so PR2 cannot be written until PR1 has been merged and actually run in production. M5
(#34, #36) and M7 (#41, #44) both shipped this way; this is the branch's own precedent.

**Eng review 0 (2026-09-12): PR1 lands as PR1a and PR1b.** The scope stays exactly as chosen;
the split is sequencing. PR1a carries everything with no application code — items 1, 2, 3, 4 (the
smoke script, its `infra/tests` suite and CI job), 7, 9, 10, 12's documentation, the `vercel.json`
build command and `index.html` meta tag for check 8's frontend tier, the host-side exception in
AGENTS.md and development-commands.md, the TECH_STACK truth fixes (Vercel promotion, Postgres Flex
17.2), and the expired break-glass line in `cloudflare-state.md`. PR1b carries the code — items 5,
6, 13, 14, 15, the `GATE_BREAK_GLASS` flag, the `job_heartbeats` migration, and every test. Two
properties fall out for free: PR1a is a real no-migration release, so criterion 8's rollback
dry-run runs on it, and PR1b is a real migration release, so criterion 19's migration-listing step
is exercised on the first execution rather than on paper. The "Between the PRs" window is
re-sequenced below accordingly.

A and C lost for the same underlying reason: A leaves every check as human discipline, which is
the failure already on the record twice; C automates well but has nowhere to put the rationale
that makes this repo's existing infra docs valuable, and it still depends on the operator
remembering to run the script.

### PR1 — artifacts, code, and configuration

**1. `infra/RUNBOOK.md`** — the repeatable sequence only.
<!-- CEO review 0: accepted expansion — candidate 3. Named RUNBOOK.md so
     REVIEW_CHECKLIST.md's two existing citations (:128, :217) resolve; they
     currently point at a file that does not exist, and :217 is this
     milestone's own acceptance criterion. --> Short, imperative, exact
commands with expected output. Contents:

- Pre-release gates: CI green on the target commit, `doc-guard` clean, and a migration review
  step that asks whether any revision in this release requires quiescing writers (the parent
  plan's migration-quiesce rule) — if so, the quiesce and restore sequence runs here, not
  ordinary `fly deploy`.
- **Deploy order, restored to backend-first.** Vercel production auto-deploy is turned off
  (below); the sequence is: merge → `cd backend && fly deploy -a mealy-app-prod` → health
  verification → promote the Vercel build to production → smoke.
- Run `infra/release-smoke.py`; it must exit 0.
- **Cloudflare dashboard verification steps, written out explicitly** — the WAF `/auth/*`
  rate-limit rule is active, the origin-lock Transform Rule is deployed (presence only, never
  the value), and Browser Cache TTL is still "Respect Existing Headers". This is the MVP path
  TODOS.md itself recommends for the two drift items, and it stands on its own whether or not
  the drift script (item 9) runs. The script narrows where to look; it does not replace looking.
- The manual checks a script cannot perform, each with its signal stated: the browser same-site
  cookie check on an `<img>` load; the stale-tab check (leave one tab on the old bundle through
  the deploy) and the chunk-load check (a cached page hitting a missing chunk must recover on a
  full reload).
- **The migration that ships with this release, listed with its reversibility.** One line per
  Alembic revision in the release: its id, whether `downgrade` is written and tested, and if not,
  that forward-fix is the only option for it. `REVIEW_CHECKLIST.md:128` requires this per release
  and the plan had no step for it (CEO review 0, candidate 3). It is also what makes the rollback
  doctrine below actionable instead of theoretical — "use `alembic downgrade` only for revisions
  explicitly written and tested as reversible" is unusable unless something says which ones those
  are, at the moment of the release.
- Release tagging in GitHub.
- Rollback trigger points: what conditions abort the release and send the operator to the
  rollback section.
- **Rollback, as two doctrines.** Frontend: promote the previous Vercel deployment ~~— genuinely
  one command, genuinely reversible~~, and it goes **first**. Final review (PR1a, 2026-09-23):
  not one command. A request for a chunk that no longer exists gets the app's HTML with a
  one-year `immutable` header (observed live), which Cloudflare caches under that chunk's
  name, so a promote-previous is reversible only after a Cloudflare Purge Everything and an
  empty-cache reload of any browser that opened the old build. RUNBOOK §5.1 carries both;
  TODOS.md tracks the `vercel.json` fix. Backend: roll the *code* back to the
  previous image, never the schema. <!-- Eng review 1 (CRITICAL GAP, closed): `fly releases
       rollback` does not exist in flyctl (v0.4.102 has only `fly releases`, a listing command);
       the plan named it in five places. --> The command is `fly releases --image -a mealy-app-prod`
  to read the previous successful release's image ref, then
  `fly deploy -a mealy-app-prod --image <ref> --skip-release-command`, run from the checkout of
  the commit being rolled back to (`fly deploy --image` re-applies the *current* checkout's
  `fly.toml [env]`, the trap `r2-cutover-runbook.md` documents). `--skip-release-command` is
  **required** across a migration boundary and harmless otherwise: the previous image's Alembic
  tree cannot locate the newer revision in `alembic_version`, so its `alembic upgrade head` fails
  with "Can't locate revision" and the deploy aborts — the doctrine as first written would have
  failed on exactly the case it was written for. After a migration has landed the default
  response is forward-fix; `alembic downgrade` only for revisions explicitly written and tested as
  reversible; backup restore only when both are unsafe. State plainly that "rollback" does not
  mean the database goes backward too.
- **Tell the household before it feels the release** (Design review 3). Deploy at a quiet hour:
  at `web=1` the single machine is replaced in place, and a save made in that window fails with
  an error toast. If someone is using the app, say so first.
- Execution log table, matching `r2-cutover-runbook.md`'s: date, operator, outcome, notes, and
  the **observed deploy downtime in seconds** (Design review 3 — the plan's own "observed
  numbers" doctrine; "brief" is not a number).
- Metadata header: owner, last executed date, estimated duration, risk level.

**2. `infra/incident-diagnostics.md`** — one entry per known failure mode, each with its symptom,
its distinguishing signal, and its ordered recovery. Link to existing deep rationale rather than
duplicating it:

- Cloudflare is a single point of failure in three places (DNS, proxy, WAF rate limit). If
  "nothing works" and Fly plus Vercel look healthy, check cloudflarestatus.com first; the
  documented fallback is temporarily grey-clouding the proxy to route direct to Fly and Vercel.
  **The trap, and why it is still open.** Grey-clouding also removes the origin lock's Transform
  Rule, so the app 421s every request — the fallback is self-defeating unless paired with a step,
  and that interaction is written down nowhere today. It also removes the `/auth/*` WAF rate
  limit. This plan tried to close the question with `cloudflare-state.md`'s "redeploy the
  previous image" answer and was **wrong to**; round 2 of spec review showed that answer has
  expired. It was written on 2026-09-11, when the previous image predated the origin-lock code
  (`4ec7ef0` / PR #46). Every image since *contains* the gate, so rolling back to a previous image no
  longer removes it — the operator would have to roll back past #46, reverting every subsequent
  change against an already-migrated database. Every other lever fails closed by design:
  blanking `ORIGIN_VERIFY_SECRET` makes the app refuse to boot, and blanking `PUBLIC_API_HOST`
  421s everything through the `not allowed` branch. The only live escape today is
  `APP_ENV != production`, which also loosens auth config and re-derives CORS. This is open
  question 6, and PR1 must land a real answer with its blast radius enumerated — the deployment
  currently has a break-glass procedure that does not work.
- **The database moved hosts, and the snapshot history did not follow.** Found 2026-09-12. A
  Fly host migration creates a new volume in a new zone, marks the old one `pending_destroy`,
  preserves the data, and silently resets the snapshot timeline to zero. Nothing warns you, and
  `fly volumes list` will not even show the old volume without `--all`. Symptom: none — the app
  is perfectly healthy and `/auth/status` still returns `{"account_exists":true}`. The only
  signal is `fly volumes snapshots list <vol> -a mealy-app-prod-db` returning nothing, which is
  why smoke check 9 exists. Recovery: nothing to recover; the action is to confirm continuous
  backups are on and wait for the first scheduled snapshot, then record the gap.
- **Break-glass when Cloudflare is unavailable**, in two modes — Mode A (proxy or WAF degraded,
  DNS resolving) is solvable with the `GATE_BREAK_GLASS` flag; Mode B (Cloudflare DNS down)
  is accepted risk for v1 because neither hostname resolves and the SPA cannot reach the API
  same-site from anywhere else. Full analysis, and the blast radius of enabling the flag, in
  open question 3. This entry states both plainly so the distinction is available during an
  outage rather than derived during one. The flag's design is settled (Eng review 7, folded into
  open question 3): enable with `fly deploy -a mealy-app-prod --image <current ref> -e
  GATE_BREAK_GLASS=1` (no build, about a minute), clear ~~with a plain `fly deploy` from `master`~~
  by redeploying the running image from its release tag's checkout without `-e` (Final review,
  PR1a, 2026-09-23, user decision: a deploy from `master` also releases any merged but
  unreleased code or migration mid-incident, past every RUNBOOK gate), and the entry's **last step is confirming `/healthz` reports `gate_break_glass: false`** — the
  daily `ops-check.yml` asserts the same, which is what closes the CEO review's remaining CRITICAL
  GAP (a flag left on after an incident). `cloudflare-state.md:296`'s expired "redeploy the
  previous image" break-glass line is replaced in PR1a with a pointer to this entry.
- **A real restore into production, as opposed to the drill.** Restoring an older snapshot
  reverts `assets.referenced` for any object adopted *after* the snapshot was taken, and the
  hourly abandoned-upload sweep deletes `referenced = false` rows older than 24h along with
  their R2 objects — so a restore can destroy live media that a restored entity still points
  at. Objects created after the snapshot are a milder case: they keep no manifest row at all,
  and the read proxy 404s on a missing row, so they become unreadable rather than deleted.
  Surfaced during spec review. The procedure is therefore: **stop the `worker` machine before a
  real restore, reconcile `assets` against the R2 bucket, and only then restart it.** This is
  the one genuine data-loss path in the milestone, and it lives here rather than in the drill
  because the drill restores to a scratch instance where none of it applies.
- Dead `worker` or `beat`: symptom is silence, not an error. The distinguishing signal is the
  worker's task-`succeeded` line, not beat's "Sending due task" line. Design review 4A: the entry
  is titled with the in-app name ("Background jobs") and lists the four job labels from item 15a
  beside their Celery task names, so a household report of "Unused photo cleanup is behind
  schedule" maps to a task without guessing. Eng review 4 (2A): the entry states that the card
  cannot tell a broken heartbeat recorder from a dead worker when the database itself is the
  problem — both read "Stopped". Eng review 5: that holds only while the web can still read the
  table and the worker cannot write to it. When the web cannot read it either, the card reads
  "Can't check" and the cron says "web cannot read job_heartbeats", so the entry carries both
  symptoms (item 15's limit has the table). The tiebreaker is `Task … succeeded` lines in the worker log
  beside the handler's heartbeat-write WARNINGs: succeeded lines present means the recorder is
  broken; absent means the worker or beat is dead.
- 421 from the origin gate: link `cloudflare-state.md`'s ordered recovery; with the new log line
  (item 5 below) the first diagnostic step becomes reading the rejection reason instead of
  guessing.
- Upstash or broker failure: iCloud sync stops while the web app stays healthy; check worker
  logs for Redis connection errors.
- Migration failure mid-release: `release_command` failed, previous image still serving; what to
  check and why forward-fix is the default.

**3. `infra/backup-restore-drill.md`** — the drill procedure plus its execution log. Restore a
real Fly Postgres snapshot into a **scratch** Fly Postgres instance, never the live database.

**The product is settled** (The Assignment, 2026-09-12): this is **unmanaged Fly Postgres Flex**
— app `mealy-app-prod-db`, image `flyio/postgres-flex:17.2`, volume `pg_data`
(`Scheduled snapshots: true`, `Snapshot retention: 5`). Not Managed Postgres, so the restore
path is snapshot-based: `fly volumes snapshots list <vol> -a mealy-app-prod-db` to pick an id,
then `fly pg create --snapshot-id <id>` into a scratch app. **The drill exercises both restore
mechanisms** (Eng review 1B): the volume-snapshot path above, and the point-in-time path item 12
turns on — `fly pg backup list -a mealy-app-prod-db` to pick a recovery point, then
`fly pg backup restore <scratch-app> --restore-target-time <RFC3339>` into a second scratch
cluster (flyctl v0.4.102: "Performs a WAL-based restore into a new Postgres cluster"). Each path
gets its own observed numbers; a backup that has never been restored is unproven, and item 12
would otherwise be adding a second unproven one. Two flyctl traps the drill must
spell out: snapshot subcommands infer the app from the *current directory's* `fly.toml`, so
running them from `backend/` silently targets `mealy-app-prod` and fails with "volume does not
belong to app" — always pass `-a mealy-app-prod-db`; and `fly volumes list` hides detached
volumes unless given `--all`. `fly status -a mealy-app-prod-db` also reports an available image
update (17.2 → 17.7), which is itself a documented drill re-run trigger.
Record observed numbers: snapshot age, restore duration, row counts on `users`, `tasks`,
`items`, `meal_entries`, `family_members`, `responsibilities`, and `assets` (the R2 object
manifest — added during spec review; it is the table whose staleness can cost real files, see
the real-restore entry in the diagnostics doc), and `job_heartbeats` (Eng review 1A — on the
scratch cluster its rows are copies and read as stale, which is expected). Destroy every scratch
instance at the end. Re-run triggers documented: after a Postgres major-version upgrade, after any change to
the backup tooling or its flags, and on a stated cadence. Builds on `ROLLOUT.md`'s existing
`pg_dump` → restore-probe → count-verify sequence.
Adversarial review: scratch names are dated (`mealy-app-drill-YYYYMMDD`,
`mealy-app-drill-pitr-YYYYMMDD`) so a leftover from a crashed drill does not collide. First
step of the drill is `fly apps list` and destroy any existing `mealy-app-drill*` app — never
skip this because "last time we cleaned up." The restore target is never `mealy-app-prod-db`;
the commands take the *new* scratch app name. `job_heartbeats` is counted when the table
exists (PR1b+); on the PR1a drill the log records `n/a — table not in this schema` rather
than failing the count. The Between-the-PRs sequence runs the drill after PR1a, before the
migration that creates the table.

**4. `infra/release-smoke.py`** — mechanical assertions, exit non-zero naming the failed check.
<!-- Eng review 2.3: Python from the start, not shell. JSON and TOML parsing, three exit classes,
     `--only` groups, `--self-test`, and a git-ancestor check are not shell work; stdlib only, the
     same discipline as the drift script; `fly` and `curl` calls sit behind one injectable runner
     so the comparison logic is unit-testable with fixture JSON. -->

It lives in `infra/`, not `backend/`, and deliberately: it shells out to `fly` and `curl` with
operator credentials, so it must run on the **host**, and `development-commands.md` currently
lists only `.agents/bin/` and `.agents/tests/` as host-side exceptions to the Docker Compose
rule. PR1a adds a third exception line for `infra/` (the scripts and their `infra/tests/` suite,
Eng review 3A) **in both `development-commands.md` and
AGENTS.md** — round 2 of spec review noted that amending only the former would leave AGENTS.md's
"IMPORTANT" block saying the opposite, and AGENTS.md is in `doc_map.exempt` so nothing would
catch the contradiction.
`infra/**` is also already doc-mapped to TECH_STACK → Infrastructure.

1. `/healthz` returns 200 both through Cloudflare and via the Fly hostname.
2. `fly status` reports **every** process group `started` — parsed, not eyeballed.
3. Machine counts reconciled against **`infra/fly-scale.json`** (item 7), parsed and diffed,
   failing on any disagreement — read via `fly machines list --json`, not the human-formatted
   `fly scale show` grid. Corrected during spec review: `fly.toml` cannot express machine
   counts, so it is not a diff target — `[processes]` maps names to commands, `[[vm]]` maps
   sizes to groups, and counts are set out-of-band by `fly scale count`. `fly.toml`'s own
   comment concedes this: "Singleton invariant enforced via `fly scale count beat=1`
   post-deploy." This check is the mechanism the epic's "reconciled, not merely checked"
   criterion depends on, so it needs a real file to reconcile against.
4. **Worker liveness proven now, not recently** — and the task already exists. Round 2 of spec
   review found `app.tasks.health_check` in `backend/app/tasks.py`: it touches no database, is
   not on the beat schedule, and nothing else calls it. `celery_app.py` sets
   `backend=REDIS_URL`, so there is a result backend — `health_check.delay().get(timeout=30)`
   proves the full enqueue → execute → result round trip without grepping logs at all. That is
   premise 2's assertion exactly: the worker ran *now*, not at some point inside an arbitrary
   window. **Mechanism:** the host cannot enqueue (`REDIS_URL` is a Fly secret), so the script
   runs the enqueue inside a `web` machine via
   `fly ssh console -C "/app/.venv/bin/python -c '...'"` and waits on the result backend. The
   interpreter is spelled absolutely for the same reason item 6 spells it absolutely — the prod
   image's `PATH` entry is relative — and `-C` is acceptable here, unlike in item 6, precisely
   because this invocation needs no TTY. No log parsing, so no `fly logs` streaming problem.
   Adversarial review: `health_check` has no Celery priority, so a non-empty queue can burn
   the 30-second `get()` behind other jobs. A timeout is exit 1 "worker round-trip unverified"
   — inspect queue depth and worker logs before declaring the worker dead. Open question 4's
   hourly primary stop can also fail this check; wait and retry once before treating it as
   a broken deploy.
5. Origin lock, both directions, on a **non-`/auth/` protected route**: `curl --resolve`
   straight to the Fly IP returns 421; the same path through Cloudflare returns 401. Corrected
   during spec review: the Cloudflare WAF rule blocks `/auth/*` at 5 requests / 10 seconds / IP,
   so a re-run, a retry or a concurrent operator login on an `/auth/` path returns 429 and the
   script would report a false failure on the single most alarming check it has. Any `protected`
   router path returns 401 unauthenticated and is not rate-limited. The origin IP comes from
   `fly ips list -a mealy-app-prod` at runtime, never from the literal hardcoded in
   `cloudflare-state.md`. A 429 is its own named outcome — "rate-limited, retry after the
   window" — and the script **does not exit 0** on it: an unverified lock is not a passed check.
6. Private media arrives with `cache-control: private, no-cache` unmodified.
7. Vercel cache headers still in effect: `index.html` `max-age=0, must-revalidate`;
   `/assets/*` immutable. Request `/index.html` **literally** — `frontend/vercel.json`'s header
   rule has `"source": "/index.html"`, and the SPA rewrite means a user landing on `/` or
   `/mealboard` never matches it — so the invariant may only hold for a request nobody makes.
   Item 11 fixes that; this check then verifies the fix on a real route as well as on
   `/index.html`.
8. **The deployed frontend build matches the release commit.** Added during spec review: item 8
   turns off Vercel auto-promotion, whose new failure mode is a forgotten promote leaving
   production on the previous bundle — and nothing else in this list would notice. Requires a
   commit SHA emitted at build time. Emit it as `<meta name="build-commit">` in
   `frontend/index.html` rather than as a Vite `define`: round 3 of spec review pointed out that
   a `define` only reaches the bundle if some `frontend/src/**` file references the token (a
   second code change, and a second doc_map route), and reading it back out of a content-hashed
   `/assets/index-<hash>.js` is its own problem. `index.html` is already in `doc_map.exempt` and
   is trivially curl-able. **How the SHA gets there** (Eng review 1.5, the plan left it unstated):
   Vite substitutes `%VITE_GIT_COMMIT%` in `index.html` from `import.meta.env` at build time, so the
   tag is `<meta name="build-commit" content="%VITE_GIT_COMMIT%">`, and `frontend/vercel.json` gains
   `"buildCommand": "VITE_GIT_COMMIT=$VERCEL_GIT_COMMIT_SHA vite build"` — versioned with the repo
   and already doc-mapped, no `vite.config.js` change. Vercel exposes `VERCEL_GIT_COMMIT_SHA` only
   when the project's "Automatically expose System Environment Variables" setting is on (item 8,
   second toggle). A name Vite cannot resolve is left as the literal placeholder, not blanked, so
   local and visual-test builds carry `%VITE_GIT_COMMIT%` harmlessly and the check treats that
   literal, an empty value, and an absent tag all as failure. The `frontend-tests` CI job gains a
   `VITE_GIT_COMMIT=$GITHUB_SHA npm run build` step that greps `dist/index.html` for the SHA, which
   proves the substitution and closes REVIEW_CHECKLIST's "build runs in CI as its own step" gap
   at the same time. ~~Assert the deployed SHA is an **ancestor of** the release commit, not
   equal to it.~~ **Review-implementation (PR1a, 2026-09-23): assert the deployed build has the release's code for its tier**,
   `git diff --quiet <deployed> <release> -- frontend/` (`backend/` for the API). The ancestor
   rule failed both of its goals: the previous release is always an ancestor, so a forgotten
   promote passed; and a squash merge never makes the pull request's head an ancestor of
   `master`, so the deploy-before-merge fallback false-failed. The code comparison catches a
   forgotten promote of a changed tier, passes a release that did not touch the tier, and
   passes deploy-before-merge. **No fallback check.**

   **Both tiers, not just the frontend** (CEO review 0, candidate 2). `GET /healthz` now returns
   the backend's commit alongside its status (item 14), so this check makes the same
   comparison for the API (Review-implementation (PR1a, 2026-09-23): of `backend/`, was "the same ancestor relationship"). The asymmetry was a real gap: the plan went to considerable
   trouble to prove the *frontend* was the code you just shipped while nothing at all proved it of
   the *backend*, which is the tier that runs migrations. An empty or absent version on either
   tier fails the check loudly — it never passes on a missing value. **Run this check only after
   the deploy has finished** (CEO review 9B): with `web=2` the rollout is rolling, so one machine
   reports the old commit and one the new until it completes, and running concurrently
   false-fails. At `web=1` the window does not exist — a small point in that column of item 7
   that nobody had counted. Round 2 of spec review
   correctly killed the "assert the `index.html` asset hash changed" fallback this plan proposed
   in round 1: `index.html` is not content-hashed — the `/assets/*` files it references are —
   and 12 of the last 22 releases touched only `backend/`, so a byte-identical bundle would
   false-fail on every backend-only release. A wrong check is worse than no check.

9. **A usable restore point exists.** `fly volumes snapshots list <vol> -a mealy-app-prod-db --json`
   returns at least one snapshot newer than 48 hours, or `fly pg backup list -a mealy-app-prod-db`
   reports a recovery point newer than 48 hours. Both mechanisms are queried and both are reported
   (Eng review 1B); the check fails only when neither is recent, and the volume id comes from
   `fly volumes list --json -a mealy-app-prod-db` at run time. Added 2026-09-12: the milestone's own assignment found production with zero
   snapshots and nothing watching, and a release checklist that verifies liveness but not
   recoverability repeats the same category of mistake one layer down. The check names the
   volume id at runtime rather than hardcoding it, since a host migration changes it.

`--self-test` mode forces a known-bad expectation so success criterion 3's negative case can
be demonstrated **without mutating production** — breaking check 3 or 5 for real is the exact
sin Demand Evidence #3 indicts. Default (no `--only`): fail check 3 (scale). With `--only`
set: fail one check *in that set* (cron proof uses the jobs-fresh assertion). Be honest about
its reach: it demonstrates the comparison-and-exit plumbing for the checks that compare
against a local expectation. It cannot demonstrate check 4's or check 5's *detection* power,
which would require a genuinely dead worker or a genuinely open origin. Criterion 3 is worded
against what it can actually prove. Adversarial review: see `--self-test` vs `--only` below.

Parse machine-readable output, not human tables: `fly machines list --json` for checks 2 and 3,
never the human-formatted `fly status` / `fly scale show` grids, whose shape has changed across
flyctl releases — a parser that silently stops matching turns an assertion into a no-op, which
is the exact failure class M8 exists to prevent. The runbook header pins a minimum flyctl
version. The script also asserts `infra/fly-scale.json`'s key set equals `fly.toml`'s
`[processes]` keys, so a typo cannot silently drop a process group from the diff — via
`python3 -c 'import tomllib'` (3.11+, and the repo is on 3.12), the same stdlib-only discipline
the drift script follows. If that makes the shell script awkward enough, write the whole thing in
Python instead; the runbook only cares that one command exits non-zero.

**Check groups are written down once** (Eng review 2.4), so the runbook and `ops-check.yml`
cannot drift:

| Group | Checks | Runs in |
|---|---|---|
| `release` | 2 every group started · 3 scale reconciled · 4 worker round trip over SSH · 8 commit match, both tiers | runbook only |
| `liveness` | 1 `/healthz` by both paths · `/healthz.jobs` fresh per task (item 15) | runbook + cron |
| `recoverability` | 9 snapshot or WAL recovery point under 48 h | runbook + cron |
| `edge` | 5 origin lock both directions · `/healthz.gate_break_glass` false · 7 Vercel cache headers · 6 private-media headers | runbook + cron |

A Cloudflare challenge page (Bot Fight Mode, if it is on) or any non-JSON where JSON was expected
is classified as exit 2, edge or tooling, never as a broken deployment; `cloudflare-state.md`
records the Bot Fight Mode setting so the drift script can diff it.

**`--skip` is a rollout tool, not a missing-key default.** Adversarial review: the PR1a smoke
command as first written (`--only=release,recoverability,edge`) had two opposing bugs — it
dropped check 1 (`/healthz` both paths), which PR1a can prove, by skipping the whole `liveness`
group; and it kept the `edge` group's `/healthz.gate_break_glass` assertion, which PR1a cannot
prove because the key does not exist yet. A missing PR1b field must not be a silent pass after
PR1b has shipped (that is how you hide a broken deploy), and it must not fail the first
execution either (LESSONS: a gate that inspects behavior only the new code produces cannot pass
before that code is live). The script therefore accepts
`--skip=healthz_jobs,healthz_version,healthz_gate_break_glass`. Skipped assertions are recorded
in the output as `skipped`, never as `pass`. After PR1b the skip list is empty; a missing key
then is exit 1. Image-rollback to a pre-PR1b image against a post-PR1b workflow is the same
signal — fail loudly, then either roll forward or disable `ops-check.yml` for the duration.

**`--release-commit` is required.** Check 8's comparison (Review-implementation (PR1a, 2026-09-23): was "ancestor test") is against this value, defaulting
to `git rev-parse HEAD`. The runbook runs the script from the release-commit checkout and
passes `--release-commit=$(git rev-parse HEAD)` explicitly, so a script invoked from a
different branch cannot false-fail or false-pass.

**`--self-test` fails one check that is in the selected `--only` groups.** The original
wording forced check 3 (scale), which the cron never runs, so `ops-check.yml --self-test`
could not prove the watchdog. With `--only=liveness,recoverability,edge --self-test` the
forced failure is the jobs-fresh assertion (fixture, no production). Criterion 17 uses
`workflow_dispatch` with `self_test=true`, not a branch edit — scheduled workflows only fire
from the default branch.

**Tests for the scripts** (Eng review 3A): `infra/tests/test_release_smoke.py` and
`infra/tests/test_cloudflare_drift.py`, pytest files with no dependencies beyond the standard
library, run on the host with `python3 -m pytest infra/tests -q`, driven by fixture JSON captured
from `fly machines list --json`, `fly ips list --json`, `fly volumes snapshots list --json`,
`fly pg backup list`, and the Cloudflare rulesets API. They cover the three exit classes, checks
3, 5, 8 and 9's outcomes, the `fly-scale.json`-versus-`[processes]` key-set assertion, `--skip`
printing `skipped` not `pass` (and a missing key *without* `--skip` being exit 1), `--self-test`
failing a check inside the selected `--only` groups, and the
drift script's redaction against a fixture that carries a dummy header value (the rulesets API
returns the Transform Rule's static header value in `action_parameters.headers`, so redaction is
structural, not a discipline). A new `infra-tests` job in `.github/workflows/test.yml` (ubuntu,
Python 3.12, no dependencies) runs the same command. `--self-test` remains the runbook's in-situ
proof of the exit plumbing.

Release tagging uses `v1-<UTC date>-<short sha>` (for example `v1-20260915-a1b2c3d`); the
runbook states the scheme once so releases are greppable in `git tag`.

**Exit codes distinguish a broken deployment from broken tooling** (CEO review 2E): `0` pass,
`1` a check failed (production is broken), `2` a precondition failed (fly CLI missing, token
expired, no network). Review-implementation (PR1a, 2026-09-23), user decision: an error the script
did not anticipate is also `2`, in both scripts. Python's own crash exit is 1, which read as
"production is broken" and skipped the remaining checks and the summary line. Conflating them means the operator cannot tell which of the two they are
looking at, at the exact moment that distinction matters most. The script also exposes **named
check groups** — `--only=liveness,recoverability,edge` (CEO review 1A) — so item 13's cron runs a
subset of the same script rather than reimplementing or grepping it: one place to fix a check.

The script prints no secret and passes no secret as an argument. A unit test asserts the drift
script's output never contains the origin secret (CEO review 3G), making presence-only structural
rather than a discipline.

**5. Host-gate rejection logging** (`backend/app/main.py`). One structured line per rejection
naming the failed check. **Six reasons**, mapped onto the gate's real branches rather than
invented — the original five did not match the code, and spec review had to correct this twice:

| Reason | Branch | Observable in production? |
|---|---|---|
| `public_api_host_unconfigured` | `not allowed` in `production_host_gate` | Yes — operator misconfiguration, not a caller |
| `host_mismatch` | `incoming != allowed` | Yes |
| `host_absent` | `incoming == ""` | No (see below) |
| `origin_verify_absent` | header missing | Yes |
| `origin_verify_mismatch` | `compare_digest` fails | Yes |
| `origin_verify_secret_empty` | `if not expected: return False` in `_origin_verified` | No (see below) |

`incoming != allowed` currently absorbs both absent and wrong, and `_origin_verified` returns a
bare `bool` across **three** outcomes — so it returns a reason string instead. The
`if not expected` branch is live code with a producing test, so it needs a name even though
production cannot reach it; round 3 of spec review caught round 2 deleting it from the table
while leaving the branch in place, which would have left the implementer with nothing to return.

**The two unobservable reasons**, named in the diagnostics doc as things an operator will never
see rather than as signals to wait for:

- `origin_verify_secret_empty` — `_initialize_origin_verify` raises at lifespan startup below 32
  characters, so the app cannot boot into that state. Its only producer is
  `test_gate_fails_closed_when_origin_secret_missing` (`:141`), which is the **request-time
  backstop** for the empty-secret branch — that test's own docstring says so, and this plan
  called it a boot-time test until round 3.
- `host_absent` — HTTP/1.1 mandates `Host` and uvicorn's h11 layer rejects a request without one
  with a 400 before the ASGI app runs; HTTP/2 maps `:authority` onto `host`. Kept as defense in
  depth, tested directly, but not an operator-facing signal.

**Test work is larger than "one new test."** All **eight** existing 421 tests
(`tests/integration/auth/test_host_gate.py` :73, :84, :103, :119, :128, :141, :165, :177 — Eng
review 2.2 recounted; the plan said five) assert only `status_code == 421` and, mostly,
`r.json() == REJECTED` — **none asserts a reason**, because the field does not exist yet. So
criterion 5 costs a reason assertion on each of them where the reason is deterministic, one
parametrized test that compares `r.content` byte for byte across all six reasons
(REVIEW_CHECKLIST → pytest: "byte-identical failure responses have a test that compares bodies
across every failure reason"), plus one new `host_absent` test (defined as "the `Host` header is missing *or* empty" —
`request.headers.get("host", "").strip()` yields `""` for both).

**Do not reuse `emit_log_line`, and do not wrap it either.** It calls `resolve_client_ip`
*inside its body*, which returns `CF-Connecting-IP` or the first `X-Forwarded-For` entry
verbatim — and on a request that went *around* the edge, the only interesting rejection, both
headers are attacker-controlled. It also binds a module-level
`logger = logging.getLogger("app.auth")` and documents itself as "structured logging for
/auth/\* endpoints", but the gate is middleware, not an auth endpoint. **A wrapper cannot change
either of those** — round 3 of spec review was right that round 2's "thin wrapper" decision was
not implementable. So: a **separate emitter** that reuses the payload *shape* and the field names
but not the function, resolving the IP from `request.client.host` (the Fly peer) and logging
under `app.gate`. It lives in `backend/app/gate_logging.py`, which **must be added to the
doc_map** (item 10) or it ships unrouted. **Shape** (Eng review 2.2): `gate_logging.py` holds the
six reason constants so tests import them from one place, and `emit_gate_rejection(request,
reason)` logs under `app.gate` with `event="host_gate"`, `outcome="rejected"`, `reason`, `ip`
from `request.client.host`, `request_id` via the existing `resolve_request_id`
(allowlist-validated, safe to import even though `emit_log_line` is not), `path` from
`request.scope["path"]` truncated to 128, and the presence and length of the `Host` and
`X-Origin-Verify` headers. The gate itself is restructured into a pure
`gate_reason(request) -> str | None` in `main.py` — ordered checks returning the first failing
reason — so the six branches get unit tests on a fake request and the HTTP tests assert the body;
with `GATE_BREAK_GLASS` on, `gate_reason` skips only the origin-verify check and the emitter logs
`outcome="bypassed"` per admitted request. The alternative — a `trust_edge_headers: bool = True`
parameter on `resolve_client_ip` — stays rejected: it defaults to the unsafe behavior and a
future caller can forget it.

**A logging failure must never change the response** (CEO review 2A, CRITICAL GAP). The emitter
is wrapped so that any exception inside it is swallowed and the gate still returns 421 with the
unchanged body. This exact class has bitten this gate once already: LESSONS records that
`hmac.compare_digest` raises `TypeError` on a non-ASCII `str`, which "would turn a forged header
into a 500 instead of a 421" — and item 5 adds a second code path that reads attacker-supplied
header data inside the same middleware. `test_gate_logging_failure_does_not_change_response`
patches the emitter to raise and asserts the 421 survives; it is the single most valuable test in
this plan, because it proves an observability bug cannot take down the auth boundary.

Never log the secret, and never log the incoming header value either — presence and length, not
content. The 421 body stays exactly `{"detail": "host_not_allowed"}` so the failure modes remain
indistinguishable to a caller.

**6. Operator password-rotation CLI** (`backend/app/cli/rotate_password.py`), runnable via
`fly ssh console`. <!-- Eng review 2.1: the behavior lives in `auth/service.py` as
     `rotate_password(db, email, new_password) -> RotationSummary`, next to `logout()` whose
     docstring diagram already anticipates it; the CLI module is argparse + `getpass` + exit codes
     (0 ok, 1 not found or rejected, 2 database unreachable) and disposes the engine before the
     loop closes (REVIEW_CHECKLIST → SQLAlchemy → Background jobs). The password is prompted
     **twice** and a mismatch exits 1 with nothing written — the tool that restores access must
     not lock the household out on a typo. --> Behavior:

- Takes the email as an argument; reads the new password from an interactive prompt, never from
  argv — an argument is visible in `ps`, the same trap the origin-lock rollout documented for
  `fly secrets`.
- Rehashes with the existing `app.auth.passwords.hash_password` (argon2id).
- **Calls the existing `app.auth.service.logout(db, user.id)`** rather than reimplementing it.
  Corrected during spec review: that function already bumps `session_version` (invalidating
  still-unexpired access JWTs) and revokes outstanding refresh tokens, in one committed
  transaction. Note it revokes only rows where `revoked_at IS NULL`, which is the correct
  behavior — "revoke every row" would re-stamp already-revoked ones.
  Adversarial review: `logout()` **commits**. `rotate_password` must set `password_hash` on
  the *same* `AsyncSession` *before* calling `logout`, and must not commit itself — one
  commit, hash then session bump then revoke, or neither. Two commits leave a crash window
  where the password has changed and old sessions still work, or the reverse. `logout`'s
  docstring currently says the caller MUST have a validated bearer; the CLI is an intentional
  exception (authenticated by `fly ssh`, not a JWT) and the docstring gets one line saying so.
  The printed summary ends with "all sessions revoked; the household must sign in with the
  new password" — every open tab dies on its next request, including the operator's.
- **Enforces `PASSWORD_MAX_LENGTH` imported from `auth/schemas.py`** (CEO review 2D). Argon2's
  hard limit is 128 characters and `RegisterIn` caps there; a CLI that does not would let you set
  a 200-character password that fails at login — locking you out of the household account using
  the one tool that exists to restore access to it.
- Prints a summary — user id, tokens revoked, new session version — and no secret.

The CLI drives `AsyncSessionLocal` under `asyncio.run` (`logout` is async), counts
`revoked_at IS NULL` rows *before* the call and re-reads `session_version` after — `logout`
returns `None`, so the summary numbers have to be gathered around it.

**Tell the household before rotating** (Design review 3). Every open tab is sent to the sign-in
page on its next request, and an unsaved form is lost. The runbook's first rotation step is to
tell the household and give them the new password; the one exception is a rotation because the
password leaked, where the order is rotate first, tell after.

**The sign-in page says why** (Design review 10, TODO 1: decided — build it in PR1b as an honest
generic message). Corrected while scoping it: the redirect is not silent. `AuthPortalPage.jsx`
already renders a bounce banner whenever `return_to` is present — "Your session ended. Sign in to
pick up where you left off." — and APP_FLOW §4's "silent one-shot redirect" note is stale. What the
banner lacks is the *why*, and the specific "the password was changed" is not derivable: the
Sign out button and the rotation CLI both call `logout()`, which revokes **every** household
session, so the two are identical at revocation; and `errors.refresh_failed` deliberately returns
one body for revoked, expired, superseded and corrupt tokens (an M3 decision, so a stolen cookie
cannot learn its own state). The change is therefore one string, `COPY.bounce`:

> "Your session ended. Someone may have signed out on another device, or the household password
> changed. Sign in to pick up where you left off."

It stays true for rotation, a sign-out on another device, and a refresh token that simply expired.
No backend, auth, or schema change; the existing tests match `/your session ended/i` and keep
passing, and one assertion is added for the new sentence. Rejected: a `users.password_changed_at`
column published on the public `/auth/status` so the page could say "the password was changed" —
it tells anyone on the internet when the household last rotated, for a message that only this
page would read. PR1b's `/update-docs` corrects APP_FLOW §4.

The exact operator sequence goes in the runbook, because it is not obvious: `fly ssh console -a
mealy-app-prod --select` → choose a **`web`** machine (the other groups are sized 512 MB and
256 MB and run no API code) → `cd /app && /app/.venv/bin/python -m app.cli.rotate_password
<email>`. Two traps, both found in round 2 of spec review: use `--select`, not `-C "..."`,
because the latter does not reliably allocate a TTY and the interactive password prompt needs
one; and spell the interpreter absolutely, because `backend/Dockerfile`'s prod stage sets
`ENV PATH=".venv/bin:$PATH"` — a **relative** entry resolved against cwd, so a shell that does
not land in `/app` gets the system `python`, which has none of the app's dependencies. Success
criterion 6 proves that exact sequence, not an equivalent one.

Tests: after rotation the old password fails and the new one succeeds; an outstanding refresh
token is rejected; a still-valid access JWT is rejected. Adversarial review: a test that
patches `logout` to raise after the hash is dirty still leaves the old hash in the database
(one transaction); `health_check` is not part of this test.

**7. Web-scale reconciliation.** The investigation the user asked for ran on 2026-09-12 (The
Assignment), and it changes the framing: **`web=2` is neither a deliberate choice nor drift — it
is the platform default, and production has never run `web=1`.**

| Machine | Group | Created |
|---|---|---|
| `2861e54f9dee48` | web | `2026-04-30T23:34:58Z` |
| `2869e6dae306d8` | web | `2026-04-30T23:35:13Z` |
| `d8d2e06fed20d8` | worker | `2026-04-30T23:47:18Z` |
| `d894036c173778` | beat | `2026-09-10T15:25:48Z` |

The two web machines were created **15 seconds apart** by the original M2 deploy: `fly deploy`
provisions two machines by default for an app that defines an HTTP service. Nobody scaled up
afterwards. `beat` is correctly at 1.

**This weakens the correctness argument this plan originally made, and the plan should say so
rather than quietly keep it.** The claim was that `web=2` forces rolling deploys with old and
new code running simultaneously after `release_command` has migrated — the mixed-version window
TODOS.md P3 ("Adopt Expand/Contract Migration Pattern for Multi-Instance Deploys") says these
migrations are not written for. That window is real, but it has now existed across **33 releases
and four and a half months** with no observed incident, because at one-household scale the
chance of a request landing inside a deploy is close to zero. "It has not bitten" is weak
evidence of safety, not proof of it — but it is enough to demote this from a correctness
emergency to an ordinary cost-versus-redundancy decision, which is what it now is:

- **`web=1`** halves the 1024 MB web spend, removes the mixed-version window entirely, and
  narrows the window in which a restart leaves two machines holding different secret values —
  the thing that made the 2026-09-11 origin-lock rollout hard to diagnose. It costs a brief
  hard outage on every deploy (the single machine is replaced in place, not drained) and no
  redundancy if a machine dies.
- **`web=2`** gives zero-downtime rolling deploys and survives one machine failing. It costs
  double, and it makes backward-compatible migrations a real obligation with nothing enforcing
  it — so keeping it deliberately means promoting expand/contract from a P3 TODO to a hard
  constraint, and updating TECH_STACK and the epic criterion to say so.

**Decided: `web=1`** (Eng review 1C, 2026-09-12). It is the boring option: every document, test,
and the `/healthz` design in item 15 already assume one instance; it halves the 1024 MB web spend;
it removes the mixed-version window that nothing enforces; and the redundancy `web=2` buys ends
at a single-instance database anyway. The cost — a brief outage on each deploy — is acceptable for
one household. Smoke check 8's run-after-rollout caveat becomes moot, and the P3 expand/contract
TODO stays a TODO. What was wrong was that nothing pinned it and three sources disagreed; the
rest of this item fixes that.

**Applying the decision is its own step, not a side effect.** Editing `fly.toml` never changes
machine counts; `fly scale count web=1 -a mealy-app-prod` does, and it runs once in the "Between
the PRs" window before the first smoke run — otherwise PR1 lands `{"web": 1, ...}` and smoke
check 3 fails on its first execution. The runbook states plainly that counts are set
out-of-band.
Adversarial review: the default-2 is a *first-create* behavior of `fly deploy` for an app
with an HTTP service. After `fly scale count web=1`, subsequent `fly deploy`s update the
existing machine and do not recreate the second. Do not re-run `scale count` after every
deploy unless check 3 fails. Check 3 is the detector if a recreate ever happens.

**Where the number is pinned: a new `infra/fly-scale.json`** — `{"web": 1, "worker": 1,
"beat": 1}` — added to the doc_map so a change routes to TECH_STACK → Infrastructure.
Corrected during spec review: `fly.toml` *cannot* hold per-group machine counts, so the
original "pin it in `fly.toml`" was not implementable. `fly.toml` gets a comment pointing at
the file, TECH_STACK states the counts in prose, and smoke check 3 diffs `fly scale show`
against the JSON. That file is what makes "reconciled, not merely checked" mechanical rather
than aspirational.

**8. Vercel production auto-deploy turned off.** Per the user's decision, git integration keeps
building on merge but stops auto-promoting to production; the runbook's promote step runs after
the Fly deploy and its health check. This restores the locked backend-first order that every
release since M2 has violated. Record the setting in TECH_STACK (which currently says
"git-integration auto-deploys") and state the new failure mode plainly in the runbook: a
forgotten promote leaves production on the previous bundle — which smoke check 8 now asserts
against.

**The capability is confirmed** (The Assignment, 2026-09-12, against Vercel's current docs), so
the chain spec review worried about no longer hangs off an unknown. The control is:
**Settings → Environments → Production → Branch Tracking → disable "Auto-assign Custom
Production Domains."** A commit on the production branch then builds and sits in state
**Staged** instead of going live; promoting is Deployments → ellipsis (…) → **Promote**, which
assigns domains instantly with no rebuild. Deployment states are Staged → Promoted → Current,
and a deployment that has already been promoted cannot be promoted again (use Instant Rollback
for that). The docs name no plan-tier restriction. A **second toggle** rides along (Eng review 1.5):
Settings → Environment Variables → "Automatically expose System Environment Variables" must be on,
so `VERCEL_GIT_COMMIT_SHA` reaches the `buildCommand` that stamps check 8's meta tag. Operator
note: the local `vercel` CLI token is expired — `vercel login` before using the CLI promote path. If the setting turns out to be unavailable, the fallback is to deploy
Fly **before** merging the PR — `release_command` migrations are required to be
backward-compatible anyway. **But that fallback is only safe on a no-migration release**, and
round 2 of spec review caught the plan understating it: `[deploy] release_command = "alembic
upgrade head"` would apply an *unmerged branch's* migrations to production, and if that PR is
then revised or abandoned, production's `alembic_version` points at a revision that does not
exist on `master` — the next deploy cannot resolve the version path, and there is no forward
fix short of hand-editing `alembic_version`. Backward-compatibility is about schema versus old
app code; it says nothing about revision-graph divergence. So the fallback is restricted to
releases with no migration, and the runbook says so in those words.

**A migration release under the fallback needs its own path**, since backend-first exists
precisely for releases where new UI depends on a new API surface, and those usually carry a
migration — round 3 of spec review caught the restriction leaving that case with nothing. The
path: merge first, then deploy Fly **immediately and before touching Vercel**, accepting a
frontend that lags by the length of a `fly deploy`. That is strictly better than today's
behavior (where the frontend leads by however long the operator takes) and needs no unmerged
deploy. The runbook makes it the named procedure for migration releases whenever the Vercel
setting is unavailable.

**Turning the setting off is its own scheduled step**, run in the "Between the PRs" window once
the setting is confirmed to exist (it is) — the runbook's deploy sequence cannot be executed
before it is flipped.
Adversarial review: the promote step names the staged deployment whose **commit SHA equals
the release commit**, and waits until that deployment is **Ready**. Promoting "the latest
staged" can promote an older leftover, or a build that has not finished. The runbook's
promote command is not "click Promote on the first row."

**Recorded dissent.** Round 2 of spec review argued this item, not item 9, is the plan's real
overreach: the epic asks that checklists *exist*, not that deploy order be enforced by platform
configuration; it creates a new failure mode whose only mitigation is smoke check 8, which in
turn is the only reason for a new build-time `define` in `frontend/vite.config.js`; and all
three hang off a Vercel capability nobody has confirmed. Its proposed cut was to keep the
runbook's stated order plus one pre-release question — "does this release add an API surface the
new UI depends on? If so, deploy Fly before merging" — and defer the rest to v1.1. The user
chose this item explicitly when asked, so it stays; the argument is recorded here so a
CEO or adversarial review can take it up with the full picture, including the two facts the
user did not have at decision time (the unverified setting, and the chained frontend change).

**9. `infra/cloudflare-drift.py`** — read-only Cloudflare API diff against
`infra/cloudflare-state.md`. Dumps the live WAF rate-limit rule, the Transform Rule's presence
(never its value), Browser Cache TTL, and the Access application list, and reports differences
against the checked-in intent. Requires a **read-only, narrowly-scoped** API token, stored as a
local operator credential, not in CI for now. Standard-library `urllib` only, so host `python3`
runs it with no dependency story — it sits beside the smoke script in `infra/` so there is one
operator-tooling directory, not two.

Review-implementation (PR1a, 2026-09-23), user decision: the Access-application comparison counts applications and does not
compare names, so an application swapped for another reads as recorded. Accepted: it cannot
happen while the file expects none; compare names once the first application is recorded
(the script's docstring says the same).

**Recorded dissent.** The spec reviewer argued this is the plan's one scope overreach: it is
absent from the epic's Done-when, it introduces a credential surface launch does not require,
and TODOS.md's own recommendation is the MVP path — "add both checks to the M8 release runbook
as explicit 'verify in Cloudflare dashboard' steps, no automation." The user was offered exactly
this cut as "B minus the Cloudflare drift script" and chose full B, so it stays. The mitigation
is that the dashboard-verification steps ship in the runbook **regardless** (item 1), so the P2
TODO is closed on its own recommended path whether or not the script survives review; the script
is an addition to that, not a substitute for it. If a later review cuts it, nothing else in the
plan moves.

Closes the P2 "Cloudflare config-as-code or drift-detection for M8 runbook" TODO at the home it
names, and supersedes the P3 "Cloudflare config drift detection for M2 Access bypass + /auth/*
rate-limit" TODO — but only **half** of it. Round 2 of spec review was right that the
Access-bypass assertion is moot (both applications removed) while the rate-limit assertion is
live and load-bearing: that is the mechanism the drift script and the runbook's dashboard step
both close. Correct the threshold when closing it — the P3 item records "10 req/min/IP", stale
against `cloudflare-state.md`'s live **5 requests / 10 seconds / IP**, and its hostname
(`api.todo.williamdoucet.dev`) predates the move to `api.mealy.dev`.

**10. doc_map rules for the new surfaces.** `.agents/config.json` maps `backend/app/auth/**`,
`storage/**`, `utils/**` and the rest, but nothing covers `backend/app/cli/**`. Unmapped paths
only print a note rather than blocking, so `/update-docs` would silently fail to route the CLI
and the new surface would go undocumented by construction. PR1 adds `backend/app/cli/**` →
BACKEND_STRUCTURE "Code Organization".

Round 2 of spec review completed the audit, and three more routes matter because `doc_guard_mode`
is `pr-strict`:

- `backend/app/main.py` is already owned by **two** sections — BACKEND_STRUCTURE "API Endpoints"
  and PRD.md "5.7 User Authentication". The gate log line touches both.
- `backend/app/gate_logging.py` (item 5's separate emitter) is a new top-level module and would
  be **unmapped** — round 3 of spec review caught this, since item 5's "separate emitter, not a
  wrapper" decision is what creates the file. Route it to BACKEND_STRUCTURE "Code Organization"
  alongside the CLI.
- `backend/app/job_health.py` (item 15's contract module, Eng review 4) is new and top-level for
  the same reason. Route it to BACKEND_STRUCTURE "Code Organization" too; the `/healthz` shape it
  produces is documented under "API Endpoints", which `main.py` already routes to.
- `frontend/index.html` carries check 8's `<meta name="build-commit">` and is already in
  `doc_map.exempt`. Choosing the meta tag over a Vite `define` avoids touching
  `frontend/vite.config.js` (→ TECH_STACK "Development Tools") and any `frontend/src/**` file
  (→ FRONTEND_STRUCTURE / APP_FLOW) at all.
- **The three new runbooks need an exemption or they become permanent friction.** The `"*.md"`
  entry in `doc_map.exempt` does *not* cover them: `glob_to_regex` in `.agents/bin/_lib.py`
  renders a single `*` as `[^/]*`, so `*.md` matches only root-level files. `infra/*.md` falls
  under the `infra/**` → TECH_STACK rule instead, which means every execution-log row — in PR2
  and in every future release — would demand a TECH_STACK edit or a `Docs: n/a` trailer. PR1
  adds `infra/*.md` to `doc_map.exempt`. Note the breadth honestly, per round 3 of spec review:
  this also un-routes the two *existing* runbooks (`cloudflare-state.md`,
  `r2-cutover-runbook.md`), which is a wider behavior change than "the three new files" and sits
  in mild tension with criterion 13. It is still the right call — an execution-log row is not a
  documented-surface change — but PR1 states the reasoning in `.agents/config.json` rather than
  letting it look like an oversight. This is exactly the kind of check that decays into a ritual
  if left, which is the failure mode the whole milestone is about.

**11. Fix `vercel.json`'s header coverage for real SPA routes.** Surfaced by round 3 of spec
review and folded into M8 at the user's direction rather than deferred. `frontend/vercel.json`
sets `Cache-Control: public, max-age=0, must-revalidate` on `"source": "/index.html"`, but the
SPA rewrite `"/(.*)" → "/index.html"` means a browser landing on `/` or `/mealboard` never
matches that source — Vercel's `headers` evaluate against the incoming request path, not the
rewrite destination. If that is how it behaves in practice, the "always revalidate the HTML"
guarantee covers only a literal `/index.html` request, and with it the parent plan's locked
chunk-load invariant: *"if a cached page hits a missing JS chunk, a full reload must recover
cleanly."*

This is pre-existing, unrelated to the release procedure, and outside the epic's M8 Done-when —
it is here because the invariant M8 is supposed to verify rests on it, and verifying a guarantee
that does not hold is worse than not verifying it. Scope discipline: **verify first**, with a
`curl -I` against a real route on the live site, and change `vercel.json` only if the headers are
genuinely absent there. If they are, the fix is a second `headers` entry whose source matches the
SPA routes (or `"/(.*)"` ordered after the asset rule). `frontend/vercel.json` maps to
TECH_STACK → Infrastructure in the doc_map, so item 10's audit already covers it.

**12. Restore the backup posture, and make its absence detectable.** Added 2026-09-12 after The
Assignment found production with no restore point. Three parts, and the first is the only item
in this plan that should not wait for PR1 to be written:

- **Enable continuous backups now:** `fly pg backup enable -a mealy-app-prod-db`. flyctl's own
  error message names this command, so it is available on this cluster (v0.4.102: "Enable backups
  on a Postgres cluster, creating a Tigris bucket for storage" — Eng review 1B); confirm afterwards
  with `fly pg backup list -a mealy-app-prod-db` and record whether it carries a cost. The drill's
  point-in-time path restores from this bucket, so item 3 proves what this bullet enables.
  Adversarial review: this is an operator action **today**, not a Between-the-PRs step that
  waits for PR1a to merge. Production has been without a restore point since 2026-09-11.
  Between-the-PRs step 0a *confirms* a recovery point exists and enables only if still
  disabled.
- **Confirm the first scheduled volume snapshot lands.** `Scheduled snapshots: true` with 5-day
  retention is already set, but the moved volume is hours old and the timeline restarted, so
  there is a window with nothing to restore from. Check back with
  `fly volumes snapshots list vol_4qlnz9q037wl2qwr -a mealy-app-prod-db` and write the observed
  first-snapshot time into the drill's execution log — that number is the real recovery-point
  objective, as opposed to the configured one.
- **Document the failure mode itself** in `infra/backup-restore-drill.md`: *a volume move resets
  snapshot history*. A Fly host migration creates a new volume in a new zone, leaves the old one
  `pending_destroy`, preserves the data, and silently drops every snapshot. Nothing warns you.
  This is why the drill's re-run triggers include "the volume id changed", not only "the
  Postgres version changed".

**13. `.github/workflows/ops-check.yml` — the check that runs between releases.**
<!-- CEO review 0: accepted expansion — candidate 1 -->

The premise challenge this review opened with: a release-time check cannot catch a failure that
happens between releases, and **both of this deployment's real incidents did exactly that.** The
worker died around the end of May and surfaced 103 days later; the restore point vanished during
a host migration with no release anywhere near it. Releases here are roughly monthly. Everything
else in this plan fires only when you ship.

A scheduled GitHub Actions workflow (daily) running the subset of `infra/release-smoke.py` that
is meaningful without a deploy in flight:

- Background jobs are fresh — **read over HTTP from `/healthz`, not via `fly ssh`**
  (CEO review 1B-A). `fly ssh` needs a WireGuard tunnel rather than just an API token, and
  headless CI is slow and flaky at it; a flaky cron is the alert-fatigue failure this item
  explicitly sets out to avoid. Item 15 already publishes this signal, so items 13 and 15
  compose instead of duplicating, and the Fly token is then needed only for the restore-point
  check. The cron does **not** derive a threshold. It applies the `/healthz.jobs` contract:
  exit 1 if any `rows[].stale` is true (name each stale row by `label`, and add "can't record
  runs" when that row's `write_error` is true), or if the reading is unusable (the contract's
  rule, including a malformed body). A NULL row the server has not marked `stale` is a pass.
  CEO review 4C — do not hardcode 15 minutes — is met by the server's per-task `stale` flag,
  not by a second formula in this item. A check against the minimum beat interval would either
  hide a dead hourly sweep behind a fresh 10-minute sync, or false-fail that sweep forever.
  Adversarial review (scoped, item 15a, 2026-09-21). Eng review 5: the unusable-reading rule is
  checked first, the same order Settings uses. A reading that is too old or malformed reports
  that, in one message, and does not also list stale rows from a reading it has just called
  unusable. Both are exit 1; only the message was unpinned.
- A usable restore point exists (smoke check 9).
- The origin lock still holds, both directions (smoke check 5).
- `/healthz` answers and reports a version (item 14).

Deliberately **not** included: scale reconciliation and the frontend/backend commit match, both
of which are release-relative and would produce noise on a schedule.

Failure notification is GitHub Actions' own email on a failed run — sufficient for one operator,
and no new alerting dependency. The workflow reuses the smoke script rather than reimplementing
checks, so a fix lands in one place.

**Cost, stated plainly:** a `FLY_API_TOKEN` in GitHub secrets. That is a broader credential than
the read-only Cloudflare token item 9 already carries dissent about, and it is the main argument
against this item. **Largely answered** (Eng review 8): flyctl v0.4.102 has `fly tokens create
readonly` — "an API token limited to reading a single org and its resources", org-scoped but
read-only, default expiry 20 years — so the token is created with `-x 8760h` (one year) and its
rotation date goes in the RUNBOOK header. PR1b confirms it can run `fly volumes snapshots list`,
`fly pg backup list`, and `fly ips list`; if any is refused, item 13 drops to its credential-free
subset as open question 5 already allows. Never echo it, and keep the workflow
`permissions: contents: read`. The workflow lands in **PR1b**, not PR1a, because its `liveness`
group reads `/healthz.jobs`, which PR1b adds. Retry-once (2F) is two steps: the first with
`continue-on-error: true`, the second run only when the first failed, so a transient network
blip never emails and a real failure still does.

**The watchdog needs a watchdog** (CEO review 8A, CRITICAL GAP). If `ops-check.yml` stops
running — disabled, deleted, or silently not triggering; GitHub disables scheduled workflows on
inactive repositories — nothing says so, and a stopped watchdog looks exactly like a healthy
system. That is this plan's own thesis one level up. The free fix: `RUNBOOK.md`'s pre-release
gates include *"the most recent `ops-check` run is green and recent"*, so the release you already
perform is what watches the watcher.
Adversarial review, three holes in that story:

1. **This repository is public.** GitHub's 60-day disable is not hypothetical here. Workflow
   *runs do not reset the timer* — only commits, PRs, and similar repository activity do. A
   passing daily cron does not keep itself alive. Monthly releases reset the clock; a stretch
   of 50 days since the last commit is a RUNBOOK re-arm ("commit anything, or the watchdog
   dies and nothing between releases will notice"). The pre-release gate catching it is the
   103-day pattern: you find out when you next ship. No keepalive commit bot (pollutes
   history, needs a PAT); no new vendor. Documented limitation of using GitHub as the
   between-release watchdog on a public low-commit repo; v1.1's alerting is what actually
   closes it.

2. **The pre-release gate cannot pass on PR1a's first execution** — the workflow lands in
   PR1b. LESSONS: a gate that inspects behavior only the new code produces cannot pass before
   that code is live. Wording: *"if `ops-check.yml` has ever run, the most recent run is green
   and under 48 h; otherwise skip and record `n/a — workflow not yet on default branch`"*.

3. **Scheduled workflows only fire from the default branch.** Criterion 17's "temporary edit
   on a branch" does not run the schedule. `ops-check.yml` has `workflow_dispatch` with a
   `self_test` boolean; the first green run is `gh workflow run ops-check.yml` immediately
   after PR1b merges, not "wait until tomorrow." The negative case is
   `gh workflow run ops-check.yml -f self_test=true`, which passes `--self-test` plus the
   cron groups (see item 4). Exit 1 fails the job (email). Exit 2 retries once (already),
   then fails the job with a summary line that starts `exit 2 tooling:` so the email is
   distinguishable from `exit 1 production:`. Do not `exit 0` on tooling failure — an
   expired Fly token would then look healthy.

**The alert-fatigue risk is real and is part of the design, not an afterthought.** A daily cron
that flaps trains the operator to ignore it, which is worse than no cron at all. So: the workflow
fails only on a check that is unambiguously broken, every failure names which check and what to
do (linking the diagnostics entry), and if it produces a false failure in its first month the
response is to fix or remove the offending check, not to mute the workflow. That rule goes in
`infra/incident-diagnostics.md` alongside the checks themselves.

**14. `/healthz` reports the deployed commit.**
<!-- CEO review 0: accepted expansion — candidate 2 -->

`backend/app/main.py:280` currently returns `{"status": "ok"}`. `REVIEW_CHECKLIST.md` (FastAPI →
Operations) requires that `/healthz` "is public, cheap, and reports the deployed version" — an
existing violation nobody had noticed, and the reason smoke check 8 could only ever verify half
the system.

`backend/Dockerfile` takes a `GIT_COMMIT` build arg and sets it as an env var; `RUNBOOK.md`'s
deploy step passes `--build-arg GIT_COMMIT=$(git rev-parse HEAD)`; `/healthz` returns it as
`version`. It stays **shallow** — no database, no Redis — so the 2026-04-23 LESSONS decision
about single-instance health probes is untouched. When the build arg is absent the value is
`"unknown"`, and smoke check 8 treats `unknown` as a failure rather than a pass. The response
carries `Cache-Control: no-store` so neither the cron nor the smoke script can read a cached
body (Eng review 1.2), and `tests/unit/test_healthz.py` — whose docstring and exact-body assertion
forbid precisely this change — is rewritten rather than patched (Eng review 2.6).

Two implementation notes from CEO review. **The `ARG` must live in the `prod` stage** (5D):
`backend/Dockerfile` is multi-stage, and putting it in `builder` yields a prod image that
silently reports `"unknown"` — a check that passes nothing and tells no one. And **this is the
highest-risk code change in the plan** (9A, CRITICAL GAP): `/healthz` is on Fly's health-check
path, so a bug or a slow read there fails health checks and Fly kills machines — at `web=1` that
is total downtime caused by a change whose entire purpose was better observability. Mitigations:
the TTL cache (item 15), `test_healthz_issues_no_database_queries`, and a runbook instruction to
deploy this while watching, with the rollback step already read.

**15. Background-job staleness, surfaced in the app.**
<!-- CEO review 0: accepted expansion — candidate 4, added against the reviewer's recommendation -->

Extend the freshness-dot pattern that shipped in `ICloudSettings.jsx` (#49) to a signal that is
true whenever the worker is alive, independent of whether iCloud is connected: the worker writes
a timestamp on every periodic task completion (a `task_postrun` signal, so no individual task has
to remember), the API exposes it, and Settings renders it with the same dot.

**Where it is exposed: on `/healthz`, not a new route** (CEO review 3A-A). The cron needs to
read this without household credentials, and `/healthz` is already public because Fly's health
check requires it — so `last_success_at`, per-row `write_error` and the derived staleness ride
along there rather than adding a third bare public route to a surface whose whole M5 discipline
is that it is small and auditable by one grep. There is no `last_write_error` field. Eng review 4:
the field names and every rule for reading them are pinned once, in "The `/healthz.jobs` contract"
at the end of this item. Adversarial review (scoped, item 15a, 2026-09-21): that contract is the
only staleness rule. Item 13's cron, Settings, and the smoke script all consume it; none of them
re-derives a threshold.

**That makes the read path load-bearing on Fly's health check, so it must not touch the
database.** <!-- Eng review 1.2 (CRITICAL GAP, closed): a TTL cache still issues the read inline
     on a miss; a read that *hangs* (not errors) blows Fly's 2-second probe timeout and the
     machine is killed — at web=1, total downtime caused by the observability change, and open
     question 4 says the Postgres primary stops hourly. --> The web process runs a **background
refresher** started in the lifespan: every 30 s it reads `job_heartbeats` under
`asyncio.wait_for(..., 1.0)` and writes module state; on timeout or error it keeps the last-known
value and sets `read: "stale"` (or `"unavailable"` when nothing has ever been read); it handles
`CancelledError` at shutdown. Any other exception is logged at WARNING and the loop continues;
the task exits only when the lifespan cancels it. A successful read replaces the snapshot
object wholesale. It does not mutate the dict a request might already be reading.
Adversarial review (scoped, item 15a, 2026-09-21). The `/healthz` handler reads memory only and never awaits the store,
so `test_healthz_issues_no_database_queries` asserts exactly that, and a second test proves the
endpoint answers within the probe budget while the store is stubbed to hang. Without this, Fly's
10-second probe would query Postgres forever and a DB flap would start killing machines —
precisely the deep-probe pattern the 2026-04-23 LESSONS decision forbids.

**The signal must not lie in the reassuring direction, and it must distinguish its own breakage**
(CEO review 2B-A). The `task_postrun` handler writes **only when `state == "SUCCESS"`** (Eng
review 3: the signal fires on failure too, and an unfiltered write would record a failed task as a
success) **and only when `sender.name` is a `beat_schedule` task** (Adversarial review).
`backend/app/tasks.py` has sixteen `@celery_app.task`s; `beat_schedule` names four
(`app.tasks.sync_all_icloud_integrations`, `sync_all_reminders`,
`hard_delete_expired_soft_deletes`, `sweep_abandoned_uploads`). The other twelve — including
`health_check`, which smoke check 4 fires on every release — are one-shot. Rows are keyed by
Celery **task name** (`entry["task"]`), not by beat-entry key (`sync-icloud-calendars`). Interval
lookup walks `beat_schedule.values()`. An unfiltered writer plus "worker dead = every row stale"
marks the worker dead the first time someone imports a recipe: `extract_recipe_from_url`
succeeds, gets a row, has no interval, and sits stale forever. Belt and suspenders: the writer
drops unknown names, **and** the reader derives staleness only for the four beat tasks. `health_check`
SUCCESS must not insert a row (test).

The handler runs through `run_async` because the task's own loop is already closed by then, catches
everything — it never taints a task result — logs at WARNING with the store error, and then makes
a second write that sets `last_error`, `last_error_at` and `error_count + 1`. If that write fails
too, the handler logs one more WARNING and stops. **A later successful upsert zeros
`error_count` and clears `last_error` and `last_error_at`.** Without that, one network blip
permanently paints "can't record job runs" even after writes recover.
<!-- Eng review 4 (2A, 2026-09-21): the definition below replaces "`last_error` is within one
     refresher interval". The schema had no error timestamp to measure, and 30 s would have made
     state 2 flash for under a minute and vanish. -->
A row's `write_error` is computed when the request is served: `last_error_at` is set, newer than
`last_success_at` (or there is no success yet), and no older than 3× the task's interval. In
words: the latest attempt to record this job failed, and it failed inside the window in which a
run was expected. Once it is older than that, the row reads as behind or stopped, not "can't
record". `error_count` stays in the table for the diagnostics doc and is not sent. A stale
timestamp *with* `write_error` reads "the writer is broken"; a stale timestamp *without* it reads
"the worker is dead". The cron's failure message names which.
**The limit, stated so nobody relies on more** (Eng review 4, 2A: decided, keep state 2 with this
definition). The error is recorded in the same database whose failure it reports. So when the
database is unreachable from the worker, the table is missing, or the handler itself is broken,
the error write fails too. If the web can still read the table, there is no `write_error`, and once
those rows are stale the card reads "Stopped", not "Can't record job runs". If the web cannot
read it either, the card reads "Can't check", which is the contract's
unusable reading, not state 2 and not state 3. Adversarial review (scoped, item 15a, 2026-09-21):
the previous sentence called every one of these "Stopped". Eng review 5: what decides between
the two is whether the web's reading is usable **now**, not whether it has ever read the table.
The adversarial wording ("still has a snapshot" / "has never read") put the line in the wrong
place. A web process that has a snapshot but can no longer refresh it (the database is down for
web and worker alike, or the table is missing, so the refresher's read raises) shows the
2-minute footnote and then state 1, because state 1 is the first match. It does not wait for a
row to age into "Stopped". A web that never read at all is the same state by a shorter road
(`read: "unavailable"`). "Stopped" therefore means: the web reads fine, and nothing is writing.

| Web reads the table now? | Worker's writes | Card | Cron |
|---|---|---|---|
| yes | success upsert fails, error write lands | state 2 once that row is stale | exit 1, stale labels + "can't record runs" |
| yes | both writes fail (worker cannot reach the database, handler bug) | rows age: state 4, then state 3 "Stopped"; no `write_error` | exit 1, stale labels |
| no, last good read under 2 min old | any | last reading plus the footnote | passes or fails on that reading |
| no, last good read over 2 min old, or never read | any | state 1 "Can't check" | exit 1 "web cannot read job_heartbeats" |

State 2 fires
only when the upsert fails while a separate write still succeeds. The worker's `Task … succeeded`
lines, next to the handler's WARNINGs, stay the tiebreaker, and item 2's "Background jobs" entry
says so. Rejected: recording write errors in Redis so the web could read them (2C). It
distinguishes reliably, but adds a second store to the health data path. Also rejected: removing
state 2 (2B), which would have reversed CEO 2B-A and a design-review state for a gain the logs
already give.
CEO review 4A wanted a NULL row to read as "no data yet" on the first minute, not as an alarm.
That grace is the next paragraph, not a second rule. The cron does not tolerate NULL for one
schedule interval, and it does not know when "a deploy" was. It reads `stale`.
Adversarial review (scoped, item 15a, 2026-09-21): the sentence this replaces was still in the
plan after Eng review 4 and would have made the cron alarm on a different clock from the card.
**Eng review 4 (1A, CRITICAL GAP closed, 2026-09-21): "no data yet" expires.** As first written,
nothing ever turned a NULL row stale, and nothing on the server knew when "a deploy" was. PR1b
deployed onto a stopped worker — the exact 2026-09-11 state (LESSONS: "`fly deploy` leaves an
already-stopped machine stopped") — would have shown "Waiting for the first runs" in gray, with
no alert, forever, and the cron would have read "no data yet" forever: the 103-day outage
rebuilt inside the tool meant to catch it. The web process records `started_at` in the lifespan
(memory only). A NULL row is stale once `now − started_at > 3 × interval`, the same threshold as
a row that has run. Every deploy restarts the web process, so the grace is exactly "after a
deploy". It is 3× and not 1× because beat first fires an interval job one full interval after it
starts, so a 1× grace would false-alarm on every hourly sweep. A web crash-restart restarts the
grace only for NULL rows; timestamped rows are unaffected.
**The limit, stated so nobody treats a restart as a recovery** (Adversarial review, scoped,
item 15a, 2026-09-21). `started_at` is this process's memory. Restarting web — a crash, a
deploy, or an operator "kick" while answering the alarm — rearms the grace for every row that
is still NULL. Rows the previous process had already marked overdue read as not stale again
until another 3× elapses, and the cron goes quiet for that long. A green jobs check in that
window does not mean the worker is up. Look at `fly status` for `worker` and `beat` before
restarting web to clear "Waiting" or "hasn't run yet". The grace is also not a deploy
timestamp shared across machines. This plan's `web=1` (item 7) is what makes it one clock. At
`web>1` the cron and the card see whichever machine answered, and two machines started minutes
apart disagree about NULL rows. A database that comes back empty without a web restart is the
opposite case: `started_at` is old, so those NULL rows are stale immediately. That includes a
restore of a pre-table snapshot onto the database this web process is already reading. The
drill does not do that; it restores into a scratch app.
Eng review 5: "empty" means the table exists and holds no rows: a `TRUNCATE`, an
`alembic downgrade` then `upgrade`, or a pre-table snapshot *after* someone ran
`alembic upgrade head` by hand. While the table is missing, the refresher's read raises, so that
window is an unusable reading ("Can't check", cron "web cannot read job_heartbeats"), not
overdue rows. Nothing here is new code. `build_jobs_payload(snapshot, now, started_at)` is pure,
and "restore without a restart" is the input (all-NULL snapshot, old `started_at`). The
refresher replaces the snapshot wholesale, so it must not carry a timestamp over from the
reading before. With a live worker the false alarm is bounded by one interval per job (10 min
for the syncs, an hour for the sweeps), and it errs toward alarm. When a restore lands on a
*new* cluster, pointing the app at it is a secret change, and `fly secrets set` restarts web
unless staged. Then NULL rows get the grace, and rows restored with old timestamps are overdue
at once until each job runs again.

A fifth state, `read:
"stale"` or `"unavailable"` from the refresher, renders as "can't check" (Eng review 1.2).
Design review 2B: superseded for `"stale"` — Settings shows the last reading with a note for up to
2 minutes, using `jobs.read_at`; `"unavailable"` still renders "can't check". See 15a.
**Thresholds are per task** (Eng review 2.5): each row's staleness is three times *its own*
`beat_schedule` interval — 30 min for the 10-minute syncs, 3 h for the hourly sweeps — derived on
the server from `celery_app.conf.beat_schedule`, and "worker dead" means every row is stale, so
the frontend and the cron consume `stale` flags rather than re-deriving them.

**Reuse `parseServerTime`, do not rewrite it** (CEO review 4B-A). The API serializes
`timestamp without time zone` with no `Z`, and `new Date()` reads that as local time — the bug
logged 2026-09-11, where a five-hour-old sync rendered "just now". The fix exists at
`ICloudSettings.jsx:545` but is **module-private**. Extract `parseServerTime` *and* the
freshness-dot presentation into a shared module, and have both `ICloudSettings` and the new jobs
indicator consume it: the two surfaces show the same concept, and the bug then has exactly one
place it can ever be fixed. Tests pin `process.env.TZ` to `America/Los_Angeles` and use the API's
real no-`Z` shape — the old fixtures used `toISOString()`, whose `Z` hid the bug entirely.
Visual-regression coverage is **not** added (CEO review 6A-A); unit tests cover the four states
and extending the VRT suite to Settings stays with its existing TODO.

**Settings must not use the authenticated API client for `/healthz`.** Adversarial review:
`frontend/src/lib/api.js` attaches the bearer and on 401 runs silent refresh then redirects to
`/auth`. `/healthz` is public and gate-exempt, but a 421 (mis-deployed origin lock), a CORS
failure, or a missing `jobs` key handled as a 401-shaped error would log the household out of
Settings while they are looking at the "are jobs alive?" line. Fetch with `apiUrl('/healthz')`
(exported by `frontend/src/lib/apiBase.js:66`, which imports no axios — Eng review 4) and
`cache: 'no-store'`, no `Authorization`, no interceptor. Missing `jobs` renders "can't
check" and never throws (Design review 2: Settings reads only `jobs`, so a missing
`gate_break_glass` or `version` changes nothing it renders) — that is the backend-first window (new
frontend, old `/healthz`) and the image-rollback window (PR1b frontend, PR1a backend). Poll
while Settings is mounted (`refetchInterval: 30_000`, stops on unmount); a one-shot on mount
is the 103-day outage for anyone who does not reload.

**The `/healthz.jobs` contract** (Eng review 4, 2026-09-21). Before this, the shape lived in prose
across items 13, 15 and 15a, open question 6 and the test artifact, and those places disagreed
(`jobs.rows` vs every task listed, `worker: "dead"`, a failure counter "that `/healthz` also
reports"). Three readers in two pull requests parse it: PR1a's smoke script, and PR1b's Settings
card and `ops-check.yml`. It is pinned here once. PR1b gives it a Pydantic response model. The
Settings MSW fixtures and the `infra/tests` fixtures copy this example; there is no cross-suite
contract test, because the api container does not mount `infra/`. The first PR1b smoke run is
the integration point, and drift there fails loudly (exit 1), never silently.

```
GET /healthz → 200 · Cache-Control: no-store · the handler never awaits a store
{
  "status": "ok",
  "version": "<git sha>" | "unknown",                          item 14
  "gate_break_glass": false,                                   open question 3
  "jobs": {
    "read": "ok" | "stale" | "unavailable",                    refresher outcome (Eng review 1.2)
    "read_at": "2026-09-21T17:02:11Z" | null,                  last successful refresher read;
                                                               null iff read == "unavailable"
    "now": "2026-09-21T17:02:40Z",                             serve time; every age is measured
                                                               against this, never a browser clock
    "rows": [                                                  [] iff read == "unavailable";
                                                               otherwise one per beat_schedule task,
                                                               in beat_schedule order
      {
        "task": "app.tasks.sync_all_icloud_integrations",
        "label": "iCloud calendar sync",                       the server's map; unmapped names are
                                                               humanized (3A)
        "interval_s": 600,
        "last_success_at": "2026-09-21T16:58:03Z" | null,
        "stale": false,                                        per request (1A for NULL rows)
        "write_error": false                                   per request (2A)
      }
    ]
  }
}
```

- **Timestamps** are UTC and serialize with a `Z`.
- **`stale`**: if `last_success_at` is set, `now − last_success_at > 3 × interval_s`; if it is
  NULL, `now − started_at > 3 × interval_s` (1A). **`write_error`** as defined above (2A).
  Both are computed per request from the in-memory rows. Neither is ever frozen at the
  refresher's last read.
- **No summary field.** "Every row stale" (stopped) is derived by each reader from
  `rows[].stale`. It is one comparison, not a precedence table. `worker: "dead"` is retired.
- **One rule for an unusable reading, applied by every reader**: `read == "unavailable"`, or
  `read_at` is null, or `now` does not parse, or `now − read_at > 120 s`, or `rows` is not an
  array, or `read` is `"ok"` or `"stale"` and `rows` is empty, or any row is missing `task`,
  `label`, or `interval_s`, or `stale` or `write_error` is not a boolean. Settings renders
  state 1 and does not throw. The smoke script exits 1 with "web cannot read job_heartbeats"
  when the reading is unavailable or older than 120 s, and with "malformed jobs" for a shape
  break, so a producer bug is not mistaken for a database outage. A missing `stale` is never
  read as fresh. Since `/healthz` stays shallow, an unusable reading is also the cron's only
  sign that the web cannot reach the database.
  Adversarial review (scoped, item 15a, 2026-09-21): the previous sentence covered only
  `unavailable` and a 120 s `read_at`. A partial row would have rendered green.
- **The browser never compares a server timestamp with its own clock** (Eng review 4). A
  kitchen tablet running 3 minutes fast would otherwise show "Can't check right now" on every
  visit, and one running slow would never cap a frozen reading. `useBackgroundJobs` computes the
  server-aligned now as `Date.parse(jobs.now) + (Date.now() − dataUpdatedAt)` and passes it to
  `deriveJobsState(jobs, now)`. Every age, the state 3 headline, the footnote and the 2-minute
  cap use it. `deriveJobsState` is called on each render with that `now`. It is not a TanStack
  `select`: `select` does not re-run when `data` is unchanged, so a run of failed polls would
  freeze the ages and the 2-minute cap on the last success. While Settings is visible, a failed
  poll still schedules the next one, and the cap is evaluated even though `data` did not change.
  The client does not recompute `stale` or `write_error`. A row the server marks fresh does not
  gain "overdue" between polls, and a `write_error` on a row that is not stale does not change
  the card. Adversarial review (scoped, item 15a, 2026-09-21).
- **Labels live in the backend** (Eng review 4, 3A: decided). `JOB_LABELS` maps the four task
  names to item 15a's labels, with a humanized fallback. The Settings card, the top alert and the
  smoke script's failure line print `label` as sent, so Design review 4A's "one name everywhere" is
  mechanical rather than two hand-kept maps. The cadence text ("runs every 10 min" / "runs every
  hour") is formatted from `interval_s`. The "What this means" lines stay in the frontend, keyed by
  task name; an unmapped task contributes no line of its own. Cadence text is one function of
  `interval_s`: `3600` → "every hour"; any other positive multiple of 60 → "every N min";
  otherwise "every N sec". The four real tasks use the first two. Adversarial review (scoped,
  item 15a, 2026-09-21).
- **Where it lives** (Eng review 4). `backend/app/job_health.py` holds:
  - `beat_intervals()`, derived once from `celery_app.conf.beat_schedule` and imported by both the
    worker's `task_postrun` filter and the web's payload, so there is one definition of "which
    tasks, how often". It raises on any schedule that is not a number or a `timedelta`: a future
    `crontab()` has no interval, and silently skipping it would hide a job. A structural unit test
    runs it over the real schedule.
  - `JOB_LABELS`.
  - The pure `build_jobs_payload(snapshot, now, started_at)`.
  - The refresher loop, with an injectable reader.

  `main.py` only starts and cancels the refresher in the lifespan and returns
  `build_jobs_payload(...)` from `/healthz`. The module is a new top-level file, so item 10 routes
  it in the doc map.
- **Expected side effect on open question 4.** The refresher's 30-second read is a steady
  database client, so expect it to keep `mealy-app-prod-db` from idle-stopping. If the primary
  still stops, each wake costs one or two `read: "stale"` cycles, which the 2-minute cap absorbs.
  PR2 records which happened.

**15a. What the household sees on Settings** <!-- Design review (plan-design-review, 2026-09-14) -->

Settings is opened by anyone on the shared household login, usually to change something else
(add a family member, the timezone, a meal slot). The indicator is therefore designed for two
readers at once: a household member who should understand it without knowing what Celery is,
and the operator, who needs to tell "one job is behind" from "the worker is dead" at a glance.

*Information architecture* (Design review 1A: decided — own card plus a conditional top alert).
Two surfaces, one data source: both read the same `/healthz` query, so they can never disagree.

```
  /settings
  +--------------------------------------------------------------+
  | Settings                                            (h1)     |
  | (red dot) iCloud calendar sync is behind schedule ·         |  <- top alert: rendered ONLY when a
  |           See background jobs                                |
  +--------------------------------------------------------------+     job is behind, all are stopped, or
  | Family Members card                                          |     runs can't be recorded; absent when
  | Timezone card                                                |     healthy, no data yet, or can't check
  | Calendar Integrations card   (iCloud "Last synced" dot)      |
  | Mealboard card                                               |
  +--------------------------------------------------------------+
  | Background jobs                          (h2, #background-jobs)
  | Work the app does on its own: iCloud sync and clearing out   |  <- one-line description, same
  | deleted items and unused photos.                             |     pattern as the Timezone card
  |                                                              |
  | PRIMARY    (dot) All running on schedule        (headline)   |
  | SECONDARY  iCloud calendar sync          ran 4 min ago       |
  |            iCloud reminders sync         ran 4 min ago       |
  |            Deleted item cleanup          ran 32 min ago      |
  |            Unused photo cleanup          ran 32 min ago      |
  +--------------------------------------------------------------+
  | Account card (Sign out)                                      |  <- stays last, by convention
  +--------------------------------------------------------------+

  top alert --(See background jobs)--> scrolls to #background-jobs, focus lands on its h2
```

- **Top alert, first and only:** what is wrong, in plain words, then a "See background jobs" link. No
  dismiss control — the alert disappears when the jobs recover, because a dismissible outage
  warning is how an outage gets ignored. It is not shown for "can't check" (a deploy or edge
  problem, not a job failure, and not something to put at the top of the household's Settings).
  Design re-review (scoped, item 15a, 2026-09-21): since Eng review 5, "can't check" also covers
  a database or a table the web cannot read now, which the earlier wording would have called
  "Stopped" with an alert and a hand-off line. It still gets neither. With no reading the card
  has nothing true to say about the jobs. When the database is down, every other screen is
  already showing errors (journey row 10). When only the table is missing, the jobs still run
  and the household loses nothing. The cron's "web cannot read job_heartbeats" reaches the one
  person who can act.
- **Card, first:** one headline stating the overall state, with the dot. **Second:** one row per
  beat job, plain-English name plus how long ago it last succeeded. **Third:** nothing.
- **The per-job rows are always visible** in every state that has a reading (Design review 8C:
  decided — mockup option A, over "exceptions only" and a "Show all 4 jobs" toggle). Green is
  backed by four visible ages that tick on each poll, which is what the operator checks after a
  deploy, and the card keeps one height as its state changes, so the Account card below never
  jumps during a 30-second poll. See the Design Source of Truth.

*Interaction states* (Design review 2). **The headline follows the worst job, never the most
recent success.** Summarizing four jobs as "ran N min ago" from the newest timestamp would let a
dead upload sweep hide behind a healthy iCloud sync — the reassuring lie CEO review 2B-A forbids.
The card's state is the first row of this table that matches:

| # | Card state | When | Dot | Headline | Per-job rows | Top alert |
|---|---|---|---|---|---|---|
| 1 | Can't check | never loaded and the fetch failed; `jobs` key missing; `jobs.read` is `"unavailable"`; the newest reading is more than 2 min old (2B); or the contract calls the reading unusable, including a malformed `jobs` object, a null `read_at`, or a `now` that does not parse (Adversarial review, scoped, item 15a, 2026-09-21) | gray | "Can't check right now" | hidden; one muted line "Trying again every 30 seconds." | none |
| 2 | Can't record | any stale row has `write_error` true (the contract, 2A) | red | "Can't record job runs" | affected rows (stale **and** `write_error`; Design re-review, 2026-09-21): "can't record runs" in red; others normal | "Background jobs can't be recorded · See background jobs" |
| 3 | Stopped | every row stale | red | "Nothing has run in 3 hr" (age of the newest success); "Nothing has run yet" when no row has ever succeeded (Eng review 4, 1A) | every row "last ran N ago · overdue" in red; a never-run row "hasn't run yet · overdue" | "Background jobs have stopped · See background jobs" |
| 4 | Behind | some rows stale | red | "1 job is behind schedule" / "2 jobs are behind schedule" | stale rows "last ran 3 hr ago · overdue" in red; a never-run row past its grace "hasn't run yet · overdue" in red (Eng review 4, 1A); fresh rows normal | one job: "Unused photo cleanup is behind schedule · See background jobs"; several: "2 background jobs are behind schedule · See background jobs" |
| 5 | Waiting | any row has never run (`last_success_at` NULL) and none is stale, i.e. every NULL row is still inside 3× its interval of **this web process** starting (Eng review 4, 1A). A restore or downgrade that empties the table without restarting web is not this row: `started_at` is already old, so those NULL rows are stale (Adversarial review, scoped, item 15a, 2026-09-21) | gray | every row NULL: "Waiting for the first runs". Some rows have a success: "Waiting on 1 job" / "Waiting on N jobs" (N = how many are still NULL) | NULL rows "hasn't run yet · runs every 10 min" / "runs every hour"; rows that have run show "ran N ago" | none |
| 6 | Running | every row fresh | green | "All running on schedule" | "ran 4 min ago" ("ran just now" under a minute) | none |

`write_error` on a row that is not stale does not match row 2 and does not change row 6. The
card stays "All running on schedule" until that row is also stale. Adversarial review (scoped,
item 15a, 2026-09-21).

Eng review 5: two pins on the table, neither a new rule. In row 2, "others normal" means every
other row renders by its own `stale` flag, as it would in state 4: a stale row with no
`write_error` still reads "last ran N ago · overdue" in red (the fixed-order bullet below:
"red text and the word overdue mark the stale ones"). It never renders as fresh, or the
"Nothing has run" line would sit above rows that look healthy. And row 1 outranks row 3 when
both match: a reading the web cannot refresh is "Can't check" once it is 2 minutes old, whatever
its rows say, so an outage that takes the database away from web and worker together never
reads "Stopped" (item 15's limit).

**What a row says** (Design re-review, scoped, item 15a, 2026-09-21). A row's text and color
follow that row's own values, never the card's state. The cells above are instances of this,
and the mockup's `rowHtml` is written from it. Not a new rule: it is the contract's "a
`write_error` on a row that is not stale does not change the card", said per row.

| The row's own values | Row text | Color |
|---|---|---|
| `stale` and `write_error` | "can't record runs" | red |
| `stale`, no `write_error`, has succeeded before | "last ran N ago · overdue" | red |
| `stale`, no `write_error`, never succeeded | "hasn't run yet · overdue" | red |
| not `stale`, never succeeded | "hasn't run yet · runs every 10 min" / "runs every hour" | normal |
| not `stale`, has succeeded | "ran N ago" ("ran just now" under a minute) | normal |

So a fresh row that carries `write_error` reads "ran N ago" in state 2, as it does in state 6.
Row 2's "affected rows" left that open, and the mockup had keyed the red text on `write_error`
alone. That is the recorder-broken, worker-alive picture about 45 minutes in: both syncs "can't
record runs", both sweeps still "ran 1 hr ago". Keyed on the flag alone, state 6 would put red
text under a green headline, and a row would change its words because of its neighbours. The
rows are the headline's evidence, so they must not depend on it.

| Feature | Loading | Empty | Error | Success | Partial |
|---|---|---|---|---|---|
| Background jobs card | heading and description render at once (static); the body shows nothing for 200 ms (`useDelayedFlag`), then a gray dot and "Checking…"; no skeleton, no rows | state 5, with each NULL row's cadence so the reader knows how long to wait. The headline is "Waiting for the first runs" only while every row is NULL; once any has succeeded it is "Waiting on N jobs" | state 1 when there is no usable reading; states 2–4 when there is one and it is bad | state 6 | state 4 (some jobs behind); or any state plus a muted footnote "Couldn't refresh · showing results from 1 min ago" while the newest reading is under 2 min old but the last fetch failed or `jobs.read` is `"stale"` (2B) |
| Top alert | not rendered | not rendered | not rendered for state 1 | not rendered | rendered for states 2–4 only |
| iCloud "Last synced" line | unchanged | unchanged | unchanged | unchanged | same text and red overdue state after the shared-module move; the fresh dot changes from stock `green-500` to `sage-500` (Design review 5A) and the fresh text from `text-text-muted` to `text-text-secondary` (Design review 6A) |

- **Stale reading, not a flap** (Design review 2B: decided — last reading plus a note). This
  supersedes Eng review 1.2's "`read: "stale"` renders as can't check": that rule would flip the
  card to "Can't check" for about a minute every time the Postgres primary wakes (open question
  4) and on every dropped poll, which trains the reader to ignore it. Showing the last reading
  stays honest only because each job's staleness keeps advancing from its real timestamp, so
  two backend requirements follow: `stale` is computed **when the request is served**, from the
  in-memory rows and the current time — never frozen at the refresher's last successful read —
  and `jobs` carries `read_at`, the time of that last successful read (memory only). "Newest
  reading" above means `jobs.read_at` from the most recent response in hand, and "more than 2 min
  old" is measured on the server-aligned clock (`jobs.now` plus the time since that response
  arrived), never against the browser clock (Eng review 4, the contract above).
- **Recovery is quiet.** When jobs recover, the headline and dot change on the next poll and
  the top alert unmounts. No toast, no animation.
- **Rows keep a fixed order** (the two 10-minute syncs, then the two hourly sweeps), so they do
  not reshuffle every 30 seconds; red text and the word "overdue" mark the stale ones. The
  headline carries the card's only dot (Design review 5: one dot per card, not five).
- **A job is never dropped.** A beat task with no label mapping arrives with its task name
  humanized by the server (`sweep_abandoned_uploads` → "Sweep abandoned uploads"; Eng review 4,
  3A), because a missing row hides a stale job. Names outside `beat_schedule` never reach the card
  (the server filters them).
- **Settings depends only on `jobs`.** A missing `version` or `gate_break_glass` key does not
  change anything Settings renders, and Settings never renders either (Design review 7A, 7B).

*User journey* (Design review 3). The next person to read this card may not be the operator —
the PRD's target state is a second household on its own deployment — so it speaks to both
(Design review 3A: decided). In states 2–4 the card carries **one line under the headline saying
what the problem means for the reader**, and states 2 and 3 add a next step. The top alert stays
one line.

| Stale job, or state | "What this means" line |
|---|---|
| iCloud calendar sync | "If you use iCloud sync, new or changed events may not show up yet." |
| iCloud reminders sync | "If you use iCloud sync, reminder changes may not sync yet." |
| Deleted item cleanup, Unused photo cleanup | "Nothing you'll notice yet." |
| State 3, stopped | "iCloud events and reminders won't sync until this is fixed. If this lasts more than an hour, tell whoever set up this app." |
| State 2, can't record, and some row is not stale, **or** every row is stale and every row has `write_error` | "They may still be running, but the app can't confirm it. If this lasts more than an hour, tell whoever set up this app." |
| State 2, can't record, every row is stale, **and** at least one row has no `write_error` | "Nothing has run, and the app can't record that either. If this lasts more than an hour, tell whoever set up this app." Adversarial review (scoped, item 15a, 2026-09-21): state 2 is checked before state 3, so one `write_error` on a stale row used to put the whole card on "may still be running" after every job had stopped. The headline and the alert stay "Can't record". |

Eng review 5 (1A: decided, 2026-09-21). The adversarial split was keyed on "every row is stale"
alone, and that is the wrong predicate. `write_error` is stamped only when a beat task reached
SUCCESS and its upsert failed, inside 3× that task's interval, so a row that carries it is proof
that job ran. With a broken recorder and a live worker, which is the case state 2 exists for
(CEO review 2B-A; "a stale timestamp *with* `write_error` reads the writer is broken"), every row
is stale after 3 h and every row has `write_error`. The card would then have said "Nothing has
run" for as long as the fault lasted, while every job was running. So the second line needs
evidence that something stopped: a stale row with no `write_error`. That keeps the adversarial
case (a worker that died after a write error: the rows without `write_error` decide it) and
both strings word for word. "Not stale" here is `stale: false`, so a NULL row inside its grace
counts. As one expression: `rows.every(r => r.stale) && rows.some(r => !r.write_error)` picks the
second line; anything else in state 2 picks the first.

Adversarial re-review (scoped to Eng review 5, 1A, 2026-09-21): that expression is the one to
implement, and it holds for both pictures it was aimed at. A broken recorder and a live worker —
every row stale, every row `write_error` — stays on the first line. That includes a row whose
`last_success_at` is still NULL: the flag means a SUCCESS whose upsert failed, not a success
that was recorded, so a never-recorded row does not by itself pick "Nothing has run". All-stale
with at least one row that has no `write_error` stays on the second line. The first line is also
what those rows look like for at most 3× the shortest flagged job's interval after the worker
dies (30 min when a 10-minute sync's error stamp is fresh). Nothing in the rows says the worker
stopped, so the copy does not split the two pictures and does not prove the worker is up. It is
not a green card: the headline, the top alert, and the cron (any stale row is exit 1) already
say the rows are stale. The line flips to the second one when the shortest flag ages out. No
third string, and no timer in the client. This bounded window is not the understatement the
next paragraph rejects. That paragraph is a line that can see a stale row with no flag, and the
rejected third string that would hide a dead worker for up to 3 hours.

**The limit of the second line, stated where the copy lives** (Design re-review, scoped, item
15a, 2026-09-21). State 2 always has a flagged row. So whenever "Nothing has run" shows, one job
did run inside its window and only its recording failed. After a worker dies, that was the last
run before it died: the flag ages out within 3× that job's interval, and state 3 takes over with
a true "Nothing has run in N". With two faults at once (a recorder that fails for some jobs
while another job stops for its own reasons), the flagged jobs keep running and the line
overstates for as long as both faults last. That is the direction this card is allowed to be
wrong in (CEO review 2B-A: never the reassuring one). The alternative wording, "Some jobs have
stopped", would understate a dead worker for up to 3 hours, and one fault is likelier than two.
The rows under the line say which job is which, the household's next step is the same sentence
either way, and the worker's `Task … succeeded` lines stay the operator's tiebreaker. Both
strings stand.

When several jobs are behind, their lines are shown once each, calendar first, and "Nothing
you'll notice yet." only when it is the sole line. The copy says "if you use iCloud sync" rather
than reading the integrations list, so the card stays independent of iCloud (and of the
authenticated API).

| Step | User does | User feels | Plan specifies |
|---|---|---|---|
| 1 | Parent opens Settings to change the timezone; jobs are healthy (the thousandth visit) | nothing; should not notice | no top alert; a quiet green card above Account (state 6) |
| 2 | Parent opens Settings because the calendar looks out of date | "is it me?", then relief that it has a name | top alert names the job; the card says what it means ("…new or changed events may not show up yet.") |
| 3 | A second household's parent sees "Background jobs have stopped" | worry, then a next step | "If this lasts more than an hour, tell whoever set up this app." (3A) |
| 4 | Operator gets the `ops-check` failure email and opens Settings on a phone | wants confirmation, fast | the headline carries the age of the newest success; rows show which job; the card refreshes every 30 s without a reload |
| 5 | Operator leaves Settings open right after a deploy | reassurance | rows keep their timestamps across a deploy (the table persists); ages tick on each poll |
| 6 | Operator opens Settings after the first PR1b deploy (web restarted onto an empty table) | "is it broken?" | state 5 while nothing is stale: "Waiting for the first runs" until the first success, then "Waiting on N jobs" with no top alert until the rest succeed. A restore that does not restart this web process is overdue, not Waiting |
| 7 | Operator rotates the password while a household member is mid-edit on the kitchen tablet | confusion, maybe alarm ("were we hacked?"); the unsaved form is lost | RUNBOOK: tell the household before rotating, unless the rotation is because the password leaked (then rotate first, tell after). The sign-in page's existing "Your session ended" banner gains the likely causes (item 6, Design review 10 TODO 1) |
| 8 | Someone saves a meal during the `web=1` machine replacement | "did it save?" | the existing error toast and optimistic revert; RUNBOOK: deploy at a quiet hour, and say so if someone is using the app; the execution log records the observed downtime in seconds |
| 9 | A tab left open across the deploy hits a missing JS chunk | a flash, then it works | the existing chunk-load invariant, verified by the runbook's manual check |
| 10 | Anyone opens Settings while the edge or API is degraded, or while the web process cannot read the jobs table (Eng review 5; Design re-review, 2026-09-21) | nothing more alarming than the rest of the app | state 1, "Can't check right now", and no top alert. For up to 2 minutes before that, the last reading with the "Couldn't refresh" footnote. Never "Stopped": that word is kept for a table the web reads fine and nothing writes to |
| 11 | Operator, or a second household's parent, sees "Can't record job runs" (Design re-review, scoped, item 15a, 2026-09-21) | "are the jobs dead, or only the bookkeeping?" | the line under the headline answers from evidence (Eng review 5, 1A). "They may still be running…" while some row is fresh or every stale row is flagged, because a flag proves that job ran. "Nothing has run, and the app can't record that either." once every row is stale and one of them has no flag. The headline, the top alert and the hand-off sentence are identical in both, so the household's next step never changes and the status region stays quiet. The rows show which job is which |
| 12 | Operator empties or restores `job_heartbeats` under a running web process (not the drill, which uses a scratch app) and opens Settings (Design re-review, scoped, item 15a, 2026-09-21) | a jolt, "did I break it?", then watching it clear | not Waiting, because this web process started long ago. State 3: "Nothing has run yet", every row "hasn't run yet · overdue", top alert "Background jobs have stopped". With a live worker the syncs land within about 10 minutes and the card steps down to state 4, "2 jobs are behind schedule" with "Nothing you'll notice yet.", until the sweeps land within about an hour. Only the state 3 stage carries the hand-off line, and it asks the household to wait an hour; that stage lasts about 10 minutes, so nobody is sent to the operator over it |

- **5 seconds:** the dot and one headline. Green means move on; red names the job.
- **5 minutes:** the operator can leave Settings open after a deploy and watch the ages tick
  without reloading.
- **5 years:** green has never been unearned, and neither has gray. The worst-job headline,
  serve-time staleness, the 2-minute stale-reading cap, and a "waiting" that expires (Eng review
  4, 1A) are what let the operator believe the dot instead of running
  `fly status`. The failure to guard against is a card that sits green for years while quietly
  wrong.

*Specific, not generic* (Design review 4). App UI; the card follows the Settings page's existing
section shell rather than restructuring the page (a stacked-card page is pre-existing and out of
scope). **One name everywhere** (Design review 4A: decided — "Background jobs"): the heading, the
top alert, the link text, `incident-diagnostics.md`'s dead-worker entry, and the `ops-check`
failure line for the jobs-fresh assertion all say "Background jobs" and use the job labels
below, so what the operator reads in an email is what the household sees on screen. Eng review 4
(3A): the Label column is the backend's `JOB_LABELS` in `backend/app/job_health.py`, sent as each
row's `label`; the Cadence column is formatted from `interval_s`. The frontend holds neither.

| Celery task (`beat_schedule`) | Label | Cadence shown |
|---|---|---|
| `app.tasks.sync_all_icloud_integrations` | iCloud calendar sync | every 10 min |
| `app.tasks.sync_all_reminders` | iCloud reminders sync | every 10 min |
| `app.tasks.hard_delete_expired_soft_deletes` | Deleted item cleanup | every hour |
| `app.tasks.sweep_abandoned_uploads` | Unused photo cleanup | every hour |

**Do not build** — each is the generic version an implementer would otherwise reach for:

- **No banner.** The top alert is a line of text directly under the `h1`: the same 6 px dot,
  `text-sm`, then an inline link. No container, background tint, colored left border, warning
  icon, or close button.
- **No pulse.** The dot is static in every state. A pinging "live" dot is a status-page trope;
  the 30-second poll already changes the text when something changes.
- **No pills or badges on the rows.** The iCloud card's `Active` / `Error` pills sit right above
  this card and are tempting to reuse; four of them turn a list into a badge mosaic. Rows are
  plain text: label, then age.
- **No tiles, icons, emoji, sparklines, or uptime bars.** Four jobs are a list, not a dashboard;
  a calendar glyph and a trash glyph add nothing the label does not.
- **Copy is utility language:** state, age, consequence, next step. No "All systems
  operational", no "Everything looks great!", no exclamation marks. "Tasks" is never used — it is
  the name of the app's to-do feature.
- **Link text names the destination** ("See background jobs"), never "See details" or "Learn
  more", so it reads correctly in a screen reader's link list.

*Design-system alignment* (Design review 5). Nothing here adds visual vocabulary; every element
copies a sibling on the same page. **Status colors** (Design review 5A: decided — sage plus
stock red): the shared dot uses `sage-500`, the documented success token, instead of stock
`green-500`, which changes the iCloud line's fresh dot too so the two stay matched; red stays
stock `red-500`/`red-600`, the app's de facto error color (237 uses, never documented). PR1b's
`/update-docs` adds both to `FRONTEND_GUIDELINES.md` §1 as the status colors and the freshness
dot to §5 as a pattern; that file is design-watched, so expect design-sync drift afterwards. No
`index.css` token change.

| File | New, moved, or edited | Why it earns its place |
|---|---|---|
| `frontend/src/lib/serverTime.js` | moved from `ICloudSettings.jsx`: `parseServerTime`, `relativeTime`; new sibling `durationSince(date)` → "45 min" / "3 hr" / "2 days" for the state 3 headline (Design review 7) | two consumers; the 2026-09-11 naive-UTC bug gets exactly one place to be fixed |
| `frontend/src/components/shared/FreshnessDot.jsx` | new, extracted from `LastSynced` | two consumers; one prop, `tone: "ok" \| "bad" \| "unknown"`; renders no text, because every caller puts words beside it |
| `frontend/src/hooks/useBackgroundJobs.js` | new | the one `useQuery` both surfaces call (TanStack dedupes them to one request per interval), plus a pure exported `deriveJobsState(jobs, now)` that returns the 15a state, headline, consequence lines and rows, so the precedence table is unit-tested without rendering. Eng review 4: the hook passes the server-aligned `now` (`jobs.now` + time since `dataUpdatedAt`), and rows carry the server's `label`; no name map here. Adversarial review (scoped, item 15a, 2026-09-21): derive on each render, not in `select`, so failed polls still advance the 2-minute cap |
| `backend/app/job_health.py` | new (Eng review 4) | `beat_intervals()`, `JOB_LABELS`, the pure `build_jobs_payload(snapshot, now, started_at)`, and the refresher loop; one definition of "which tasks, how often" shared by the worker's filter and `/healthz` (see the contract in item 15) |
| `frontend/src/components/settings/BackgroundJobsSection.jsx` | new | the card |
| `frontend/src/components/settings/BackgroundJobsAlert.jsx` | new | the top line; returns `null` outside states 2–4 |
| `frontend/src/components/settings/ICloudSettings.jsx` | edited | `LastSynced` imports the two shared pieces; `syncIsOverdue` and `SYNC_STALE_AFTER_MS` stay (the P3 TODO) |
| `frontend/src/pages/FamilyMembersPage.jsx` | edited | wraps the `h1` and alert in a `<header>`; mounts the section directly above `AccountSection` |

| Element | Classes (light / dark paired) | Copied from |
|---|---|---|
| Page header | `<header className="mb-6 sm:mb-8">` around the existing `h1`, which drops its own `mb-6 sm:mb-8` | — |
| Top alert | `<p className="mt-2 text-sm text-red-700 dark:text-red-400">` — **inline text, not flex** (Design review 8D: the mockup showed a flex row breaks awkwardly at 375 px), so it wraps like a sentence; the dot is `FreshnessDot` with `align-middle mr-1.5 mb-0.5`; the `·` separator is `aria-hidden`. `red-700`, not `red-600`, because it sits on the page gradient, where `red-600` measures 4.3:1 at the `warm-beige` end (Design review 6) | `LastSynced` overdue line |
| Alert link | `py-3 sm:py-0 whitespace-nowrap font-medium text-terracotta-700 dark:text-blue-400 underline underline-offset-2 hover:decoration-2 rounded focus:outline-none focus:ring-2 focus:ring-terracotta-500 dark:focus:ring-blue-500` — an **inline** link, so `py-3` gives a 44 px hit area on phones without changing the line height; `whitespace-nowrap` because the mockup showed "See background / jobs" splitting across lines at 375 px (8D). **Underlined** because terracotta beside red is too close in hue to tell link from alert by color alone; `terracotta-700` because `terracotta-600` is 3.6:1 on the gradient (Design review 6) | Guidelines §9 focus ring; dark-mode mapping |
| Card shell | `<section aria-labelledby="background-jobs">` with `bg-card-bg dark:bg-gray-800 rounded-xl border border-card-border dark:border-gray-700 p-6 mt-6` | the four sibling cards |
| `h2` | `id="background-jobs" tabIndex={-1}` + `text-lg font-semibold text-text-primary dark:text-gray-100 mb-4 scroll-mt-20 sm:scroll-mt-6 focus:outline-none` | sibling cards |
| Description | `text-sm text-text-secondary dark:text-gray-400 mb-3 max-w-prose` | Timezone card, one gray darker (6A) |
| Headline | `flex items-start gap-1.5 text-sm font-medium`, with the dot given `mt-[7px]` so it stays on the first line when the headline wraps on a phone (8D; `items-center` would float it between two lines); states 1, 5, 6 `text-text-primary dark:text-gray-100`; states 2–4 `text-red-600 dark:text-red-400` | `LastSynced` |
| `FreshnessDot` | `inline-block w-1.5 h-1.5 rounded-full shrink-0`; ok `bg-sage-500 dark:bg-green-400`, bad `bg-red-500 dark:bg-red-400`, unknown `bg-text-muted dark:bg-gray-500`; `aria-hidden="true"` | `LastSynced` + 5A |
| Consequence line | `text-sm text-text-secondary dark:text-gray-400 mt-1 max-w-prose`; state 1's "Trying again every 30 seconds." uses the same classes | — |
| Rows | `<dl className="mt-4 space-y-3 sm:space-y-2">`, no dividers; each row `<div className="sm:grid sm:grid-cols-[11rem_1fr] sm:gap-x-4">`; `<dt>` label `text-sm text-text-primary dark:text-gray-200`; `<dd>` age `text-sm text-text-secondary dark:text-gray-400`, stale `text-red-600 dark:text-red-400` | iCloud card text scale |
| Footnote | `text-xs text-text-secondary dark:text-gray-400 mt-3` | — |

- **Do not reuse `EmptyState.jsx`** for state 5 or state 1: it is centered with an icon in a
  circle, which is the blacklist's starter-template look in a status line. **Do not reuse
  `Tooltip.jsx`** for absolute times: it is hover-only and does nothing on a phone.
- **The query, deliberately:** `queryKey: ['healthz']`; `queryFn` is a raw
  `fetch(apiUrl('/healthz'), { cache: 'no-store' })` (`apiUrl` from `lib/apiBase.js`) that throws a
  plain `Error` on a non-2xx, so `queryClient.js`'s `handle401` (axios errors only) can never sign
  anyone out;
  `refetchInterval: 30_000`; `staleTime: 0`, so returning to Settings fetches at once instead of
  showing a cached reading; `retry: false` (the client default — the 2B rule handles failure);
  TanStack's defaults pause polling in a hidden tab and refetch on focus.
- **Recorded exception to `REVIEW_CHECKLIST.md`** (React and TanStack Query: "polling … stops when
  the pending state clears"; "`refetchInterval` is a function that returns `false`"). This is a
  liveness poll with no pending state to clear. It stops on unmount and pauses while the tab is
  hidden, which is the checklist's underlying concern.

*Responsive and accessibility* (Design review 6). Standard Tailwind breakpoints; the Settings
shell is `max-w-5xl` with `px-4 sm:px-6 lg:px-8`, and the card adds `p-6`.

| Viewport | Top alert | Card |
|---|---|---|
| base, under 640 px (bottom bar; sticky 64 px header) | wraps; the link drops to its own line when it must and is 44 px tall | about 295 px of text at 375 px wide, roughly 42 characters of `text-sm`, so each row **stacks**: label, then its age beneath (`space-y-3` between rows); headline and consequence line wrap; the `h2` carries `scroll-mt-20` so the sticky header does not cover it after the jump |
| `sm` 640 px (sidebar rail, header hidden) | one line in most states; link returns to text height | rows become a two-column grid, label in a fixed `11rem` column and the age immediately beside it. Not `ml-auto`: at `lg` that would put the age about 700 px from its label |
| `md` 768 px, `lg` 1024 px, `xl` 1280 px | unchanged | unchanged apart from page padding; the description and consequence lines stop at `max-w-prose` so they do not run the full 912 px |

At 200% zoom or a 320 px viewport everything reflows (wrapping flex, stacked rows) with no
horizontal scroll.

- **Structure.** The card is `<section aria-labelledby="background-jobs">`. Rows are a `<dl>`
  of `<dt>` label and `<dd>` age pairs, so a screen reader reads "iCloud calendar sync, ran 4
  min ago". Each age is `<time dateTime={iso} title={absolute local time}>`; the `title` is a
  desktop convenience for the operator, not the only way to get the information.
- **Never color alone.** The dot is `aria-hidden`; the headline words carry the state, and a
  stale row says "overdue" as well as turning red (WCAG 1.4.1).
- **Announcements.** Neither the headline nor the rows is a live region — ages change on every
  poll and would be read aloud every 30 seconds. The card holds one visually hidden
  `<p role="status">`, present from mount and empty, whose text is set **only when the state
  number from 15a changes** after the first reading: "Background jobs: 1 job is behind
  schedule.", "Background jobs: all running on schedule." Nothing is announced on mount, on the
  loading-to-first-reading transition, or when an age or the footnote changes. A headline change
  that stays inside state 5 ("Waiting for the first runs" → "Waiting on N jobs") is not
  announced. Only the card announces; the top alert never does, so a change is not read twice.
  Adversarial review (scoped, item 15a, 2026-09-21).
- **The top alert is not `role="alert"`.** It is plain text in reading order directly after the
  `h1`, the first thing a screen reader reaches on the page. An assertive alert would interrupt
  every visit for as long as a job is behind.
- **Keyboard and focus.** The link is the only new interactive element (option A has no
  disclosure toggle). It follows
  the `h1` in tab order. Activating it calls `preventDefault()` — a bare hash change would push a
  history entry through the data router — then scrolls the section into view (`behavior:
  'smooth'`, or `'auto'` when `prefers-reduced-motion: reduce` matches) and focuses the `h2` with
  `focus({ preventScroll: true })`, so the next Tab continues from inside the card.
- **Contrast** (measured, WCAG AA 4.5:1 for `text-sm`; Design review 6A: decided — the darker
  gray for the card and the iCloud line). `text-text-secondary` on `card-bg` is 5.8:1 and
  replaces `text-text-muted` (3.0:1) for every piece of secondary text in the card and for the
  iCloud "Last synced" fresh line. `red-600` on `card-bg` is 4.8:1. On the page gradient, the top
  alert uses `red-700` (5.7:1 at the `warm-beige` end, where `red-600` is 4.3:1) and its link
  `terracotta-700` (5.0:1, where `terracotta-600` is 3.6:1). Dark mode: `gray-400` on `gray-800`
  is 5.8:1 and `red-400` is 5.3:1. The dot is decorative, so WCAG 1.4.11 does not apply to it.
- **Motion.** None is added. The only motion is the link's smooth scroll, which respects reduced
  motion.

*Decisions an implementer would otherwise guess* (Design review 7).

| Decision | Resolved as |
|---|---|
| Does Settings show the deployed `version`? | **No** (7A: decided). Smoke check 8 already proves it where the operator works; a commit hash means nothing to the household; and it is not job information, so it would give the section a second job. |
| Does Settings warn when `gate_break_glass` is on? | **No** (7B: decided). The operator sets it deliberately during an incident they are handling; the daily `ops-check` email and the runbook's clearing step reach the one person who can act; a security warning the household cannot act on only erodes trust. |
| Does `/healthz.jobs` list a beat task that has never run? | **Yes, always.** The server returns one entry per `beat_schedule` task, with `last_success_at: null` when no `job_heartbeats` row exists yet — a task that has never run must never be omitted, or the card silently drops a job. An empty list is malformed and renders state 1. Eng review 4: "always" means whenever the store was read. When `read` is `"unavailable"`, `rows` is `[]` (state 1), so the server never invents NULL rows it did not read. A NULL row turns stale 3× its interval after the web process starts (1A). |
| Where does the Settings card get "now"? | **From the server** (Eng review 4): `jobs.now` plus the time since the response arrived. Never `Date.now()` against a server timestamp, so a device with a wrong clock sees the same state as every other. |
| How is the state 3 headline worded when the age is long? | With `durationSince()` ("Nothing has run in 45 min / 3 hr / 2 days"), never `relativeTime` ("…in 3 hr ago"). |
| Which clock does the `<time title>` use? | The viewer's browser time zone, the same clock as the relative ages. The household Timezone setting governs synced calendar events, not this. |
| Is the alert shown anywhere but Settings (for example on Calendar when iCloud sync is behind)? | No, in M8; item 15's scope is Settings. Recorded in NOT in scope. |
| Per-job rows: always visible, only when something is wrong, or behind a toggle? | **Always visible** (8C: decided — mockup option A). |

**Scope correction made while folding:** the app **cannot** show restore-point status. That data
lives in the Fly API, which the backend holds no credentials for and should not. Item 15 covers
background-job staleness only; recoverability monitoring stays with item 13's cron. Writing it
otherwise would have sent the implementer after something impossible.

Two consequences this item carries, both accepted deliberately:

- **`ui_scope` becomes `true`**, so `/plan-design-review` changes from optional to a required
  gate before implementation. The plan's metadata is corrected accordingly.
- It substantially consumes the P2 TODO "Background-job health signal that does not depend on
  iCloud", whose own text says it "needs a plan" because it adds an endpoint and a data store.
  That TODO is resolved by this item rather than left open; the store decision it poses (Redis
  key versus a one-row table) is **decided: a `job_heartbeats` table, one row per task name**
  (Eng review 1A) — see open question 6.

### Between the PRs — execution

Ordered, because two of these are preconditions that live nowhere else and round 3 of spec
review found them asserted in prose but absent from any sequence. Re-sequenced for the PR1a/PR1b
split (Eng review 0):

**Before PR1a merges** (Review-implementation (PR1a, 2026-09-23), user decision: step 0 moved here from "after"). The merge is
what makes Vercel build. Flipped after it, PR1a's own frontend auto-promotes ahead of the
backend (the order this plan exists to fix), and, with the variables not yet exposed, it is
stamped with an empty commit: Vite writes `""` for the set-but-empty `VITE_GIT_COMMIT`, so
check 8 fails the first run on a healthy release. RUNBOOK §0 carries the same rule.

0. **Flip the two Vercel settings**: Settings → Environments → Production → Branch Tracking →
   disable "Auto-assign Custom Production Domains"; and Settings → Environment Variables → enable
   "Automatically expose System Environment Variables" (check 8's build command needs
   `VERCEL_GIT_COMMIT_SHA`). Run `vercel login` first; the local token is expired.

**After PR1a merges (no application code, no migration):**

0a. **Confirm a restore point exists** (item 12). `fly pg backup list -a mealy-app-prod-db`
   and `fly volumes snapshots list` (volume id at runtime, `-a mealy-app-prod-db`). Enable
   continuous backups only if still disabled — this step is a confirm, because enabling was
   supposed to happen the day The Assignment found none. The RUNBOOK pre-release
   `ops-check` gate is `n/a — workflow not yet on default branch`.
1. **`fly scale count web=1 -a mealy-app-prod`** (decided, Eng review 1C). Editing `fly.toml` does
   not do this, and PR1a will have merged `infra/fly-scale.json` — so without this step the very
   first smoke run fails on check 3. Subsequent deploys keep the count; do not re-scale unless
   check 3 fails.
2. Deploy PR1a by following `infra/RUNBOOK.md` itself, passing `--build-arg GIT_COMMIT`. The
   migration-listing row reads "none". Promote the Vercel deployment whose commit SHA equals
   the release commit, after it is Ready — not "the latest staged."
3. Run `infra/release-smoke.py --only=release,liveness,recoverability,edge --skip=healthz_jobs,healthz_version,healthz_gate_break_glass --release-commit=$(git rev-parse HEAD)`.
   Check 1 still runs. The three skipped keys are PR1b-only and print as `skipped`, never
   `pass`. Adversarial review: skip list.
4. **Dry-run the rollback path on this release** (criterion 8): `fly releases --image`, then
   `fly deploy --image <previous ref> --skip-release-command` from the previous commit's checkout,
   Vercel "promote previous" of the matching SHA, smoke `--only=liveness,edge --skip=healthz_jobs,healthz_gate_break_glass`, then roll forward and smoke again.
5. Execute the backup-restore drill, both paths (criterion 7, Eng review 1B). Dated scratch
   names; destroy leftover `mealy-app-drill*` first; never target `mealy-app-prod-db`;
   `job_heartbeats` row count is `n/a` (table not in this schema).
6. Run the drift script (item 9) with the read-only Cloudflare token exported in the shell.

**After PR1b merges (application code, the `job_heartbeats` migration):**

7. Deploy PR1b by following the runbook; the migration-listing row names the revision as additive
   and reversible. Watch the `/healthz` deploy live with the rollback command already pasted
   (item 14 sits on Fly's probe path). Skip list is empty. Mode A break-glass becomes available
   only after this deploy is live.
8. Run `infra/release-smoke.py --release-commit=$(git rev-parse HEAD)` in full; every group
   must pass, including check 8 on both tiers. A missing `jobs` / `version` / `gate_break_glass`
   key is now exit 1.
9. Exercise the password-rotation CLI end to end (criterion 6). Household sessions die; re-login
   with the new password is part of the proof.
10. `gh workflow run ops-check.yml` immediately (do not wait for the daily schedule). Then
    `gh workflow run ops-check.yml -f self_test=true` and confirm the email names the check
    and starts with `exit 1 production:` (criterion 17).
11. Rehearse the break-glass flag in a local `APP_ENV=production` compose run, never in
    production (criterion 15).

**Image-rollback of PR1b** (or any later image that added `/healthz` fields) against a
workflow still on master: `ops-check` liveness fails on the missing `jobs` key. That is a
correct signal. Roll forward, or disable the workflow for the duration. Do not teach the
script to pass on absence.

**Expect the runbook to be wrong in places** — that is the point of executing it, and the
corrections are PR2's most valuable content.

### PR2 — evidence and corrections

- Execution logs filled in: the release runbook's log row, the drill's observed numbers.
- Every correction the execution revealed, folded into the artifacts.
- The abandoned-upload sweep's worker `succeeded` line — the genuinely open M7 item — closed or
  re-recorded. The M7 runbook's unchecked `fly status` box is **struck, not ticked**, citing the
  `git log -S` finding: it postdates that runbook's only execution, and ticking a box inside a
  completed cutover runbook during an unrelated M8 release would be category-confused. Its
  substance lives on as smoke check 2.
- **The v1.1 scope contract**, resolving the parent plan's open item 8. Candidates already named by the epic
  and TODOS: `deploy.yml`, Vercel previews, `visual-tests` required on `master`, Sentry,
  structured JSON logging, app-layer rate limiting on `/auth/login`, password rotation as
  `workflow_dispatch`, the `/auth/login` burst test, and the scheduled drift script. (The
  background-job health signal is no longer a candidate: item 15 ships it — Eng review, Step 0.)
- Epic M8 status, `IMPLEMENTATION_PLAN.md` Phase 2 row, and TODOS.md reconciliation.

## Open Questions

Three of the original six were closed on 2026-09-12 by executing The Assignment; they are kept
below as answers rather than deleted, because the evidence is what item 7 and item 8 now rest on.

**Closed — Why does production run two web machines?** Neither deliberate nor drift: both were
created 15 seconds apart by the original M2 deploy on 2026-04-30, which is `fly deploy`'s default
for an app with an HTTP service. Production has never run `web=1`. See item 7, which now presents
this as a cost-versus-redundancy decision rather than a correctness fix.

**Closed — Can this Vercel project build a production-branch commit without promoting it?** Yes.
Settings → Environments → Production → Branch Tracking → disable "Auto-assign Custom Production
Domains"; builds then sit in state Staged until promoted. No plan-tier restriction documented.
See item 8.

**Closed — Which Fly Postgres product is this, and what is its restore command?** Unmanaged Fly
Postgres Flex (`mealy-app-prod-db`, `flyio/postgres-flex:17.2`), so the restore path is
`fly pg create --snapshot-id <id>` from a volume snapshot. See item 3.

Still open:

1. **Cloudflare API token scope and storage.** The drift script needs a read-only token. Kept as
   a local operator credential in v1 rather than a CI secret; revisit if the script is ever
   scheduled. Confirm the minimum scopes during PR1.
2. **Drill cadence.** The field's floor for a small team is monthly. For a one-household app
   with a single operator that may be more ceremony than it earns; the drill document states a
   cadence and the re-run triggers, and the number is the operator's call at execution time.
   One input is now known: a host migration can reset the snapshot timeline without warning, so
   cadence alone is not sufficient — the re-run triggers matter more than the interval.
3. **What is the real break-glass when Cloudflare is down?** Still the most consequential open
   item. This plan briefly closed it on `cloudflare-state.md`'s "redeploy the previous image"
   answer, which expired the moment the origin-lock code shipped in every image: rolling back to a
   previous image no longer removes the gate, blanking `ORIGIN_VERIFY_SECRET` refuses to boot,
   blanking `PUBLIC_API_HOST` 421s everything, and the only remaining lever is
   `APP_ENV != production`, which also loosens auth config and CORS. **The deployment currently
   has a documented break-glass procedure that does not work.** PR1 lands a real one. It cannot
   be rehearsed in production.

   The Assignment's paper exercise (2026-09-12) established that this is **two** questions, not
   one, and the plan previously conflated them:

   - **Mode A — Cloudflare proxy or WAF degraded, DNS still resolving.** Grey-cloud both records
     and traffic reaches Fly and Vercel directly, but with no Transform Rule there is no
     `X-Origin-Verify` and the gate 421s everything. This one is solvable: a `GATE_BREAK_GLASS`
     flag read by `production_host_gate`, and it belongs in `fly.toml [env]` rather than as a Fly
     secret — for exactly the reason recorded in LESSONS for `STORAGE_BACKEND`, that a secret
     survives a code rollback and would leave the gate silently open forever, whereas a versioned
     flag is cleared by the same rollback that reverts the code.
   - **Mode B — Cloudflare DNS itself is down.** Neither hostname resolves, and grey-clouding
     does not help because that is still Cloudflare's DNS. The only reachable name is
     `mealy-app-prod.fly.dev`, which the Host check rejects; and the SPA would have to run from
     `*.vercel.app`, which is not same-site with the API, so the `SameSite=Strict`
     `__Host-refresh` cookie will not ride and login breaks regardless (the same mechanism as the
     Vercel-preview TODO). Mode B is probably **accepted risk** for v1 rather than solved — but
     it gets written down as accepted, in the diagnostics doc, rather than discovered during an
     outage.

   **Blast radius to enumerate before shipping Mode A's flag:** with the gate bypassed there is
   no WAF rate limit on `/auth/*`, so argon2 on a 1 GB web VM is DoS-able by anyone who finds
   the origin; and `resolve_client_ip` starts trusting attacker-supplied `CF-Connecting-IP`, so
   auth logs become forgeable for the duration. That is the concrete argument for pairing this
   with the P2 app-layer rate-limit TODO, or for documenting the exposure window explicitly and
   keeping the flag's lifetime measured in minutes.

   **Design settled (Eng review 7, folded):** `GATE_BREAK_GLASS = "0"` in `fly.toml [env]`, strict
   parse (only the string `"1"` enables it; `true`, `yes`, and padded values stay off, with a unit
   test per case); when on, `gate_reason` skips only the origin-verify check — the Host check
   stays — and every admitted request logs one `app.gate` WARNING with `outcome="bypassed"`;
   `/healthz` reports `gate_break_glass`. Enable during an incident with
   `fly deploy -a mealy-app-prod --image <current ref> -e GATE_BREAK_GLASS=1` (no build, about a
   minute; `-e` overrides the toml value for that release only), clear ~~with a plain `fly deploy`
   from `master`~~ by redeploying the running image from its release tag's checkout without `-e`
   (Final review, PR1a, 2026-09-23, user decision; see item 2). Detection of a flag left on: the daily cron asserts `/healthz.gate_break_glass`
   is false *and* that the direct-to-origin probe still 421s, and `fly secrets list` must never
   show the name (a secret would survive a rollback and override the toml default). Rehearsed in
   the integration suite and a local `APP_ENV=production` compose run; never in production.
   Adversarial review: the flag code ships in **PR1b**. Between PR1a and PR1b, Mode A is still
   unsolved — do not attempt `-e GATE_BREAK_GLASS=1` against a PR1a image; it is a no-op.
   After the clearing deploy, if `/healthz.gate_break_glass` is still true, `-e` stuck as a
   machine-level env. Do not `fly secrets set` to "fix" it. Diagnostics entry: remove the
   override (`fly machine update` / equivalent) until the flag is false, then record that
   `-e` was sticky so the next incident does not repeat it.

5. **What is the narrowest Fly token that can read machines and volume snapshots?** Item 13
   needs one in GitHub Actions. **Largely answered** (Eng review 8, verified against flyctl
   v0.4.102 on the operator host): `fly tokens create readonly -o <org> -x 8760h` creates "an API
   token limited to reading a single org and its resources" — read-only, org-scoped, one-year
   expiry instead of the 20-year default. What remains for PR1b is confirming it can run
   `fly volumes snapshots list`, `fly pg backup list`, and `fly ips list`; if any is refused,
   item 13 drops to its credential-free subset (origin lock, `/healthz`, cache headers) rather
   than accepting a deploy-capable token on a schedule. Raised in CEO review, candidate 1.
6. **Where does item 15's background-job timestamp live: a Redis key or a one-row table?**
   **Decided: a table, one row per task name** (Eng review 1A, 2026-09-12). `job_heartbeats`
   (`task_name` PK, `last_success_at` **timestamptz** — the new column follows REVIEW_CHECKLIST →
   PostgreSQL rather than the legacy naive columns, and the API serializes it with a `Z` —
   `last_error`, `last_error_at` **timestamptz** (Eng review 4, 2A: "recent" needs a time to
   measure), `error_count`), one additive Alembic revision with a real `downgrade()`, upserted
   with `INSERT ... ON CONFLICT` so redelivery is idempotent. Per-task rows are what let the
   diagnostics doc tell "the upload sweep is stale but the iCloud sync is fresh" (a task-specific
   failure) from "every row is stale" (a dead worker). A Redis key would go blank in exactly the
   outage it is meant to report, because Redis is part of the async stack. The migration also
   gives PR1b a real revision for the runbook's migration-listing step. BACKEND_STRUCTURE's ER
   diagram gains the table.

4. **Why is the Postgres primary stopping and starting hourly, and should it?** Opened
   2026-09-12. The DB machine's event log shows `exit stopped flyd` at `04:00:27Z` followed by
   `start starting proxy` at `04:10:24Z`, and the same pattern an hour earlier — the Fly proxy
   is waking a machine that idles out. On an app whose Celery beat fires every 10 minutes this
   is at best pointless and at worst a source of connection errors that look like something
   else; `pool_pre_ping` (added in #30) is presumably what has been absorbing it. Establish
   whether this is deliberate cost-saving or a default that arrived with the recent host
   migration, and either document it in the diagnostics doc as expected or turn it off.

Resolved earlier in planning and therefore *not* open: whether smoke check 4 needs a
purpose-built no-op task (it does not — `app.tasks.health_check` already exists, is off the beat
schedule, and has a result backend), and which shape the gate's IP-logging fix takes (a separate
emitter, not a wrapper and not a `trust_edge_headers` parameter).

## Success Criteria

1. `infra/RUNBOOK.md`, `infra/incident-diagnostics.md` and
   `infra/backup-restore-drill.md` exist, each covering exactly one runbook type, each carrying
   an owner and a date — "last executed" for the two procedures, "last reviewed" for the
   diagnostics doc, which is a lookup table and is never executed.
2. A real production release has been executed against the release runbook, and its execution
   log row is filled in.
3. `infra/release-smoke.py` exits 0 against production, and `--self-test` has been shown to
   exit non-zero naming the failed check — demonstrated without mutating production.
4. `fly machines list --json` agrees with `infra/fly-scale.json` (`{"web": 1, "worker": 1,
   "beat": 1}`) and TECH_STACK; the `web` count is pinned by the decision recorded in item 7
   (`web=1`, Eng review 1C).
5. The host gate emits one log line per rejection naming the failed check; no secret and no
   attacker-controlled header value appears in it (the IP resolves from `request.client.host`,
   not from `CF-Connecting-IP`); it logs under `app.gate`; the 421 body is unchanged; tests
   assert all six reasons against the branches that produce them and compare the 421 body byte
   for byte across them (Eng review 2.2).
6. The password-rotation CLI has been run end to end over `fly ssh console`: the old password
   fails, the new one works, and an outstanding refresh token and a still-valid access JWT are
   both rejected. Design review 10: the signed-out tab lands on the sign-in page showing the
   updated "Your session ended" banner with its likely causes.
7. A Fly Postgres snapshot has been restored to a scratch instance **and** a point-in-time
   restore from continuous backups has been performed into a second scratch instance (Eng review
   1B), each with snapshot or recovery-point age, restore duration and row counts written into
   the drill's execution log, and both scratch instances destroyed.
8. The rollback path has been dry-run at least once — frontend promote-previous, and
   `fly deploy --image <previous ref> --skip-release-command` on PR1a's no-migration release
   (Eng review 1); the diagnostics doc states why the flag is mandatory across a migration
   boundary.
9. Vercel no longer auto-promotes to production and the runbook's promote step is part of the
   executed release — or, if that setting proves unavailable, the runbook documents both
   fallback paths: deploy-before-merge for no-migration releases, and merge-then-deploy-Fly-
   immediately for migration releases. Backend never trails the frontend by more than one
   `fly deploy` under any path.
10. The deployed frontend build is assertable against the release commit (smoke check 8).
11. The v1.1 scope contract is written, resolving the parent plan's open item 8.
12. Both Cloudflare drift TODOs are resolved: the P2 item by the runbook's dashboard-verification
    steps and the drift script, the P3 item as superseded.
13. `.agents/config.json` maps `backend/app/cli/**`, `backend/app/gate_logging.py`,
    `backend/app/job_health.py` (Eng review 4) and the `infra/` tooling, so `/update-docs` routes
    every new surface; `infra/tests/**` is covered by
    the existing `**/tests/**` exemption.
14. `frontend/vercel.json`'s revalidation headers are confirmed to apply to real SPA routes.
    **Confirmed by observation 2026-09-12 (Eng review, Step 0):** `https://mealy.dev/mealboard`
    returns `cache-control: public, max-age=0, must-revalidate` (Vercel's default for static
    files), so no `vercel.json` change is needed. The chunk-load recovery invariant is still
    verified on a route a user actually lands on during the executed release, not only on
    `/index.html`.
15. **A break-glass procedure exists that actually works** — open question 3 — covering Mode A
    (Cloudflare proxy degraded) with its blast
    radius enumerated and exercised somewhere other than production. Added in round 3 of spec
    review, which correctly noted that reopening the question without giving it a criterion left
    it as a hard PR1 blocker nothing would measure. Mode B (Cloudflare DNS down) is documented
    as accepted risk rather than solved, which also counts — what does not count is leaving it
    unstated.
16. **Production has a restore point, and losing one is detectable.** Continuous backups are
    enabled on `mealy-app-prod-db`, at least one real snapshot or recovery point exists, the
    observed first-snapshot time is written into the drill's execution log, and smoke check 9
    fails when no recent restore point is present. Added 2026-09-12 after The Assignment found
    production with none.

17. **A scheduled check runs between releases** (item 13): `ops-check.yml` is green on a
    `workflow_dispatch` immediately after PR1b merges (not "wait for tomorrow"), has been shown
    to fail via `-f self_test=true` (not a branch edit — schedules only fire from the default
    branch), and every failure message names the check, the exit class (`exit 1 production:` /
    `exit 2 tooling:`), and links its diagnostics entry. `FLY_API_TOKEN` is scoped as narrowly
    as Fly allows. The RUNBOOK header records that this repo is public and that 50 days without
    a commit is a re-arm of the GitHub 60-day scheduled-workflow disable.
18. **`/healthz` reports the deployed commit** (item 14), `RUNBOOK.md`'s deploy step passes the
    build arg, and smoke check 8 asserts on **both** tiers that the deployed code matches the
    release's (`frontend/`, `backend/`; Review-implementation (PR1a, 2026-09-23), was "the ancestor relationship"), treating
    `unknown` as a failure.
19. **`infra/RUNBOOK.md` exists and every release lists its migration with its reversibility**
    (item/candidate 3), so `REVIEW_CHECKLIST.md:128` and `:217` both resolve to a real file.
20. **Background-job staleness is visible in the app** (item 15): the worker writes a completion
    timestamp per task, the API exposes it from memory with per-task staleness, Settings renders
    its five states with the existing freshness-dot pattern, and it is true whether or not iCloud
    is connected. Design review 2: "five states" is now the six card states plus loading in item
    15a, the headline follows the worst job, and the top alert renders for states 2–4 only.
    Eng review 4: `/healthz.jobs` matches the contract pinned in item 15. A job that has never run
    turns overdue 3× its interval after the web process starts rather than waiting forever. The
    Waiting headline is "Waiting for the first runs" only while every row is NULL, then "Waiting
    on N jobs" until the rest succeed. No state threshold or age in the frontend compares a
    server timestamp with the browser clock. The card, the top alert and the smoke script's
    failure line all print the server's `label`. A malformed `jobs` object is "can't check" /
    smoke exit 1, never a green card. Adversarial review (scoped, item 15a, 2026-09-21).
    Eng review 5: state 2 says "Nothing has run" only when every row is stale and at least one
    row has no `write_error`; a reading the web cannot refresh is "Can't check" after 2 minutes
    and never "Stopped".
21. **The scripts are tested and CI runs the tests** (Eng review 3A): `python3 -m pytest
    infra/tests -q` passes locally and in the new `infra-tests` job, and `frontend-tests` builds
    with `VITE_GIT_COMMIT` and finds the SHA in `dist/index.html`. Adversarial review: the suite
    covers `--skip` (skipped ≠ pass; missing key without skip is exit 1) and `--self-test` of
    a selected `--only` group.

**On the epic's Done-when wording.** The epic asks that "smoke and rollback checklists exist";
this plan ships three documents rather than folding everything into one, because mixing runbook
types is the named failure mode and this repo's annotated-runbook precedent is worth keeping.
That satisfies the criterion rather than narrowing it, and PR2 updates the epic text to match
what shipped — the same treatment item 7 gives the `web` count.

## Distribution Plan

Not a downloadable artifact; this is a hosted web service and the existing deployment pipeline
covers it. M8's deliverable *is* the distribution procedure: DNS plus a Vercel production
promotion plus a Fly deployment, executed in a fixed order from a written runbook. v1.1 codifies
that runbook into `.github/workflows/deploy.yml` with a GitHub `production` environment approval
gate; that automation is explicitly deferred by the epic.

## Dependencies

- M7 shipped 2026-09-11 (PR #44). Satisfied.
- A Cloudflare API token with read-only scope, for the drift script (PR1). Confirm the minimum
  scopes during PR1.
- ~~Confirmation that this Vercel project can build without auto-promoting.~~ **Satisfied
  2026-09-12**: the setting exists (Settings → Environments → Production → Branch Tracking →
  "Auto-assign Custom Production Domains"). A `vercel login` is still needed locally.
- **Continuous backups enabled on `mealy-app-prod-db`, and at least one real restore point**
  (item 12). The drill cannot be executed against a cluster with no snapshots, which is the
  state found on 2026-09-12.
- **A `FLY_API_TOKEN` in GitHub Actions secrets** (item 13): a `fly tokens create readonly`
  token with a one-year expiry. New credential surface; see open question 5 (largely answered).
- **A working break-glass for a Cloudflare outage** (open question 3). Today there is none; every
  lever fails closed. Decided: the `GATE_BREAK_GLASS` flag designed in open question 3, enabled
  with `fly deploy --image <current> -e GATE_BREAK_GLASS=1` in about a minute, with its blast
  radius written down and its "left on" state detected daily — it is by definition a switch that
  disables a security control.
- Fly Postgres snapshots enabled and a scratch instance the drill can create and destroy.
- Operator time: the executed release, the drill and the rollback dry-run are human-only and
  cannot be delegated to the implementation skill.
- Related plans: `prod-contract-freeze-plan-20260421-182714.md` (M8's full rationale and its
  four reviews), `infra/r2-cutover-runbook.md` (artifact precedent),
  `.agents/plans/features/mealboard-main-page-updates/ROLLOUT.md` (drill precedent),
  `todo-app-notes/DevOps/Secret Rotation Guide.md` (rotation-order precedent).

## Deferred

- **Cloudflare drift detection in CI on a schedule.** The script ships and runs from the
  runbook; scheduling it needs a CI-held token. Recorded in the v1.1 contract (PR2).
- **Terraform for Cloudflare.** The P2 TODO's option (b). The script path is far smaller and
  reuses the snapshot file's schema as-is; convert later only if the operational benefit
  appears.
- ~~**Background-job health signal that does not depend on iCloud** (TODOS P2).~~ No longer
  deferred: item 15 ships it (CEO review candidate 4), and the TODO is resolved in PR2's TODOS
  reconciliation. Eng review, Step 0: this entry contradicted item 15 and is corrected here.
- **App-layer rate limit on `/auth/login`** and the **`/auth/login` burst test** (TODOS P2 x2).
  Explicitly deferred to v1.1 by the epic; the burst test also hits production endpoints, which
  does not belong inside a launch release. Both stay open.
- **Structured-JSON logging on protected-route 401s** (TODOS P2). The epic defers the broad
  structured-logging story; M8's single host-gate line is operability on an existing security
  gate, not that story. Stays open.
- **`deploy.yml`, Vercel previews, `visual-tests` as required, Sentry, password rotation as
  `workflow_dispatch`.** All epic-level v1.1 deferrals; each is named in the PR2 scope contract
  rather than re-argued here.

## The Assignment

**EXECUTED 2026-09-12.** Kept here with its findings, because three open questions and two plan
items now rest on what it turned up, and because the fifth question is still live.

The assignment was: before any implementation starts, open the Fly dashboard and answer when the
second `web` machine was created and what else happened that day — then four supporting facts,
none of them knowable from the repo.

| Question | Answer |
|---|---|
| When was the second `web` machine created? | `2026-04-30T23:35:13Z`, **15 seconds after** the first. The original M2 deploy created both; `fly deploy` provisions two by default for an app with an HTTP service. Never deliberate, never drift — production has never run `web=1`. |
| Can Vercel build without promoting? | Yes. Settings → Environments → Production → Branch Tracking → disable "Auto-assign Custom Production Domains". Builds sit in state **Staged**; promote from the Deployments list with no rebuild. |
| Which Postgres product, and what is its restore command? | Unmanaged **Fly Postgres Flex** (`mealy-app-prod-db`, `flyio/postgres-flex:17.2`). Restore is `fly pg create --snapshot-id <id>` from a volume snapshot. An image update to 17.7 is available. |
| Are snapshots enabled, and how old is the newest? | Configured (`Scheduled snapshots: true`, retention 5) but **there were none**. The volume was created `2026-09-11T23:30Z` by a host migration and the snapshot timeline restarted. `fly pg backup` was also disabled. |
| What is the smallest change that gets traffic to the origin with Cloudflare down? | Still open — and it turned out to be two questions, not one. See open question 3 (Mode A / Mode B). |

**What the assignment found that it was not looking for**, and the reason it was worth doing as
a discrete step rather than folding into implementation: production was running with **no restore
point at all**, and no check anywhere would have said so. Data was intact — the public
`/auth/status` endpoint returned `{"account_exists":true}` — but recoverability had silently
gone to zero when the database changed hosts. The plan's thesis is the silent failure nobody is
looking for; the assignment found a live one before PR1 wrote a line. Item 12 and smoke check 9
exist because of it, and the one action that should not wait for PR1 is
`fly pg backup enable -a mealy-app-prod-db`.

**Still outstanding for the operator:** flip the Vercel setting (after `vercel login` — the local
token is expired), enable continuous backups, decide the `web` count now that item 7 presents it
as a cost-versus-redundancy call, work out Mode A's break-glass on paper, and establish whether
the Postgres primary's hourly stop/start cycle is deliberate (open question 4).

## What I noticed about how you think

- You picked *"Find out why it's 2 first, then decide"* on the web-scale question, over a
  recommendation that came with an argument you had every reason to accept. The epic criterion
  you wrote yourself says M8 must land on a number, so choosing to investigate costs you a step
  and gains you nothing procedurally — you did it because deciding from a guess is not deciding.
  That is the same instinct as the criterion's own wording: "reconciled, not merely checked."
- The M8 done-when you wrote on 2026-09-11 did not say "check the scale counts." It said counts
  must be *reconciled, not merely checked*, and then spelled out why `min_machines_running = 1`
  is "a floor, not a cap." You had already worked out that the failure mode was a check that
  passes while the thing it guards drifts — which is the same failure the unchecked `fly status`
  box represents, one document over.
- Your epic's criterion for the gate log line ends with a constraint most people would omit:
  *"with the 421 response body unchanged so the two failure modes stay indistinguishable to a
  caller."* You asked for operator visibility and, in the same sentence, refused to buy it with
  attacker visibility. That is the whole security-usability tradeoff resolved in one clause.
- Six premises, including three that name things that are currently broken on your own
  deployment, and you agreed to all six without softening any of them.

## NOT in scope
<!-- CEO review: considered and deferred -->
- Visual-regression baseline for the new Settings indicator — unit tests cover the four states; extending the VRT suite to Settings stays with its existing TODO. TODOS.md: no, already tracked.
- In-app restore-point status — the app holds no Fly credentials and should not; item 13's cron owns recoverability monitoring. TODOS.md: no, structurally impossible rather than deferred.
- Metrics, dashboards, alerting beyond the cron's own failure email — epic-defers Sentry and structured logging to v1.1. TODOS.md: already tracked.
- Three-week log-based debuggability — Fly retention is far shorter and the epic defers the logging story. Accepted and stated in the diagnostics doc rather than silently assumed (CEO review 8B). TODOS.md: already tracked.
- Scale reconciliation and commit-match on the scheduled cron — both are release-relative and would produce noise on a schedule. TODOS.md: no.
<!-- Eng review: considered and deferred -->
- Moving the gate out of `main.py` into its own module — structural and behavioral changes stay in separate steps (Beck), and PR1b is the behavioral one. TODOS.md: no, not valuable enough on its own.
- Rate-limiting the gate's rejection log line — a scanner hitting the Fly IP can generate log volume, but the auth logger already carries the same exposure and Fly retention bounds it. TODOS.md: no; accepted and stated in the diagnostics doc.
- Rehearsing the cross-migration rollback (`--skip-release-command`) against production — the mechanism is a flyctl flag and the Alembic failure it avoids is deterministic; the diagnostics doc documents it and the dry-run runs on PR1a. TODOS.md: no.
- Aligning compose and CI Postgres (16) with production (Postgres Flex 17.2) — real drift, outside M8; TECH_STACK states the truth in PR1a. TODOS.md: yes, added 2026-09-12.
- Having `ICloudSettings.jsx` consume the server-derived per-task staleness instead of its own 30-minute constant — DRY follow-up once item 15 exists. TODOS.md: yes, added 2026-09-12.
- Scheduling the drift script in CI — needs a CI-held Cloudflare token; already in Deferred and the v1.1 contract. TODOS.md: no.
<!-- Design review: considered and deferred -->
- Showing "iCloud sync is behind" on the Calendar page, where the symptom is noticed — item 15's scope is Settings (CEO review), and the daily `ops-check` email is the operator's detector either way. TODOS.md: yes, added 2026-09-14.
- App-wide small-text contrast (`text-text-muted`, 3.0:1; `terracotta-600` text, 3.6–4.0:1) — M8 fixes only the new card and the iCloud "Last synced" line (6A); the rest is pre-existing debt across ~50 files. TODOS.md: yes, added 2026-09-14.
- A sign-in page message that says specifically "the password was changed" — not derivable without a `users.password_changed_at` column published on the public `/auth/status`, because Sign out and rotation both revoke every session and refresh failures share one body by design. The honest generic version ships in PR1b instead (item 6, Design review 10). TODOS.md: no, superseded.
- Restructuring Settings' stack of section cards into a layout (design rules, hard rejection 7) — pre-existing; the new card follows the page's shell. TODOS.md: no.
- `status-ok` / `status-bad` tokens in `index.css` (5A, option C) — edits a design-watched file and leaves 237 stock-red uses untokenized; M8 documents stock red as the error color instead. TODOS.md: no.
- Deployed version or break-glass state on Settings (7A, 7B) — considered and rejected, not deferred: smoke check 8 and the daily email already reach the one person who can act. TODOS.md: no.
- A pulsing "live" dot, per-job pills, tiles, icons, or an uptime bar on the card (pass 4) — rejected as the generic status-page look. TODOS.md: no.
<!-- Eng review 4 (scoped re-review of item 15a, 2026-09-21): considered and deferred or rejected -->
- Recording heartbeat-write errors in Redis so "can't record" survives a database outage (2C) — rejected, not deferred: a second store on the health data path, for a distinction the worker's `Task … succeeded` log lines already give. TODOS.md: no.
- Removing state 2 and the error columns (2B) — rejected: it reverses CEO 2B-A and a design state; the fixed definition plus the stated limit is enough. TODOS.md: no.
- A server-side `worker` / summary field on `/healthz.jobs` — rejected: "every row stale" is one comparison each reader makes from `rows[].stale`; a summary would be a second derivation to keep in step. TODOS.md: no.
- A cross-suite contract test between backend pytest and `infra/tests` — the api container does not mount `infra/`, and a CI-only path breaks REVIEW_CHECKLIST → GitHub Actions ("no CI-only code path"); the first PR1b smoke run is the integration point and fails loudly. TODOS.md: no.
<!-- Eng review 5 (scoped re-review of the adversarial pass's changes to item 15a, 2026-09-21): considered and rejected -->
- Keeping "every row stale → Nothing has run" and only naming the limit (1B) — rejected: the line would be false for as long as a broken recorder lasts, which is the case state 2 exists for. TODOS.md: no.
- One neutral state 2 line with no branch (1C) — rejected: simplest code, but it hands design a copy rewrite and drops a distinction the rows already support; 1A keeps both approved strings. TODOS.md: no.
- A smoke message that reports an unusable reading *and* its stale rows — rejected: one rule, one message, the same order Settings uses; the rows of a reading just called unusable are not evidence. TODOS.md: no.
- Persisting `started_at` (a column or a Redis key) so a web restart stops rearming the never-run grace — out of this pass's scope; the adversarial review named the limit and it only bites before a job's first-ever success. TODOS.md: no.
<!-- Design re-review (scoped re-review of item 15a's user-visible outcomes, 2026-09-21): considered and rejected -->
- Making state 2's "Nothing has run" case read as state 3, so the headline and alert say "stopped" when the line under them does — rejected: it reverses the adversarial ladder (state 2 before state 3) and changes the state number, so the status region would announce it. With a dead worker the mismatch is bounded: that line needs a flagged row, a flag lives at most 3× its job's interval, and then state 3 takes over. The household's next step is the same sentence in both. TODOS.md: no.
- A third state 2 line for two faults at once ("Some jobs have stopped, and the app can't record the others.") — rejected: it would understate a dead worker for up to 3 hours, the reassuring direction CEO 2B-A forbids, to be exact about a double fault. The limit is stated under item 15a's "What this means" table instead. TODOS.md: no.
- An alert or a hand-off line for a "Can't check" that lasts for hours — rejected: see the top-alert bullet in item 15a. The card cannot time its own blindness across reloads, and the cron already tells the operator. TODOS.md: no.
- Announcing the change of state 2's line, or of the state 5 headline — rejected: the status region speaks only when the state number changes, and the next change that matters (to state 3, or to state 6) is announced. TODOS.md: no.
- Rewording "can't record that either" or "Waiting on N jobs" — not reopened: both strings were decided on 2026-09-21 and read correctly beside their rows. TODOS.md: no.
- Removing the mockup's leftover `ui.expanded` and `jobs-toggle` wiring from options B and C — mockup chrome with no effect on what renders; left alone. TODOS.md: no.

## What already exists
| Sub-problem | Existing code or flow | Plan reuses it? |
|---|---|---|
| Prove the worker ran | `app.tasks.health_check` (`backend/app/tasks.py`), off the beat schedule, result backend configured | yes — smoke check 4 |
| Revoke sessions on rotation | `auth/service.py:378 logout()` — bumps `session_version`, revokes `revoked_at IS NULL` rows, one transaction | yes — item 6 calls it |
| Structured auth log shape | `auth/logging_utils.py:emit_log_line` | shape only — a wrapper cannot change its logger binding or its `resolve_client_ip` call (Section 2) |
| Timezone-safe server timestamps | `ICloudSettings.jsx:545 parseServerTime` (module-private) | yes — extracted to a shared module (4B-A) |
| Freshness-dot presentation | `ICloudSettings.jsx` (#49) | yes — extracted and shared with the jobs indicator |
| Public operational probe | `/healthz` (`main.py:280`) | yes — carries version (item 14) and job health (3A-A) |
| Backup drill mechanics | `ROLLOUT.md` pg_dump → restore-probe → count-verify | yes — item 3 builds on it |
| Cloudflare intent file | `infra/cloudflare-state.md` | yes — drift script's diff target |
| Shell-script precedent | `todo-app-notes/DevOps/slice6-rate-limit-burst-test.sh` | yes |
| Scheduled CI | `.github/workflows/test.yml`, `doc-guard.yml` | yes — item 13 adds `ops-check.yml` |
| Password policy | `auth/schemas.py:PASSWORD_MAX_LENGTH` | yes — item 6 imports it (2D) |
| Request-id sanitization | `auth/logging_utils.py:54 resolve_request_id` — allowlist-validated | yes — the gate emitter imports it (Eng review 2.2) |
| Sync-to-async bridge | `tasks.py:10 run_async` — fresh loop, engine disposed | yes — heartbeat writer and CLI (Eng review 2.1, 3) |
| Auth test fixtures | `tests/integration/auth/conftest.py` — `register_user`, fast argon2 hasher | yes — rotation and gate tests |
| Log-record scanner | `tests/integration/auth/test_log_hygiene.py:_record_strings` | yes — "no secret in the gate line" assertion |
| Vercel SPA cache headers | Vercel default `max-age=0, must-revalidate` on every static path (observed on `/mealboard`, 2026-09-12) | yes — item 11 needs no change |
| Read-only Fly token type | `fly tokens create readonly` (flyctl v0.4.102) | yes — item 13's `FLY_API_TOKEN` |
| Point-in-time restore | `fly pg backup restore` (WAL-based, new cluster) | yes — drill path 2 (Eng review 1B) |
| Vite HTML env substitution | `%VITE_*%` in `index.html`; unresolved names stay literal | yes — check 8's meta tag (Eng review 1.5) |
| Settings section shell | `FamilyMembersPage.jsx` card (`bg-card-bg … rounded-xl border p-6 mt-6`) + `h2 text-lg font-semibold mb-4`; Timezone card's description line | yes — Background jobs card (Design review 5) |
| Status line with words beside the dot | `ICloudSettings.jsx` `LastSynced` — red plus "sync overdue", dot `aria-hidden`, color never the only signal | yes — `FreshnessDot` extracted; the headline and the top alert follow it (Design review 4, 5) |
| Relative time | `ICloudSettings.jsx` `relativeTime` | yes — moved to `lib/serverTime.js`, plus a `durationSince` sibling (Design review 7) |
| No-flash loading | `hooks/useDelayedFlag.js` | yes — "Checking…" appears only after 200 ms (Design review 2) |
| Query client with axios-only 401 handling | `lib/queryClient.js` (`handle401` ignores non-axios errors; `retry: false`) | yes — first non-auth `useQuery`; a raw `fetch` can never sign anyone out (Design review 5) |
| Session-ended explanation | `AuthPortalPage.jsx` `COPY.bounce` banner on `return_to` | yes — copy extended with the likely causes (Design review 10) |
| Status and success colors | `sage-500` token (documented); stock `red-500/600/700` (237 uses, undocumented) | yes — 5A; both documented in PR1b |
| Rejected for this surface | `components/shared/EmptyState.jsx` (centered, icon in a circle), `components/shared/Tooltip.jsx` (hover-only) | no — wrong vocabulary for a status line; `<time title>` instead of a tooltip (Design review 5) |
| Unauthenticated URL builder | `frontend/src/lib/apiBase.js:66 apiUrl` — no axios import | yes — the `/healthz` fetch (Eng review 4; the plan had said only "not `api.js`") |
| Offset-aware timestamp parsing | `ICloudSettings.jsx:545 parseServerTime` — appends `Z` only when no offset is present | yes — the `timestamptz` job fields (`Z`) and the naive iCloud field (no `Z`) both go through it unchanged |
| The job list and cadences | `backend/app/celery_app.py beat_schedule` — four entries, numeric `schedule` seconds | yes — `job_health.beat_intervals()` derives both the writer's filter and the payload's rows from it (Eng review 4) |
| Test setup for page queries | `frontend/tests/setup.js` MSW with `onUnhandledRequest: 'warn'`; `RootLayout.jsx` `QueryClientProvider` | yes — a default `/healthz` handler and a fresh `QueryClient` in Settings tests (Eng review 4) |

## Dream state delta
```
  CURRENT STATE              THIS PLAN                     12-MONTH IDEAL
  Releases improvised   ---> Written, executed runbook; --> Releases are boring and
  from memory; failures      mechanical checks at            automated; the system tells
  silent until someone       release AND on a schedule;      YOU when it decays, before
  happens to look            drill run; break-glass real     anyone looks
```
After this plan the system watches itself daily on the three properties that have actually
failed here (worker liveness, recoverability, origin lock) and can be released from a document
rather than from memory. The next step toward the ideal is v1.1's approval-gated `deploy.yml`
plus real alerting — not more checks, but fewer manual steps between a green check and a
release.

## Exception and rescue registry
| Codepath | What can go wrong | Error type | Caught? | Rescue action | User sees | Tested? |
|---|---|---|---|---|---|---|
| `gate_logging.emit_gate_rejection` | header non-ASCII / oversized; handler raises | `UnicodeError`, `TypeError`, `OSError` | Y (2A) | swallow, emit nothing | 421, body unchanged | Y — new |
| `cli.rotate_password` | email not found | none | Y | exit 1, no write | "no user with that email" | Y |
| `cli.rotate_password` | password > 128 | argon2 / silent bad hash | Y (2D) | reject before hashing | "password too long" | Y — new |
| `cli.rotate_password` | DB unreachable | asyncpg / pool timeout | Y | exit 2, no partial write | connection error | Y |
| `task_postrun` writer | store unreachable | `ConnectionError`, pool timeout | Y (2B-A) | swallow, WARNING, counter++ | task still succeeds | Y — new |
| `/healthz` job fields | store unreachable / cache cold | `ConnectionError` | Y (3A-A) | serve last-known + flag | 200, `stale` flag | Y — new |
| `/healthz` version | `GIT_COMMIT` unset | none | Y | `"unknown"` | smoke check fails loudly | Y — new |
| `release-smoke.py` | tooling vs check failure | n/a | Y (2E) | exit 1 vs exit 2 | named cause | Y — `--self-test` + `infra/tests` (3A) |
| `ops-check.yml` | runner network blip | n/a | Y (2F) | retry once | no alert on transient | n/a |
| `cloudflare-drift.py` | API error / token expired | HTTP error | Y | exit 2, never echo secret | named cause | Y (3G) |
| Settings `/healthz` fetch | 421 / CORS / missing `jobs` | HTTP / TypeError | Y | "can't check"; no 401 interceptor | still signed in | Y — new |
| `task_postrun` writer | one-shot task SUCCESS | none | Y | drop; no row | n/a | Y — new |
| `rotate_password` | crash between hash write and `logout` | process kill | Y | impossible: one commit | n/a | Y — new |

## Failure modes
| Codepath | Failure mode | Caught? | Test? | User sees? | Logged? |
|---|---|---|---|---|---|
| gate logging | exception turns 421 into 500 | Y | Y | 421 unchanged | Y |
| `/healthz` | slow/erroring read fails Fly health check → machines killed | Y | Y | nothing (cache) | Y |
| job signal | worker dead | Y | Y | red dot + cron alert | Y |
| job signal | writer broken, worker fine | Y | Y | distinct message | Y (WARNING) |
| job signal | NULL on fresh deploy / post-restore | Y | Y | "Waiting" only while this web process is inside the grace; a restore that does not restart web is overdue (Adversarial review, scoped, item 15a) | n/a |
| job signal | threshold stale after schedule change | Y (4C) | Y | derived, cannot decay | n/a |
| `ops-check.yml` | stops running entirely | Y (8A) | manual | caught at next release | n/a |
| smoke check 8 | rolling deploy mid-flight | Y (9B) | n/a | run after deploy completes | n/a |
| rotation CLI | sets an unusable password | Y | Y | rejected before write | n/a |
| break-glass flag | left on after an incident | Y (cron) | Y | failing cron email | n/a — superseded by Eng review table |

**The CEO-review remaining CRITICAL GAP is closed.** Eng review shipped `/healthz.gate_break_glass`
plus the cron; the row above is kept so a reader of this table does not re-open a fixed gap.
Adversarial review: do not implement from this table's older `N` cells.

### Eng review failure modes, one row per new codepath
<!-- Eng review: Test? = N, Handled? = N, silent together are a CRITICAL GAP -->
| Codepath | Failure mode | Test? | Handled? | User sees | Critical gap? |
|---|---|---|---|---|---|
| `gate_reason()` + emitter | emitter raises on a hostile header → 421 becomes 500 | Y | Y (wrapped) | 421 unchanged | no |
| `gate_reason()` | `GATE_BREAK_GLASS` parsed loosely (`"true"`) → lock silently open | Y | Y (strict parse) | nothing; cron catches within a day | no |
| break-glass flag | left on after an incident | Y (cron: `/healthz` flag + origin probe) | Y | failing cron email | no — was the CEO review's remaining gap |
| `/healthz` refresher | store read hangs → probe timeout → machine killed | Y (stubbed hang) | Y (background task + `wait_for`) | nothing; `read: stale` | no — was CRITICAL (Eng review 1.2) |
| `/healthz` refresher | database down at boot | Y | Y | `jobs.read: unavailable` | no |
| `/healthz` version | `GIT_COMMIT` unset (ARG in the wrong stage) | Y | Y | check 8 fails loudly | no |
| heartbeat writer | store error | Y | Y (swallow, WARNING, counter) | task unaffected | no |
| heartbeat writer | FAILURE state written as a success | Y | Y (SUCCESS filter) | n/a | no |
| staleness derivation | schedule changed, threshold left stale | Y | Y (derived per task) | n/a | no |
| `service.rotate_password` | >128 chars / confirmation mismatch / not found | Y | Y | exit 1, nothing written | no |
| `service.rotate_password` | database drops mid-call | Y | Y (one commit inside `logout`) | exit 2; both writes or neither | no |
| `release-smoke.py` | flyctl output shape changes | Y (`infra/tests` fixtures) | Y (exit 2 on parse failure) | named cause | no |
| `release-smoke.py` check 5 | 429 from the WAF | Y | Y | exit 1 "unverified" | no |
| `release-smoke.py` check 8 | placeholder / empty / `unknown` / tier code differs from the release (Review-implementation (PR1a, 2026-09-23)) | Y | Y | exit 1 naming the tier | no |
| `cloudflare-drift.py` | API response carries the header value | Y (3G fixture) | Y (redaction) | value never printed | no |
| `ops-check.yml` | stops running (disabled, inactive repo) | manual (RUNBOOK pre-gate) | Y | caught at the next release | no |
| `ops-check.yml` | Bot Fight Mode challenge instead of JSON | Y (fixture) | Y (exit 2) | no false alarm | no |
| rollback across a migration | old image's `alembic upgrade head` cannot locate the revision | n/a — deterministic, documented | Y (`--skip-release-command`) | deploy proceeds | no — was CRITICAL (Eng review 1) |
| Settings indicator | `/healthz` unreachable | Y | Y | "can't check" | no |
| drill | scratch cluster left running | manual | Y (last step + `fly apps list`) | cost only | no |
| `task_postrun` writer | one-shot task (`health_check`, `extract_recipe_from_url`, …) inserts a row → "every row stale" lies | Y (filter to `beat_schedule` task names; reader ignores unknown) | Y | n/a | no — Adversarial review |
| heartbeat `error_count` | one blip permanently paints "can't record job runs" | Y (success zeros count + clears `last_error`) | Y | recovers | no — Adversarial review |
| Settings `/healthz` fetch | uses `api.js` → 421/CORS trips 401 interceptor → household signed out | Y (unauthenticated `fetch`, missing keys → "can't check") | Y | "can't check", still signed in | no — Adversarial review |
| PR1a smoke | `--only` drops check 1 and keeps `gate_break_glass` | Y (`--skip=` on three PR1b keys; `liveness` still runs) | Y | skipped, not pass | no — Adversarial review |
| `ops-check.yml` | public repo, 60 days no commit → GitHub disables the schedule | manual (RUNBOOK 50-day re-arm + pre-gate) | Y (documented limitation) | caught at next release; blind between | no — accepted, named |
| `ops-check.yml` | criterion 17 "edit a branch" never fires the schedule | Y (`workflow_dispatch` + `self_test`) | Y | email on dispatch | no — Adversarial review |
| drill after PR1a | count `job_heartbeats` on a schema that lacks the table | Y (n/a when absent) | Y | log `n/a` | no — Adversarial review |
| Vercel promote | "latest staged" is an older leftover or not Ready | Y (SHA match + Ready) | Y | wrong bundle, check 8 fails | no — Adversarial review |
| `rotate_password` | two commits (hash, then `logout`) | Y (same session, `logout` is the only commit) | Y | neither or both | no — Adversarial review |
| check 4 | 30s `get()` times out behind a non-empty queue | Y (exit 1 unverified, inspect queue; retry once on OQ4) | Y | named cause | no — Adversarial review |
| `/healthz.jobs` NULL row | worker dead at the PR1b deploy → every row NULL → "Waiting for the first runs" forever, cron reads "no data yet" forever | Y (grace tests; critical path 15) | Y (NULL stale 3× interval after `started_at`) | red "hasn't run yet · overdue", then "Nothing has run yet" + top alert; cron fails | no — **was CRITICAL** (Eng review 4, 1A) |
| `/healthz.jobs.write_error` | "recent" measured on a column that did not exist; 30 s window flashes state 2 | Y (window, aged-out, older-than-success, and a NULL `last_success_at` with `last_error_at` inside the window still flags — a recorded success is not required) | Y (`last_error_at`, 3× interval window) | "Can't record job runs" while it is true | no — Eng review 4, 2A. Adversarial re-review (2026-09-21): the NULL-success case is the fresh-deploy shape of "recorder broken, worker fine"; without it the flag never lights and state 2's line cannot protect that picture |
| `/healthz.jobs.write_error` | database unreachable / table missing / handler bug → error cannot be recorded either | Y (both writes raise → WARNING only) | Y (documented limit; worker `succeeded` lines are the tiebreaker) | "Stopped" once a snapshot ages out with no `write_error`; "Can't check" if the web has never read the table (Adversarial review, scoped, item 15a). Eng review 5: read "snapshot ages out" as *rows pass 3× while the web still reads the table*. If the web cannot read it, "Can't check" after 2 min, whether or not it ever had a snapshot | no — accepted, named (2A) |
| Settings 2-minute cap | browser clock skewed ±minutes → permanent "can't check" or a frozen reading never capped | Y (skewed-clock test) | Y (server-aligned now from `jobs.now`) | same state on every device | no — Eng review 4 |
| `/healthz.jobs` shape | PR1a smoke parser and PR1b producer written against different prose | Y (fixtures from the pinned example; response model) | Y (one contract in item 15) | exit 1 on the first PR1b smoke run, never silent | no — Eng review 4 |
| `/healthz.jobs` label | two hand-kept name maps (JS, Python) drift | Y (readers hold no map) | Y (server sends `label`) | one name everywhere | no — Eng review 4, 3A |
| `job_health.beat_intervals()` | a future `crontab()` beat entry has no interval → task silently dropped | Y (structural test over the real schedule) | Y (raises) | a failing test, never a missing row | no — Eng review 4 |
| `/healthz.jobs` read unavailable | server lists NULL rows it never read → "Waiting" instead of "can't check" | Y | Y (`rows: []` iff unavailable) | state 1 | no — Eng review 4 |
| `/healthz.jobs` second derivation | item 13 re-derives "minimum beat interval" or "one schedule interval" while the card uses per-task `stale` | Y (smoke fixture consumes `stale` only) | Y (item 13 points at the contract; the one-interval sentence is gone) | cron and card disagree, or the hourly sweep false-fails | no — Adversarial review (scoped, item 15a, 2026-09-21) |
| state 5 headline | two syncs have run, sweeps still NULL inside grace → "Waiting for the first runs" for up to 3 h | Y | Y (headline "Waiting on N jobs"; no top alert) | rows and headline agree | no — Adversarial review (scoped, item 15a, 2026-09-21) |
| state 2 copy | every row stale and one `write_error` → "They may still be running" | Y | Y ("Nothing has run" only when every row is stale and at least one row has no `write_error`; this row is that case. Not "only when some row is fresh" — that parenthetical sends the next row to "Nothing has run" while every job is running) | "Nothing has run, and the app can't record that either." Headline stays "Can't record" | no — Adversarial review (scoped, item 15a, 2026-09-21); parenthetical corrected by the re-review of Eng review 5 (1A), 2026-09-21 |
| state 2 copy | recorder broken, worker fine: after 3 h every row is stale and every row has `write_error` → "Nothing has run" for as long as the fault lasts, while every job runs | Y (three fixtures: some row not stale; all stale, all `write_error`; all stale, one without) | Y ("Nothing has run" needs a stale row with no `write_error`) | "They may still be running, but the app can't confirm it." | no — Eng review 5 (1A) |
| state 1 vs state 3 | database down for web and worker, snapshot in memory → a test or an operator expects "Stopped" | Y (every row stale and `read_at` over 120 s old → state 1; smoke says "web cannot read", not a label list) | Y (first match; unusable outranks stale) | "Can't check right now" | no — Eng review 5 |
| NULL rows after a restore | refresher keeps the previous reading's timestamps when the table comes back empty → rows look fresh or merely late | Y (timestamped snapshot, then all-NULL snapshot, old `started_at` → every row stale on the next request) | Y (snapshot replaced wholesale) | "hasn't run yet · overdue" until each job runs | no — Eng review 5 |
| test artifact | leftover "the cron tolerates one interval of NULL after any deploy or restore" → a second clock gets implemented in the smoke script | Y (smoke fixture: a NULL row passes only when the server sent `stale: false`) | Y (sentence replaced) | cron and card agree | no — Eng review 5 |
| `/healthz.jobs` shape | missing `stale`, empty `rows` with `read: ok`, or `now` that does not parse → green card | Y | Y (unusable → state 1 / smoke "malformed jobs") | "Can't check right now" | no — Adversarial review (scoped, item 15a, 2026-09-21) |
| Settings 2-minute cap | derivation inside TanStack `select` → failed polls never age the reading into state 1 | Y | Y (derive on each render, not in `select`) | state 1 within one poll of 2 min | no — Adversarial review (scoped, item 15a, 2026-09-21) |
| NULL grace clock | operator restarts web while the worker is still dead → overdue NULL rows read as Waiting again and the cron goes quiet | Y (new `started_at` clears NULL staleness) | Y (named limit; diagnostics say look at `worker` and `beat` before restarting web) | Waiting again for up to 3× | no — accepted, named (Adversarial review, scoped, item 15a, 2026-09-21) |

Critical gaps remaining after this review: **0**. Three were found by eng review (two new, one
inherited from the CEO review) and closed; adversarial review found no new critical gap that
stays open — the GitHub 60-day disable is a documented limitation of the chosen watchdog, not
an unhandled silent failure. Eng review 4 (2026-09-21, scoped to item 15a) found one more: a
never-run job reading "waiting" forever. It is closed by the post-start grace (1A). This
scoped pass found the grace and the card still lied in three places (a second cron formula, a
Waiting headline after some jobs had run, and "may still be running" after every job had
stopped). Those are folded in. A web restart still rearms the never-run grace; that limit is
named, not left silent. Still 0 remain open. Eng review 5 (2026-09-21, scoped to what that pass
changed) found no critical gap: every miss it found erred toward alarm, and none was silent.
It narrowed the state 2 line to evidence (1A), moved the "Stopped" / "Can't check" line to
whether the web can read now, and removed two leftover sentences. 0 remain open.
Adversarial re-review (scoped to Eng review 5, 1A, 2026-09-21): the predicate holds, and the
two pins hold (smoke checks an unusable reading before it lists stale rows; "Stopped" vs
"Can't check" turns on whether the web can read the table now). Two leftovers would have undone
the predicate: a failure-mode parenthetical that restricted the reassuring line to "some row is
fresh", and a diagram formula that dropped a NULL `last_success_at` and called all-flags
"worker fine". Both are folded in. All-flags after the worker dies is the same rows for at most
3× the shortest flag; that window is named, not a new string. 0 remain open.

## Diagrams
### Deployment sequence and rollback
```
  PRE:  CI green ── doc-guard ── migration listed w/ reversibility ── ops-check recent
         │
  1.  fly deploy ──▶ release_command: alembic upgrade head
         │              └── fails ──▶ previous image keeps serving ──▶ STOP, forward-fix
  2.  fly status: every process group started, rollout complete
  3.  Vercel: promote staged build   (never before step 2)
  4.  infra/release-smoke.py  (all groups)
         │
         ├── exit 0 ──▶ tag release ──▶ log execution row ──▶ DONE
         ├── exit 2 ──▶ tooling broken, not production ──▶ fix laptop, re-run
         └── exit 1 ──▶ ROLLBACK
                          │
                          1. Vercel: promote previous  ◄── frontend FIRST
                          2. still broken? fly deploy --image <prev> --skip-release-command  (code only, NOT schema)
                          3. migration already applied? forward-fix is the default;
                             alembic downgrade only if RUNBOOK listed it reversible;
                             restore from backup only if both are unsafe
```

### Job heartbeat data flow (item 15 — Eng review 1A, 1.2, 3; updated Eng review 4, Eng review 5)
```
  worker (Celery)                          web (uvicorn)                             readers
  ┌──────────────────────────┐             ┌──────────────────────────────────┐
  │ any task ends            │             │ lifespan: started_at = now (mem) │
  │   task_postrun signal    │             │   refresher task, every 30 s:    │
  │   state == SUCCESS? ─────┼─no─▶ drop   │   wait_for(read rows, 1.0 s)     │
  │   name in beat_intervals()┼─no─▶ drop  │     ok ─▶ snapshot = rows,       │
  │        │ yes             │             │           read_at = now, read=ok │
  │   run_async(upsert)      │             │     timeout / error ─▶ keep      │
  │   ON CONFLICT (name)     │             │       snapshot; read=stale, or   │
  │   ok → count=0, clear    │             │       unavailable if never read  │
  │     last_error(_at)      │             │                                  │
  │   raises? WARNING, then  │             │ GET /healthz: memory only —      │
  │     2nd write: last_error│             │   build_jobs_payload(snapshot,   │──▶ /healthz.jobs ──▶ Settings card + alert
  │     last_error_at, count+1│            │     now, started_at)             │     (contract,       (server-aligned now)
  │     raises? WARNING only │             │   never awaits the store         │      item 15)  └──▶ smoke / ops-check.yml
  └────────────┬─────────────┘             └──────────────────────────────────┘                    (prints `label`)
               ▼
        job_heartbeats  (beat tasks only; keyed by Celery task name)
        task_name PK · last_success_at tz · last_error · last_error_at tz · error_count
  per request, for each beat_schedule task (in schedule order; rows=[] iff read=unavailable):
        stale        = last_success_at ? now − last_success_at > 3 × interval
                                       : now − started_at     > 3 × interval    ◀ NULL expires (1A)
        write_error  = last_error_at is set
                       and (last_success_at is NULL or last_error_at is newer)
                       and now − last_error_at ≤ 3 × interval                  ◀ (2A)
        label        = JOB_LABELS[task] or humanized(task)                     ◀ (3A)
        stopped      = every row stale (each reader derives it; no summary field)
        unusable     = unavailable, null read_at, unparseable now, bad shape,
                       empty rows while read is ok/stale, or now − read_at > 120 s
                       → Settings state 1 / smoke exit 1 ("malformed jobs" if the shape is wrong)
        waiting copy = every row NULL → "Waiting for the first runs"
                       else some NULL and none stale → "Waiting on N jobs"
        state 2 copy = every row stale AND some row has no write_error
                         → "nothing has run, and the app can't record it"
                       else → "may still be running"                        ◀ (Eng review 5, 1A)
                         (a row with write_error ran inside 3 × interval, even if
                          last_success_at is still NULL. All flagged does not prove
                          the worker is still up: same rows for ≤3× the shortest
                          flag after it dies. The copy stays "may".
                          Adversarial re-review, 2026-09-21)
        precedence   = unusable outranks everything: web cannot read now → state 1 inside
                       120 s, never "stopped"; stopped = web reads fine, nothing writes
        web restart  = started_at resets; NULL grace rearms; timestamped rows do not  ◀ named limit
        empty table, no web restart = NULL rows under an old started_at → stale at once
                       (missing table = the read raises = unusable, not NULL rows)
  a row whose name is not in beat_schedule is ignored
```

### Host gate decision tree (item 5 + break-glass — Eng review 2.2, 7); replaces `main.py:193`
```
  request ──▶ APP_ENV != production? ──yes──▶ pass through
                 │ no
                 ▼
       scope["path"] == "/healthz"? ──yes──▶ pass through
                 │ no
                 ▼
       PUBLIC_API_HOST unset ─────────────▶ 421  reason=public_api_host_unconfigured
       Host header ""  ───────────────────▶ 421  reason=host_absent
       Host != PUBLIC_API_HOST ───────────▶ 421  reason=host_mismatch
       GATE_BREAK_GLASS == "1"? ──yes──▶ admit · WARNING outcome=bypassed
                 │ no
       ORIGIN_VERIFY_SECRET empty ────────▶ 421  reason=origin_verify_secret_empty
       X-Origin-Verify missing ───────────▶ 421  reason=origin_verify_absent
       compare_digest fails ──────────────▶ 421  reason=origin_verify_mismatch
       else ──────────────────────────────▶ call_next
  every 421: body {"detail":"host_not_allowed"} (identical) · one app.gate WARNING
  the emitter is wrapped: an exception inside it never changes the response
```

## Inline diagrams to add during implementation
<!-- Eng review: execute-plan adds these as part of the change; "update" = stale today -->
| File | Layer | Diagram | Why |
|---|---|---|---|
| `backend/app/main.py` (comment at `:193`) | shared | the decision tree above, six reasons and the bypass branch | update: today's comment describes two checks and no reasons |
| `backend/app/gate_logging.py` | shared | what the emitter does to its host: log, then return; never raise | invisible behavior that must never alter a 421 |
| `backend/app/tasks.py` (signal handler) | service | task end → SUCCESS filter → `job_health.beat_intervals()` name filter → `run_async(upsert)` → clear errors on success / on raise: WARNING, second write stamps `last_error_at`, WARNING only if that fails too | Adversarial review: 12 one-shot tasks must not get rows; lookup is `entry["task"]`. Eng review 4 (2A): why the second write exists and why it cannot report a database outage |
| `backend/app/job_health.py` (Eng review 4; was `main.py` (refresher)) | API | lifespan refresher → snapshot + `read_at` → `build_jobs_payload(snapshot, now, started_at)` → `/healthz`; the per-request `stale` / `write_error` rules, including the NULL grace | why the handler never awaits anything, and why "waiting" expires |
| `frontend/src/hooks/useBackgroundJobs.js` (`deriveJobsState`; Eng review 5) | UI | first-match ladder 1 → 6 (unusable outranks everything), the state 5 headline count, and state 2's line: all stale AND some row without `write_error` → "nothing has run", else "may still be running" | the predicate looks wrong without the reason: `write_error` is only ever stamped by a SUCCESS, so a row that carries it ran. A full set of flags does not prove the worker is still up (Adversarial re-review, 2026-09-21) |
| `backend/app/auth/service.py:26` | service | rotation: set hash → count live rows → `logout()` commits both → re-read version | update: name `rotate_password()`; ordering is what keeps a crash fail-closed |
| `backend/app/models.py` (`JobHeartbeat`) | data | row lifecycle NULL → fresh → stale, per-task threshold | staleness is derived, never stored |
| `infra/release-smoke.py` | test | check groups × exit classes × `--skip` matrix | Adversarial review: skipped ≠ pass; `--self-test` fails a check in `--only` |
| `backend/tests/integration/auth/test_host_gate.py` | test | fixture: production env + the six reason cases | monkeypatched env per reason is not obvious from the tests |
| `infra/RUNBOOK.md` | docs | the corrected deployment-and-rollback diagram from this plan | update: the first draft named `fly releases rollback` |
| `backend/fly.toml:46` | config | (comment) counts pinned in `infra/fly-scale.json`, asserted by check 3 | update |

## Stale diagram audit
| File | Diagram | Still accurate? | Action |
|---|---|---|---|
| `backend/app/auth/models.py:69` | refresh-token state machine (`live ──logout / pwd-rotation──▶ revoked`) | **yes** — it already anticipated the rotation CLI, and item 6 reaching `service.logout()` matches it exactly | none |
| `backend/app/auth/service.py:5` | refresh rotation Case A/B | yes — untouched by this plan | none |
| `backend/app/auth/dependencies.py:66` | PyJWT exception tree | yes — item 5 adds gate reasons, not token errors | none |
| `.agents/docs/BACKEND_STRUCTURE.md:45` | schema ER diagram | **no** — OQ6 picked a table (Eng review 1A) | add `JobHeartbeat` in PR1b |
| `backend/app/main.py:193` | prose diagram of the gate's two checks | **no** after item 5 and the break-glass flag | rewrite as the decision tree below (Eng review 2.6) |
| `backend/fly.toml:46` | "singleton invariant enforced via `fly scale count beat=1` post-deploy" | **no** — counts are pinned in `infra/fly-scale.json` and asserted by smoke check 3 | point at the file (PR1a) |
| `backend/app/auth/service.py:26` | "Operator password rotation (M8 CLI; mechanism wired here)" | partly — the mechanism becomes `rotate_password()` | name the function (PR1b) |
| `infra/cloudflare-state.md:296` | break-glass = "redeploy the previous image" | **no** — expired the day PR #46 shipped (LESSONS 2026-09-11) | replace with a pointer to the diagnostics entry (PR1a) |
| `backend/tests/unit/test_healthz.py:1` | docstring forbidding version metadata on `/healthz` | **no** after item 14 | rewrite the test (PR1b) |
| `.agents/docs/BACKEND_STRUCTURE.md:781` | `/healthz` row: shallow `{"status": "ok"}` | **no** after item 14 | `/update-docs` (PR1b) |
| `.agents/docs/TECH_STACK.md` Production Deployment | "Fly Postgres — Managed PostgreSQL 16" | **no** — unmanaged Postgres Flex 17.2 (The Assignment) | correct in PR1a; TODO for aligning compose and CI |
| `.agents/docs/APP_FLOW.md` Settings | no "Background jobs" line | **no** after item 15 | `/update-docs` (PR1b) |
| `.agents/docs/FRONTEND_STRUCTURE.md` §4 Settings | `ICloudSettings` only | **no** — shared freshness module + jobs indicator | `/update-docs` (PR1b) |
| `.agents/docs/APP_FLOW.md` §4 Error Handling | "a 401 triggers a silent one-shot redirect to `/auth?return_to=…`" | **no** — `AuthPortalPage` has shown a bounce banner on `return_to` since M4, and Design review 10 changes its copy | `/update-docs` (PR1b) |
| `.agents/docs/FRONTEND_GUIDELINES.md` §1 Color Palette, §5 Component Patterns | no error color; no status dot pattern | **no** after Design review 5A — `sage-500` fresh dot, stock red as the documented error color, `FreshnessDot` pattern | `/update-docs` (PR1b); design-watched file, expect design-sync drift |
| `backend/app/auth/service.py:378` `logout` docstring | "Caller MUST validate the access token" | **no** — CLI is an intentional exception | one-line amendment (PR1b) |
| This plan, "Job heartbeat data flow" | NULL = "no data yet (not stale)", `worker dead` summary, "(5 states)", refresher in `main.py` | **no** — predated `read_at`, `now`, the NULL grace, `last_error_at`, and `label` | redrawn in place (Eng review 4) |
| `frontend/src/components/settings/ICloudSettings.jsx:531` | comment "Both sync schedules run every 10 minutes … three missed cycles" beside `SYNC_STALE_AFTER_MS` | yes today; becomes a second definition once `/healthz.jobs` exists | kept by 15a's file table; tracked by the P3 TODO "ICloudSettings consumes the server-derived job staleness" |

## Unresolved decisions
<!-- CEO review: the user did not answer or moved on; never defaulted -->
None. Every issue raised (1A, 1B, 2A, 2B, 2D, 2E, 2F, 3A, 3G, 4A, 4B, 4C, 5B, 5D, 6A, 8A, 8B, 9A, 9B) was
either decided by the user or had an obvious fix stated and folded in. The plan's six Open
Questions are implementation-time unknowns, not deferred decisions.
<!-- Eng review: decisions that may bite you later; the user did not answer or moved on -->
None from the engineering review either. Step 0 (C: PR1a and PR1b), 1A (table, one row per task),
1B (both restore paths), 1C (`web=1`), 3A (`infra/tests` plus a CI job), both TODO proposals, and
the `migration` risk tag were all decided on 2026-09-12; every other finding had an obvious fix
and is folded in above with an `Eng review N` breadcrumb.
<!-- Adversarial review: no new product/scope decisions; every finding was an underspecification
     and is folded in with an `Adversarial review:` breadcrumb. The GitHub 60-day disable is a
     named limitation of the chosen watchdog, not an unresolved choice. -->
<!-- Design review: decisions the user did not answer or moved on from; never defaulted -->
None. 1A, 2B, 3A, 4A, 5A, 6A, 7A, 7B, 8A, 8B, 8C and the three TODO decisions were all answered on
2026-09-14; every other design finding was an obvious fix, folded into item 15a (and items 1 and
6) with a `Design review N` breadcrumb.
<!-- Eng review 4 (scoped re-review of item 15a, 2026-09-21): decisions that may bite you later -->
None. 1A (a never-run job turns stale 3× its interval after the web process starts), 2A (keep
state 2 with `last_error_at` and a per-task window, limit stated), and 3A (the server sends
`label`) were answered on 2026-09-21. The pinned `/healthz.jobs` contract, `jobs.now` and the
server-aligned clock, the `job_health.py` module, the structural schedule test, the `apiBase.js`
naming, and the Settings test harness were obvious fixes, folded in with `Eng review 4`
breadcrumbs.
<!-- Adversarial review (scoped re-review of item 15a, 2026-09-21): no new product decision.
     The Waiting headline split and the all-stale state 2 line are corrections of copy that
     contradicted the worst-job rule. Keeping the NULL grace in memory, so a web restart
     rearms it, is the limit named next to the rule — not a new column. -->
<!-- Eng review 5 (scoped re-review of the adversarial pass's changes to item 15a, 2026-09-21): decisions that may bite you later -->
None. 1A (state 2 says "Nothing has run" only when every row is stale and some row has no
`write_error`) was answered on 2026-09-21. The "Stopped" / "Can't check" discriminator, the
pre-table-snapshot sentence, the smoke message order, "others normal" in state 2, and the two
leftover sentences (item 2, the test artifact) were obvious fixes, folded in with `Eng review 5`
breadcrumbs.
<!-- Adversarial re-review (scoped to Eng review 5, 1A, 2026-09-21): no new product decision.
     The predicate and both strings stand. The dead-worker window on all-flags is a named
     limit, not a third line. -->
<!-- Design re-review (scoped re-review of item 15a's user-visible outcomes, 2026-09-21): decisions the user did not answer or moved on from; never defaulted -->
None, and no question was asked. Every string this pass checked was already decided on
2026-09-21 and none was changed. The mockup update, "what a row says", the wider reason state 1
has no alert, journey rows 10 to 12 and the stated limit of state 2's second line were obvious
fixes, folded in with `Design re-review` breadcrumbs.

## REVIEW REPORT

| Review | Skill | Why | Status | Findings |
|---|---|---|---|---|
| CEO Review | `/plan-ceo-review` | Scope and strategy | ISSUES OPEN at review (SELECTIVE_EXPANSION, 2026-09-12), matching its `issues_open` log entry; the one remaining critical gap was closed by Eng review the same day (see item 2's break-glass entry and the Failure modes note). Corrected 2026-09-14: this row previously read "CLEAR". · resolved by /plan-eng-review, 2026-09-17 (framework 1.1.0 `resolved` entry): item 2's break-glass entry and the Failure modes note — `/healthz.gate_break_glass` plus the `ops-check` edge assertion. | 4 expansions accepted: scheduled `ops-check.yml` (both real incidents fell between releases, where nothing looked); `/healthz` reports the deployed commit (closes an existing REVIEW_CHECKLIST violation and the backend/frontend verification asymmetry); `RUNBOOK.md` naming + per-release migration reversibility (REVIEW_CHECKLIST cites a file that did not exist, one citation being M8's own criterion); in-app background-job staleness (flips `ui_scope` true). 4 CRITICAL GAPs found and closed in-plan: gate logging could turn 421 into 500; the watchdog had no watchdog; `/healthz` sits on Fly's health-check path; `fly ssh` is unreliable in CI (items 13/15 now compose over HTTP). 1 CRITICAL GAP left for PR1: the break-glass flag has no expiry and no detector. Also caught: the logged 2026-09-11 timezone bug would recur because `parseServerTime` is module-private; the CLI could set a >128-char password and lock out the account it exists to rescue. 0 unresolved decisions. PRD + roadmap synced. |
| Eng Review | `/plan-eng-review` | Architecture and tests (required) | CLEAN (FULL_REVIEW, 2026-09-12) · clean (2026-09-21): scoped re-review of item 15a's backend contract · re-review demanded by /plan-adversarial-review, 2026-09-21: State 5's headline is no longer always "Waiting for the first runs" (only while every row is NULL; otherwise "Waiting on N jobs"), and a restore that does not restart web is overdue rather than Waiting. State 2 no longer says jobs may still be running when every row is stale. A database the web has never read is "Can't check", not "Stopped"; "Stopped" is only once a snapshot ages out with no write_error · clean (2026-09-21): scoped re-review of what that adversarial pass changed in item 15a (Eng review 5) | **2026-09-21, Eng review 5 (scoped to the adversarial pass's changes to item 15a; Fable 5.1):** 0 critical gaps; every miss erred toward alarm. Sound as written: the state 5 headline split, a restore without a web restart reading overdue (it falls out of the pure payload function, no new code), and the cron reading `stale`; `jobs.now`, `label` and the `last_error_at` 3× window hold. Not sound, decided 1A: state 2's "Nothing has run" line was keyed on "every row is stale", but `write_error` is stamped only by a SUCCESS, so a row that carries it ran. A broken recorder with a live worker would have read "Nothing has run" from hour 3 on. The line now also needs a stale row with no `write_error`; both strings are unchanged. Moved: "Stopped" vs "Can't check" turns on whether the web can read the table now, not whether it ever has, so an outage web and worker share is "Can't check" after 2 min and never "Stopped" (truth table in item 15). Corrected: a pre-table snapshot is a missing table, which is an unusable reading, not NULL rows. Removed two leftovers the adversarial sweep missed: item 2's "both read Stopped", and the test artifact's "the cron tolerates one interval of NULL". Pinned: the smoke script checks the unusable rule first; "others normal" in state 2 means each row renders by its own `stale` flag. Tests: artifact edited in place, critical path 17 and three state 2 fixtures added. Performance 0. 0 TODOs. Flags unchanged. 0 unresolved. Adversarial re-review demanded for the state 2 predicate. Note for the design re-review: the adversarial demand note's sentence on state 2 is now narrower than it reads; item 15a's two state 2 rows are the truth. **2026-09-21, Eng review 4 (scoped, item 15a; Opus 5):** 1 CRITICAL GAP found and closed. A never-run job read "Waiting for the first runs" forever, so a worker dead at the PR1b deploy was silent on the card and the cron. Now a NULL row turns stale 3× its interval after the web process starts (1A). State 2's "recent write error" was measured on a column that did not exist, over a 30 s window. Now it uses `last_error_at` and a per-task window, and states its limit: a database outage reads "Stopped" (2A). The `jobs` shape is pinned once as a contract in item 15. `jobs.now` gives a server-aligned clock, since the 2-minute cap had compared a server timestamp with the browser clock. The server sends each row's `label`, so there is one name map for JS and Python (3A). `backend/app/job_health.py` is the one home for `beat_intervals()`, labels, the payload builder and the refresher, doc-mapped. Tests: diagram + critical paths 15 and 16, artifact edited in place. Performance 0. 0 TODOs. Flags unchanged. 0 unresolved. Adversarial re-review demanded, because 15a's contract never had an adversarial pass and 2A changes its write-error rule. **2026-09-12:** Step 0: complexity check triggered (~35–40 files); scope kept, PR1 split into 1a (docs, scripts, config) and 1b (code, UI) so criteria 8 and 19 fall out of the sequence. 2 CRITICAL GAPs found and closed in-plan: the backend rollback named a flyctl command that does not exist (`fly releases rollback`) and would have failed across a migration boundary regardless → `fly deploy --image <ref> --skip-release-command`; item 15's TTL-cached database read sat on Fly's probe path → background refresher, the handler never awaits the store. The CEO review's remaining gap (break-glass left on) closed via `/healthz.gate_break_glass` + the cron. Decided: `web=1`; `job_heartbeats` table, one row per task; the drill exercises both restore paths; scripts in Python with `infra/tests` + an `infra-tests` CI job. Verified on this host or the live site: `fly tokens create readonly` exists (OQ5 largely answered), `/mealboard` already gets `max-age=0, must-revalidate` (item 11 needs no change), `%VITE_GIT_COMMIT%` + `vercel.json` `buildCommand` is check 8's mechanism. Architecture 8 issues / Code quality 7 / Tests: diagram + 13 critical paths, 0 gaps / Performance 0 blocking. 2 TODOs added, `risk_tags` += `migration`, 0 unresolved. |
| Adversarial Review | `/plan-adversarial-review` | Red-team pass, prefer another model | ISSUES FOUND (7 passes, 2026-09-14) · clean (2026-09-21): scoped re-review of item 15a's /healthz.jobs contract · re-review demanded by /plan-eng-review, 2026-09-21: state 2's "Nothing has run, and the app can't record that either" line no longer applies whenever every row is stale. It now also needs at least one row with no write_error. When every row is stale and every row has write_error the line stays "They may still be running", because write_error is stamped only when a task reached SUCCESS inside 3x its interval, so those jobs ran (Eng review 5, 1A, user-decided) · clean (2026-09-21): scoped re-review of Eng review 5 (1A), the state 2 predicate | **2026-09-21, scoped re-review of Eng review 5 (1A), the state 2 predicate (Grok 4.7):** the predicate holds. "Nothing has run, and the app can't record that either" only when every row is stale and at least one row has no `write_error`; all-stale and all-`write_error` stays "They may still be running". Both pins hold: smoke checks an unusable reading before stale rows, and "Stopped" vs "Can't check" turns on whether the web can read the table now. Folded: a failure-mode parenthetical still said the reassuring line applies only when some row is fresh (that false-alarms a live worker), and the heartbeat diagram dropped a NULL `last_success_at` and called all-flags "worker fine". Named, not a new string: all-flags is also a worker that died inside 3× the shortest flag; the card stays red and the cron already failed. No re-review demanded. **2026-09-21, scoped re-review of item 15a's `/healthz.jobs` contract (Grok 4.7):** the contract had never been attacked, and two older sentences still contradicted it. Item 13 no longer re-derives a minimum beat interval; the cron reads `stale`. The "one schedule interval" grace is gone. State 5's headline is "Waiting for the first runs" only while every row is NULL, then "Waiting on N jobs". State 2 says jobs may still be running only while some row is fresh. A malformed `jobs` body is "can't check", not green. Named limit: restarting web rearms the never-run grace. Design and eng re-review demanded for the headline and the state 2 line. **2026-09-14:** PR1a smoke skip list inverted (dropped check 1, kept PR1b-only `gate_break_glass`); `task_postrun` on all 16 tasks would poison "worker dead"; GitHub 60-day disable is real on this public repo and workflow runs do not reset it; `--self-test` of check 3 cannot prove the cron; Settings via `api.js` can log the household out; `logout()` commits so rotation must share the session; drill counted `job_heartbeats` before the table exists; Vercel "latest staged" is the wrong promote. All folded in-plan. Named limitation: 60-day watchdog death if no commit. 0 new critical gaps left open. |
| Design Review | `/plan-design-review` | UI and UX | CLEAN (7 passes + HTML mockups, 2026-09-14) · re-review demanded by /plan-adversarial-review, 2026-09-21: State 5's headline is no longer always "Waiting for the first runs" (only while every row is NULL; otherwise "Waiting on N jobs"), and a restore that does not restart web is overdue rather than Waiting. State 2 no longer says jobs may still be running when every row is stale. A database the web has never read is "Can't check", not "Stopped"; "Stopped" is only once a snapshot ages out with no write_error · clean (2026-09-21): scoped re-review of item 15a's user-visible outcomes after the adversarial pass and Eng review 5 | **2026-09-21, scoped re-review of item 15a's user-visible outcomes (Fable 5.1):** 7 → 9. The tables held on all four demanded points: the state 5 headline split; a restore without a web restart reading overdue (state 3 "Nothing has run yet", then state 4); state 2's two lines per Eng review 5 (1A); and "Can't check" vs "Stopped" turning on whether the web can read now. The mockup did not hold: its one Waiting fixture still read "Waiting for the first runs" with two jobs run, state 2 had one line, a never-run overdue row would have rendered "last ran null min ago", and red row text was keyed on `write_error` alone. Brought to the table, fourteen fixtures rendered and read back at desktop and 375 px, layout unchanged (8C stands), pin refreshed. Pinned: "what a row says" follows the row's own values, so a fresh flagged row reads "ran N ago" in state 2 as in state 6 (row 2's "affected rows" had left it open). Added: why state 1 still has no alert now that it covers an unreadable database; journey rows 11 (state 2) and 12 (a restore under a running web); the stated limit of state 2's second line (under two faults at once it overstates, toward alarm; both strings stand). No string changed, no question asked, 0 TODOs, 0 unresolved, no re-review demanded. **2026-09-14:** Design completeness 3 → 9. New item 15a specifies what the household sees: a "Background jobs" card above Account plus a top-of-Settings alert only when a job is behind (1A); six states plus loading whose headline follows the **worst** job, never the newest success (a dead sweep could otherwise hide behind a fresh sync); a stale reading keeps its last value with a note for 2 min instead of flapping to "can't check" every time the Postgres primary wakes (2B, supersedes Eng 1.2's rendering); plain-language "what this means" lines and a hand-off for a second household (3A); one name everywhere (4A); sage dot + documented stock red (5A); measured AA contrast, darker gray for the card and iCloud line (6A); version and break-glass never on Settings (7A, 7B); always-visible job rows chosen from 3 mockups (8C). **Backend contract added, not yet re-reviewed by eng/adversarial:** `jobs.read_at`, `stale` computed at serve time, every beat task listed with `null` when never run. Found and corrected: the sign-in redirect is not silent (APP_FLOW §4 stale); the specific "password changed" message is not derivable, so PR1b ships an honest generic bounce-banner copy change (TODO 1 → built). Runbook: tell the household before rotating, deploy at a quiet hour, record observed downtime. 2 TODOs added (app-wide contrast, Calendar-page sync alert). 0 unresolved. |
| Implementation Review | `/review-implementation` | First code review (required) | clean (PR1a, 2026-09-23) | 18 found, 17 fixed, 1 accepted (Access apps compared by count; plan item 9); 0 open. Overruled the plan in place, with breadcrumbs: smoke check 8 compares each tier's code (`git diff` of `frontend/`, `backend/`) instead of ancestry, which passed a forgotten promote and failed deploy-before-merge; Between-the-PRs step 0 (the two Vercel settings) moves before the PR1a merge; an unanticipated script error is exit 2. Also fixed: failed WAL rows no longer count as restore points; standbys neither counted nor "start it"; `/healthz` unreachable on one path while the other answers is exit 1; the empty Vercel stamp names the setting; RUNBOOK subshells, `alembic history` first line, verify-stopped before a quiesced migration or a real restore; drill `BEGIN READ ONLY` and a scratch-archive check; CI build carries `VITE_API_BASE_URL`. Item 11 needed no change: `/mealboard` and `/` send `max-age=0, must-revalidate` (re-verified live). `infra/tests` 99 → 110, the 10 new regression tests red on the pre-review scripts. |
| Adversarial subagent | inside `/review-implementation` | Fresh-context pass on the diff | issues_open (large, 2026-09-23) · resolved by /review-implementation, 2026-09-23: all 13 closed in the working tree (see the Implementation Review row) · final pass inside `/final-review`: issues_open (large, 2026-09-23) · resolved by /final-review, 2026-09-23: 18 fixed, 1 mitigated + TODOS P2, 3 informational (in the Final Review concern), 2 by design / not a defect | 13: check 8 ancestry, standbys, failed WAL rows, one-path timeout, crash exit 1, Access-app count, restart policy vs stop, empty stamp, `cd backend`, `alembic history` first line, break-glass checkout, drill archive and read-only, CI env shape. **Final pass, 24:** a missing `/assets` chunk served as year-cached HTML (confirmed live), break-glass cleared from `master`, first-run `web=1` absent from the RUNBOOK, counts read mid-WAL-replay, quiet-database PITR target, §4 `fly deploy` from the repo root, Access apps on other zones, a started standby uncounted, pre-PR1b `--skip=healthz_jobs`, asyncpg URL reshape, Instant Rollback to roll forward, no Staged build, cancelled CI run, self-test pinned to `web=2`, stale `fly releases rollback` in LESSONS, and more |
| QA | `/qa` | Browser verification | — | — |
| Design Audit | `/design-review` | Live-site grade | — | — |
| Final Review | `/final-review` | Second opinion (required) | clean (PR1a, 2026-09-23) | claude-code / Opus 5.5, the same harness and model as the first review (independence reduced, warned). 30 found (1 critical), 24 fixed, 1 mitigated and deferred to TODOS P2 with the user's approval, 2 by design or not a defect, 3 informational leads; 0 open. **Critical, confirmed live:** a request for a missing `/assets/*.js` gets `index.html` with a one-year `immutable` header, cacheable at Cloudflare, so the RUNBOOK's own chunk-load check poisons the cache a frontend rollback depends on. RUNBOOK §5.1 now purges and empty-cache-reloads, the check's signal is corrected, and TODOS P2 tracks the `vercel.json` fix. **Overruled the plan in place:** item 1's "genuinely one command, genuinely reversible" frontend rollback; item 2 and open question 3's break-glass clear, now a redeploy of the running image from its release tag (user decision). Also fixed: RUNBOOK §5.2 on its first run (no previous tag; the smoke script missing from the previous checkout), §4 `cd backend`, first-run `web=1`, a cancelled CI run, no Staged build, rolling the frontend forward; the dead-worker purge now runs from web before the worker starts; never start a standby, and a *started* standby now counts in check 3; drill counts wait for `pg_is_in_recovery() = f` and check `archive_*` on both paths; an R2 upload check that closes the boto3 TODO at the first M8 release (user decision); Access apps filtered to the zone, failing closed (user decision); the self-test fails whatever the pin. `infra/tests` 110 → 133 (real-git and mutation controls). Unverified leads: the read-only smoke subsets against production before merge; Vercel's apex certificate for Mode A; check 6's `max-age` branch may never fire. |

**VERDICT:** CLEARED TO SHIP

## Adversarial Review Summary

- **Skill:** `plan-adversarial-review`
- **Harness:** grok
- **Model:** Grok 4.7
- **Date:** 20260921-222442
- **Eng review provenance:** harness=claude-code model=Fable 5.1 (different family)
- **Passes run:** 7, scoped to Eng review 5 (1A): item 15a state 2's consequence line, the
  `write_error` premise that line rests on, and the two pins named in the demand (smoke checks
  an unusable reading before stale rows; "Stopped" vs "Can't check" turns on whether the web
  can read the table now). The earlier passes stay in the REVIEW REPORT row.

### What this review changed
- Item 15a: the Eng review 5 expression stands. "Nothing has run, and the app can't record that either" only when every row is stale and at least one row has no `write_error`. All-stale and all-`write_error` stays "They may still be running", including when `last_success_at` is still NULL. Named the limit the diagram had overclaimed: those rows are also a worker that died inside 3× the shortest flagged interval, the card stays red, the cron has already failed, and the copy does not split the two pictures.
- Failure modes: the "all stale, one flag" row's Handled cell no longer says the reassuring line applies only when some row is fresh. The `write_error` row's test now includes a NULL recorded success.
- Heartbeat diagram: `write_error` matches the prose (NULL `last_success_at` still flags). The comment no longer equates all-flags with "worker fine".
- Test artifact: the null-success body stays on "may still be running", and the payload builder must flag that body.
- Both demanded pins checked and left as written.

### Remaining adversarial concerns
none.

Carried, not re-opened: the web-restart rearm of the never-run grace; the GitHub 60-day scheduled-workflow disable; open questions 1, 2, and 4; Mode B. Office-hours is still `issues_open` on the hourly Postgres stop; this pass did not close it.

### Why these were flagged
This pass was intentionally adversarial: it looked for a false calm or a false alarm in the
state 2 predicate, especially a broken recorder with a live worker and the all-error stale
picture, plus a worker that dies while every flag is still set. It did not reopen the rest of
the M8 plan.
