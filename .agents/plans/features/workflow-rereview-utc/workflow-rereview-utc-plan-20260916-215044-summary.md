# A review can send an earlier review back — declared re-review, visible concerns, UTC everywhere — execution summary

- Plan: .agents/plans/features/workflow-rereview-utc/workflow-rereview-utc-plan-20260916-215044.md
- Branch: workflow-rereview-utc
- Started: 2026-09-18T03:32:50Z
- Metadata: {"implementation_status": "not-started", "plan_kind": "feature", "plan_mode": "feature", "reason": "Eng review R (scoped re-run, 2026-09-18 UTC): clean; R1B (the next review audits the previous none) and R2A (mechanical rollback capture, stale rows converted to blocked before the revert) folded in; nothing reversed. Next: PR-F1 in ../framework.", "registry_key": "plan:workflow-rereview-utc", "review_status": ["eng-reviewed", "adversarial-reviewed"], "risk_tags": ["infra", "security"], "ui_scope": false, "updated_at": "2026-09-18T03:15:19Z", "workflow_status": "eng-reviewed"} (read before the start sync set `implementing`)
- Test artifact: .agents/plans/testing/workflow-rereview-utc-test-artifact.md
- Metadata (resume for Step 13, 2026-09-18T04:38:19Z): {"implementation_status": "blocked", "plan_kind": "feature", "plan_mode": "feature", "reason": "Steps 1-12 done in ../framework: PR-F1 #5 (1.1.1) and PR-F2 #6 (1.2.0, stacked on #5) are open. PR-T (step 13) waits until framework origin/main reads VERSION 1.2.0; resume with /execute-plan after both merge.", "registry_key": "plan:workflow-rereview-utc", "review_status": ["eng-reviewed", "adversarial-reviewed"], "risk_tags": ["infra", "security"], "ui_scope": false, "updated_at": "2026-09-18T04:13:53Z", "workflow_status": "implementing"} (read before the start sync set `implementing` again; gate: `--next` names `/execute-plan`, `unreadable` 0; framework #5 merged 2026-09-18T04:18:01Z as `681a512`, #6 merged 2026-09-18T04:18:31Z as `b17c320`; `origin/main` `VERSION` 1.2.0)
- Design source of truth: none (framework helpers and skill text; no UI; the plan has no `## Design Source of Truth` section)
- Design sync: `modules.design_sync` is `true`; `design-sync-check` prints DRIFT (2 commits since 2026-05-08), the known warning this plan's §7 turns off; `--pin-sha` prints `c0f7f30`

## Steps
- [✓] Step 1 — PR-F1 §6 `_lib.timestamp_slug` in UTC (docstring, `ctx` `TIMESTAMP` follows); `WORKFLOW.md` filename lines say UTC (2026-09-18T03:45:34Z)
- [✓] Step 2 — PR-F1 §6 the one UTC rule in `_shared/preamble.md` with the `<utc-date>` / `<utc-timestamp>` slots; the slot sweep over the §6 inventory (skills and references); `ship` / `update-docs` `date -u +%F`; `obsidian-sync.md` `completed=<utc-date>` (2026-09-18T03:45:34Z)
- [✓] Step 3 — PR-F1 §6 `When (UTC)` dashboard header in `workflow-state` (2026-09-18T03:45:34Z)
- [✓] Step 4 — PR-F1 §7 design-sync helpers: product-neutral strings, marker key `design_tool_url` (2026-09-18T03:45:34Z)
- [✓] Step 5 — PR-F1 §8 tests: `ctx` `TIMESTAMP` under `TZ=Pacific/Kiritimati` in a subprocess, `local_date_violations` lint on fixtures and the real payload, `When (UTC)` header, design-sync strings (2026-09-18T03:45:34Z)
- [✓] Step 6 — PR-F1 §9 release: `VERSION` 1.1.1, `CHANGELOG.md` (UTC, design-sync rename, east-of-Greenwich note), `README.md` (UTC, `When (UTC)`, `obsidian-workflow` sentence, design-sync); framework suite green; commit on `utc-dates`, push, open PR-F1 (2026-09-18T03:45:34Z)
- [✓] Step 7 — PR-F2 §2 `_lib.py`: `can_demand`, `is_run` / `last_run` / `plan_review_history` (pass 1), two-pass `reviews_for_plan` with `stale`, `READER_ONLY_KEYS` stripped first, type guards, `one_line` / `DISPLAY_LIMIT`, `current_plan` (moved from `workflow-state`; fills `review_log_append`), `concern` / `concern_ts` and `stale_notes` on the returned copy, state diagram (2026-09-18T04:13:49Z)
- [✓] Step 8 — PR-F2 §1 / §3 `review-log`: `--rereview` / `--rereview-note`, reader-only keys refused, declaration rules above the `resolved` early return, `resolved` refused on a stale target with its own message, `concern` validation, plan lookup through `_lib.current_plan`, docstring and validation-order comment (2026-09-18T04:13:49Z)
- [✓] Step 9 — PR-F2 §4 `workflow-state` and `review-read`: `BLOCKING` at the four sites, `blocking_reason` / `stale_reason`, `stale !` row, `Concern:` and `Reason:` lines with de-duplication, every free-text fragment through `_lib.one_line`, `concerns` and `reason` in both JSON outputs, gate comment block; `review-read` plan mode shows `stale`, `--all` strips reader-only keys, both use `current_plan` (2026-09-18T04:13:49Z)
- [✓] Step 10 — PR-F2 §5 skills and shared docs: `_shared/dashboard.md` new section (what counts, declare, scoped re-run with escalation, `stale` in dispositions / verdicts / diagram, `CONCERN:` / `REASON:` template lines); `plan-footer.md` row rule; `obsidian-sync.md` concern and stale sentences; the four plan reviews (Ground-step audit of an earlier `none`, handoff "`failed` or `stale`", Completion per-review question then `--rereview`, "Do not use when" exceptions; design step 2 exception); `review-implementation` and `final-review` overruled-plan-text paragraph (+ R1B audit in review-implementation); `WORKFLOW.md` `stale` and pointer (2026-09-18T04:13:49Z)
- [✓] Step 11 — PR-F2 §8 tests: `test_lib.py` stale matrix and guards, `test_review_log.py` both forms and byte-identical log, `test_workflow_state.py` M8 replay and banner-level checks, `test_skill_lint.py` signatures, `test_cli.py` unchanged; the 20 existing CLI plan-review calls gain `--rereview none` (2026-09-18T04:13:49Z)
- [✓] Step 12 — PR-F2 §9 release: `VERSION` 1.2.0, `CHANGELOG.md` upgrade note, `README.md` (`--rereview`, `concern`, `stale`, `Concern:` / `Reason:`; examples gain `--rereview none`); framework suite green; commit on `rereview`, push, open PR-F2 (2026-09-18T04:13:49Z)
- [✓] Step 13 — PR-T §10 upgrade into this branch after PR-F1 and PR-F2 merged: fetched and fast-forwarded onto `origin/master` (#54), asserted framework `origin/main` `VERSION` 1.2.0, `framework upgrade <literal worktree path> --dry-run` then apply (41 files, 0 WARN, the four gate helpers together), §7 two project-owned edits, host suite 449 passed, before/after `review-read --plan` dispositions identical for both in-flight registry entries, LESSONS check (2026-09-18T04:44:17Z)

Not boxes: plan §11 (the M8 follow-through on `prod-launch-release`) runs after PR-T merges and is an operator step on another branch. The framework commits and pull requests are outside `/ship`'s scope; the assumptions below decide them, following the parent plan's precedent.

## Step notes

### Steps 1–6 — PR-F1, framework 1.1.1 (2026-09-18T03:45:34Z)
- Repo and branch: `../framework`, branch `utc-dates` cut from `main` at `89f6aab`; commit `d856fad`; pull request https://github.com/willdoucet/framework/pull/5 (open; merging is the operator's step). 34 files, +190/−51.
- Step 1: `_lib.timestamp_slug` uses `datetime.now(timezone.utc)` with a docstring naming `ctx`'s `TIMESTAMP`; `WORKFLOW.md`'s two filename lines say the timestamp is UTC.
- Step 2: `_shared/preamble.md` gains the one rule beside the `ctx` variables (commands `date -u +%F`, `date -u +%FT%TZ`, `ctx` `TIMESTAMP`; slots `<utc-date>`, `<utc-timestamp>`; nothing already written is rewritten). The §6 inventory swept: 41 exact-match replacements in 33 files, every anchor matched exactly the expected count. `obsidian-sync.md` `completed=<utc-date>`; `update-docs` (`date -u +%F`, `<utc-date>` in the roadmap-row and LESSONS-row sentences, `report-format.md`, `doc-checks.md`); `execute-plan` (`(2026-09-09T14:32:00Z)`, `(<utc-timestamp>)`, `(reconciled <utc-date>)`, the two "timestamp" prose slots); `qa` and `design-review` with their report templates and score JSON; `ship` (two `date -u +%F`, two `<utc-date>` prose slots, `pr-body.md`, `learning-summary.md`); `learning-module`; `init-project/doc-blueprints.md`; `office-hours` plan and epic templates (`<utc-date>`) and `office-hours/SKILL.md:178` `{plan date}` in place (file still 499 lines); `plan-eng-review/test-artifact.md`. `{{DATE}}` in `templates/docs` untouched.
- Step 3: `workflow-state` `dashboard_lines` header `When (UTC)` (10 characters in the 11-character column).
- Step 4: `design-sync-check` reads `design_tool_url` (fallback text "the design tool"); `design-sync-mark` writes `design_tool_url`, help and docstring say "the design tool's URL"; `test_design_sync.py` asserts the new key. Plain rename, no carry-over, as the plan's spec review decided.
- Step 5: `payload/tests/test_lib.py` `test_ctx_timestamp_is_utc_whatever_tz_the_shell_has` runs `ctx --json` in a subprocess with `TZ=Pacific/Kiritimati` and asserts the parsed `TIMESTAMP` is within 60 s of `datetime.now(timezone.utc)`, plus the in-process slug. Negative control by hand (`main`'s `_lib.py` beside the same `ctx`, run from inside the worktree): old slug `20260918-173707`, 14.0 h off the UTC clock; new slug `20260918-033707`, 0.0 h. `tests/test_skill_lint.py`: `LOCAL_DATE_PATTERNS`, `local_date_violations`, nine bad fixtures (every old spelling, case-insensitive prose, `date +` without `-u`), ten good ones (`{{DATE}}`, both slots, `{plan date}`, `date -u`, `YYYYMMDD-HHMMSS`, unquoted `YYYY-MM-DD`, `update +1`), and `dated_payload_files()` parametrized over every `SKILL.md`, `references/*.md`, `_shared/*.md` and `WORKFLOW.md`. `test_workflow_state.py` asserts the header line `Review       | Status        | When (UTC) | Extra`.
- Step 6: `VERSION` 1.1.1; `CHANGELOG.md` `## 1.1.1 — 2026-09-18` with the bullets and the east-of-Greenwich upgrade note (Eng review OQ2 wording); `README.md`: `TIMESTAMP` row says UTC, dashboard example header `When (UTC)`, the `obsidian-workflow` sentence says it manages frontmatter and the registry for every plan, vault-sourced or not, and the design-sync section names `design_tool_url`.
- Verification: `python3 -m pytest tests payload/tests -q` → 525 passed (baseline 428), before and after the version bump; `framework version` prints 1.1.1; the §6 completion grep over `payload/skills` and `payload/WORKFLOW.md` returns nothing; `grep -ri claude_design` over `payload`, `bin`, README and CHANGELOG returns nothing.
- Decisions: the preamble rule reads "never from what you believe the date is" so the payload passes its own lint ("today's date" is a forbidden spelling); `{plan date}` is the display slot the plan chose.
- Test-artifact gap: none for this PR. Deviations: none.

## Step notes (continued)

### Steps 7–12 — PR-F2, framework 1.2.0 (2026-09-18T04:13:49Z)
- Repo and branch: `../framework`, branch `rereview` cut from `utc-dates` at `d856fad`; commit `6161b2e`; pull request https://github.com/willdoucet/framework/pull/6, opened against `utc-dates` so its diff is the gate alone (merge #5 first; GitHub retargets it to `main` when `utc-dates` is deleted). 21 files, +1698/−160 over #5.
- Step 7, `payload/bin/_lib.py`: `can_demand` beside `can_resolve` (same-stage gating tiers, never self, never `ship`); `READER_ONLY_KEYS` with `strip_reader_only`; `is_run`; `plan_review_history` (pass 1: entries sorted by `ts`, runs, demands collected from every run's `rereview` only when it is a list, the name a string, `can_demand` holds and the target has a run; duplicates collapse; a non-string note becomes its JSON text; a non-string `skill` is skipped where 1.1.0 would have raised on an unhashable list); `last_run`; `reviews_for_plan` as pass 2 (the 1.1.0 resolution walk on stripped entries, then `stale` on a passed or resolved review with a demand at or after its last run, `>=`; `demanded_by` on a failed one; `concern` and `concern_ts` carried from the last run onto a resolution record, a raw `concern` on a resolution dropped); `review_disposition` reads `stale` from `stale_by` on the copy; `one_line` and `DISPLAY_LIMIT = 300` (JSON text for non-strings, `str.split()` collapses U+2028/U+2029/U+0085, cut with `…`, returns `""` when `json.dumps` fails); `current_plan` moved from `workflow-state` and used by `review_log_append`; the state diagram above `LEGACY_STATUS_MAP` and the pass diagram above `plan_review_history`.
- Step 8, `payload/bin/review-log`: `--rereview` (append, `SKILL|none`) and `--rereview-note`; `RESERVED_FIELDS` names the flag for `rereview` and `rereview_note`; `validate_declaration` on the merged entry, called above the `resolved` early return: reader-only keys refused, `concern` a non-empty string on run entries only, the declaration required from the four plan reviews on `clean` / `issues_open`, refused on `resolved` / `done` and from a non-gating tier, list-of-strings with no `none` member, note rules, each name checked (self, `ship`, gating, `can_demand`), the plan resolved through `current_plan` (exit 2 when none) and each name's run checked through `plan_review_history` / `last_run`; the `resolved` branch refuses a stale target with `a re-review demand clears only when <skill> runs again` (the subagent wording for `adversarial-subagent`) as its own branch before the `!= failed` fall-through; docstring examples and the validation-order comment.
- Step 9, `payload/bin/workflow-state`: `BLOCKING = ("failed", "stale")` at `gate_state`, `reason_for`, `first_blocking` and `fmt_missing` (` (re-review demanded)`); `invoke_of`, `stale_reason`, `blocking_reason`; `failed_reason` gains the demanded suffix; `attention` computes `concerns` (tier order, from the copy's `concern`) and `reason` (flattened, untruncated) once for `decide` and `dashboard`; `fmt_attention` prints `Concern:` lines and the de-duplicated `Reason:` line under the Open lines in the banner and `--next` text (suppressed when blocked or needs-context); the blocked branch's reason goes through `one_line`; the dashboard row reads `stale !`, prints its `Reason:` line in full, and both JSON outputs carry `concerns` and `reason`; `find_current_plan` replaced by `_lib.current_plan`; the gate comment block and docstrings rewritten. The completion grep `grep -n '"failed"\|"passed"' payload/bin/*` leaves only the intended sites: the "already passed" resolver hint and the `resolved` rules. `payload/bin/review-read`: `listed_disposition` strips reader-only keys for `--all`; plan mode shows `stale`; `current_plan`; docstring.
- Step 10, skills and shared docs: `_shared/dashboard.md` (`CONCERN:` / `REASON:` template lines, the sentence that `stale` is not failed, the new section "When your review changes what an earlier review approved" with what counts, what does not, the test sentence, how to declare, how it clears with the scoped re-run and the escalation rule, the state diagram with `stale`, Verdicts with `stale` and ` (re-review demanded)`); `_shared/plan-footer.md` (the demand row rule); `_shared/obsidian-sync.md` (the `concern` sentence on `DONE_WITH_CONCERNS`, the `stale` sentence after the table; the table itself unchanged, so the vocabulary parity test holds); the four plan reviews (handoff "`failed` or `stale`", the Ground-step audit of an earlier `none`, "Do not use when" exceptions for eng and CEO, the failed-or-stale exception in design's bullet and step 2, the per-review question before the `review-log` call, `--rereview none` in the call); `review-implementation` (step 5 audit paragraph recorded as its `concern`, the overruled-plan-text paragraph with the same-second subagent footgun after the REVIEW REPORT update) and `final-review` (the same paragraph); `WORKFLOW.md` pointer. Line counts: CEO 425, eng 363, adversarial 280, design 418, review-implementation 352, final-review 183, all under 500.
- Step 11, tests: `test_lib.py` (`can_demand`, `is_run` and the keys, the stale matrix, equal `ts` in both file orders, the union-merged failure, every hand-appended shape, strip-first inside the resolution walk, the concern carry, malformed fields never raise, `one_line`, `current_plan`); `test_review_log.py` (the declaration required in both forms, a demand stored, 17 rejections in both forms with a byte-identical log, refused on `resolved` / `done` / `office-hours`, reader-only keys and `concern`, `resolved` refused on a stale target by every resolver and a stale resolver refused, resolving a failed-and-demanded review, a demand needs a plan, epic branches, `review-read` stale); `test_workflow_state.py` (the M8 replay with the execute-plan guard, both adversarial-over-eng cases, no way around a demand, stale reasons, stale ship-stage review and the same-second subagent, stale while implementing / blocked / ready-for-review, the already-passed hint, concern and reason lines, blocked suppression, the display-limit dedupe, hostile lines at the banner level, long notes, `ui_scope` switched off, pre-1.2.0 entries, unreadable log, the source-level `one_line` check); `tests/test_skill_lint.py` (`rereview_violations` with fixtures, the new signature in `SHARED_SIGNATURES`, the stale exceptions, the overruled-text paragraph, the dashboard block, the `review-log` docstring and README examples). Existing CLI plan-review calls that needed `--rereview none`: **5** (`test_json_form_autofills_context` and the four `test_ts_must_be_a_non_empty_string_when_supplied` cases), not the 20 the plan estimated; the rest were `resolved` entries or raw `_repo.log_review` appends. The rule was not loosened.
- Step 12: `VERSION` 1.2.0; `CHANGELOG.md` `## 1.2.0 — 2026-09-18` with the upgrade note; `README.md` (the Feature pipeline sentence, the Verdict row, a `Concern, Reason` row, `--rereview` / `--rereview-note` option rows, the reserved keys, the two examples with `--rereview`, the demand paragraph, `review-read` `stale`).
- Verification: `python3 -m pytest tests payload/tests -q` → 580 passed (525 after PR-F1); `framework version` prints 1.2.0. By hand in a temp repo before the tests were written: the M8 replay (demand → NOT CLEARED, `Eng          | stale !`, `--next` names eng with the demanded reason, both resolution attempts refused with the demand message, a hand-appended clean before the demand and a hand-appended resolution leave it blocked, only an eng run dated after the demand clears it) and the declaration refusal.
- Decisions: `--dashboard --json` keeps raw note values in each row's `extra` (untruncated, as the plan says for the dashboard), so a note holding U+2028 is a legal JSON string there; the forgery rule (exactly one `Next:` line) is asserted on the banner and `--next` text, and the JSON outputs are asserted to parse. `stale_reason` for `adversarial-subagent` ends `— re-run /review-implementation`, as `failed_reason` does. PR-F2 targets `utc-dates` rather than `main` so review sees the gate alone.
- Test-artifact gaps: none for the framework side; the artifact's critical paths 7, 8 and 9 and its Deployment guards are PR-T and operator steps and are still to run.
- Deviations: none in behaviour. The plan's "20 existing CLI-written plan-review calls" was 5 in fact.

### Step 13 — PR-T, framework 1.2.0 into todo-app (2026-09-18T04:44:17Z)
- Waited from 2026-09-18T04:13:49Z until #5 (merged 2026-09-18T04:18:01Z, `681a512`) and #6 (merged 2026-09-18T04:18:31Z, `b17c320`) landed on framework `main`. Resumed from the worktree with `/execute-plan workflow-rereview-utc`; the gate program printed `cleared for implementation` (`--next` named `/execute-plan`, `unreadable` 0).
- §10.1 merge: `git fetch origin --prune`; `origin/master` was one commit ahead (`e1699fc`, #54: docs and `asset_lifecycle.py`). The branch's uncommitted `LESSONS.md` rows collided with #54's new Bug Log row on the same table line, so the branch diff was saved to a patch, the file restored, `git merge origin/master` fast-forwarded `36e1b5d` → `e1699fc` (no merge commit), and the patch re-applied with `git apply --3way`; the one conflict (both sides append a 2026-09-17 Bug Log row after the same line) was resolved by keeping both rows, #54's first. `LESSONS.md` vs `HEAD` is the branch's original 23 insertions plus one new bullet (below).
- §10.2 source: the local `../framework` checkout was on the deleted `rereview` branch; switched to `main` and fast-forwarded to `b17c320`, asserted `HEAD == origin/main`, `VERSION` 1.2.0 in the working tree and at `origin/main:VERSION`, `framework version` → 1.2.0. Never upgraded from `utc-dates` or `rereview`.
- §10.2 upgrade: `python3 ~/Documents/Dev/Projects/framework/bin/framework upgrade <the worktree's literal absolute path> --dry-run` — header names the worktree, `installed 1.1.0 -> 1.2.0`, 41 payload files planned, 0 `WARN`, `_lib.py` / `workflow-state` / `review-log` / `review-read` all listed. Apply printed the identical 41 files as `done`; `config.json` and `manifest.json` `framework_version` 1.2.0 (manifest `generated_at` 2026-09-18T04:39:21Z, 139 files); the main checkout's `git status` was empty afterwards (LESSONS Corrections 2026-09-16 checked). Nothing under `.agents/bin`, `.agents/skills`, `.agents/tests`, `.agents/templates` hand-edited.
- §10.3 / §7: `.agents/config.json` `modules.design_sync` → `false` (json round-trip verified byte-identical before the edit, so only the flag and the upgrade's `framework_version` differ); `git rm .agents/state/design-system-synced.json`. `design-sync-check` → `design-sync: module disabled`, exit 0; `design-sync-check --pin-sha` → `c0f7f30bcdfc1c6d2a2fdfeba8d39c6fa4c294cd`, exit 0 (success criterion 8). `grep -ril "claude design\|claude_design"` over `.agents/bin`, `.agents/skills`, `.agents/tests`, `.agents/templates`, `.agents/state` (review log excluded) → nothing.
- §10.4 tests: `python3 -m pytest .agents/tests -q` → 396 passed before the upgrade (1.1.0), **449 passed** after (1.2.0). `framework doctor <worktree>` → 0 failures, 1 warning (the registry entry is `implementing`; informational).
- §10.5 before/after gate (critical path 7, Deployment guards): registry entries not `shipped` / `abandoned` on this branch = 2 (`epic:v1-productionization`, `plan:workflow-rereview-utc`). `review-read --plan <basename> --json` captured before the merge, after the merge (identical) and after the upgrade: dispositions identical — epic `{}`; this plan office-hours `failed`, plan-eng-review `passed`, plan-adversarial-review `passed`; no new keys on any row. **0 changes.** `plan:prod-launch-release` (M8) is not on this branch (its entry is committed only on `prod-launch-release`); success criterion 10 is plan §11's operator step after PR-T merges.
- 1.2.0 on this branch: the banner prints a `Reason:` line (the plan's frontmatter `reason`) under `Next:` and above `Note:`; `--next --json` carries `concerns: []` and `reason`; the dashboard header reads `When (UTC)`; verdict unchanged `CLEARED FOR IMPLEMENTATION`, `--next` still `/execute-plan`, the execute-plan gate program still prints `cleared for implementation`. `ctx` `TIMESTAMP` read `20260918-044140`, equal to `date -u`, while local time read 2026-09-17 21:41 (criterion 7 spot check at UTC−7).
- §10.6 LESSONS: the plan's rows were already in the working tree from the reviews (two office-hours Decisions 2026-09-17, the eng-review and adversarial Decisions, the two scoped-re-run Decisions 2026-09-18, the `review_log_append` Bug Log row, the "Patterns That Do Not Work" bullet, the timezone-test section). Added one bullet under "Patterns That Work" for the uncommitted-doc-rows merge dance above.
- Plan-file repair (deviation, clerical): the Files table held two rows and the Eng Review REVIEW REPORT cell held a literal `\1` from a botched substitution in the eng re-review. The two row prefixes (`payload/skills/plan-{ceo,eng,adversarial,design}-review/SKILL.md`, `payload/skills/{review-implementation,final-review}/SKILL.md`) were reconstructed from §5 with an HTML comment saying so; the Eng cell's lost first-run findings were replaced by a labelled reconstruction from the plan's "Eng review" breadcrumbs and the review-log counts. `/review-implementation` should read both.
- Test-artifact gaps: none new. Critical path 7 ran here; 8 (M8 banner after merging `master`) is §11's operator step and 9 (rollback) runs only if a rollback happens.
- Deviations: (1) no merge commit — `origin/master` fast-forwarded cleanly; (2) the `\1` repairs above edit reviewed plan text, labelled; (3) Docker suites not run: this branch changes no file under `backend/` or `frontend/`, and `docker-compose` from a worktree shares the `backend` compose project name with the main checkout's running stack.

### Step 13, second upgrade — framework 1.3.0 into todo-app (2026-09-18T21:26:47Z)
- Why: `/review-implementation` (2026-09-18 UTC, `issues_open`) found 14 defects in the 1.2.0 payload (1 critical: a lone surrogate blanked the plain banner). Payload code is not hand-edited here, so they were fixed upstream, at the user's direction, as framework 1.3.0: https://github.com/willdoucet/framework/pull/7, merged 2026-09-18T21:22:45Z as `a0e8d70`. The plan's §10.2 and success criterion 13 carry the review's breadcrumb: the version to assert is 1.3.0.
- §10.1: `git fetch origin --prune`; `origin/master` still `e1699fc`, 0 behind, nothing to merge.
- §10.2 source: local `../framework` moved from `gate-hardening` to `main`, fast-forwarded to `a0e8d70`, `HEAD == origin/main`, `origin/main:VERSION` 1.3.0, `framework version` 1.3.0, `2df2d61` is an ancestor. Never upgraded from the branch.
- §10.2 upgrade: `python3 ~/Documents/Dev/Projects/framework/bin/framework upgrade <the worktree's literal absolute path> --dry-run` — header names the worktree, `installed 1.2.0 -> 1.3.0`, 22 payload files (PR #7's 26 minus `CHANGELOG.md`, `README.md`, `VERSION`, `tests/test_skill_lint.py`), 0 `WARN`, `_lib.py` / `workflow-state` / `review-log` / `review-read` together, plus `obsidian-workflow`. Apply printed the identical 22 as `done`. `config.json` and `manifest.json` `framework_version` 1.3.0 (manifest `generated_at` 2026-09-18T21:24:13Z, 139 files); `modules.design_sync` still `false`; `.agents/bin`, `skills`, `tests`, `templates`, `hooks` and `WORKFLOW.md` byte-identical to framework `origin/main`. The main checkout's `git status` was empty before and after (LESSONS Corrections 2026-09-16).
- §10.4 tests: `python3 -m pytest .agents/tests -q` → **469 passed** (449 on 1.2.0). `framework doctor <worktree>` → 0 failures, 1 warning (the registry entry is `ready-for-review`; informational).
- §10.5 before/after gate, 1.2.0 reader vs 1.3.0 reader, `review-read --plan <basename> --json`: `epic:v1-productionization` and `plan:workflow-rereview-utc` — dispositions identical, rows identical, no new keys. Extended to M8 (`prod-launch-release`, not on this branch) read-only, by running this worktree's helpers with the main checkout as cwd: identical too. **0 changes.**
- Behaviour, not declarations: the two probes that found U14 and U1 re-run against the upgraded helpers — a log line holding `"\ud800"` now yields an 8-line banner with `--next` and `--dashboard` exit 0 (was 0 lines, exit 1); a demand dated before the target's last run is refused with exit 1 and nothing written (was stored, gate stayed CLEARED).
- Not run: Docker suites, for the reason in deviation (3); the diff still has no path outside `.agents/`.

### Step 13, third upgrade — framework 1.3.1 into todo-app (2026-09-19T21:53:07Z)
- Why: `/final-review` (2026-09-19 UTC, `issues_open`) found six more informational defects in the 1.3.0 payload (F1–F6, plan section "Final review: findings and their routes"); by user decision they were fixed upstream now rather than deferred: https://github.com/willdoucet/framework/pull/9 (`19904b2`), merged 2026-09-19T21:47:32Z as `5766316`. Run by the same session that wrote the fix, at the user's request, as steps 1 and 2 of that review's hand-off; the reviews of this delta are still to come.
- §10.1: `git fetch origin --prune`; `origin/master` still `e1699fc`, 0 behind, nothing to merge.
- §10.2 source: local `../framework` moved from `final-review-fixes` to `main`, fast-forwarded to `5766316`, `HEAD == origin/main`, `origin/main:VERSION` 1.3.1, `framework version` 1.3.1, `19904b2` is an ancestor. Never upgraded from the branch.
- §10.2 upgrade: `python3 ~/Documents/Dev/Projects/framework/bin/framework upgrade <the worktree's literal absolute path> --dry-run` — header names the worktree, `installed 1.3.0 -> 1.3.1`, 12 payload files (PR #9's 15 minus `CHANGELOG.md`, `README.md`, `VERSION`), 0 `WARN`, `_lib.py` / `workflow-state` / `review-log` / `review-read` together, plus `obsidian-workflow`. Apply printed the identical 12 as `done`. `config.json` and `manifest.json` `framework_version` 1.3.1 (manifest `generated_at` 2026-09-19T21:50:13Z, 139 files, 0 hash mismatches); `modules.design_sync` still `false`; `.agents/bin`, `skills`, `tests`, `templates`, `hooks` and `WORKFLOW.md` byte-identical to framework `origin/main`. The main checkout's `git status` was empty before and after (LESSONS Corrections 2026-09-16); no path outside `.agents/` changed.
- §10.4 tests: `python3 -m pytest .agents/tests -q` → **477 passed** (469 on 1.3.0; the 8 new tests). `framework doctor <worktree>` → 0 failures, 1 warning (the registry entry is `ready-for-review`; informational).
- §10.5 before/after gate: `review-read --plan <basename> --json` snapshotted with the 1.3.0 reader before the upgrade and re-read with the 1.3.1 reader after it, for `plan:workflow-rereview-utc`, `epic:v1-productionization` and, read-only with the main checkout as cwd, M8 (`prod-launch-release`): dispositions identical and the **full JSON identical**, no new keys; `--dashboard --json` identical (verdict `CLEARED FOR IMPLEMENTATION`, to ship `adversarial-subagent`, `final-review`). The text rows of both plans and M8's `--next` are identical under the two readers too (no entry in either log carries a list holding a comma, so F2's new quoting shows nowhere yet). **0 changes.**
- Behaviour, not declarations: the two probes that found F1 and F2, re-run against the installed helpers in throwaway repos — `resolve-plan` on an epic branch returns `branch_fallback` / `epic` (was exit 2) while `ctx`'s `PLAN_FILE` stays empty there on purpose; an Eng row with two outstanding demands splits into exactly its seven fields on bare commas (was three extra fragments), and `--dashboard --json` still carries `stale_notes` as a list.
- Not run: Docker suites, for the reason in deviation (3); the diff still has no path outside `.agents/`.


## Doc impact

- Steps 1–12 change `../framework` only; nothing in this repository's doc-mapped areas moved. PR-T will bring `.agents/**` (doc-map exempt), `.agents/config.json` (`modules.design_sync` false) and the deleted marker `.agents/state/design-system-synced.json`; per `_shared/docs-contract.md` none of PRD, APP_FLOW, TECH_STACK, FRONTEND_*, BACKEND_STRUCTURE, IMPLEMENTATION_PLAN or REVIEW_CHECKLIST describes the review helpers' flags. `development-commands.md` lists the helper names under `.agents/bin/`, which do not change. `LESSONS.md` already carries this plan's Decisions rows and the Bug Log row from the reviews (uncommitted on this branch); `TODOS.md` carries the four P3 items from the reviews. `/update-docs` runs when Step 13 completes.
- Step 13 (2026-09-18T04:44:17Z): the change set is `.agents/**` upgrade output (doc-map exempt), `.agents/config.json` (`framework_version` 1.2.0 by the upgrade, `modules.design_sync` false by §7), the deleted marker `.agents/state/design-system-synced.json`, and the `LESSONS.md` / `TODOS.md` rows. `development-commands.md` names the helpers (unchanged names) and `TECH_STACK.md` names `.agents/docs/` only; no owning doc section describes the review helpers' flags, the design-sync module flag, or the installed framework version. `/update-docs` checks for any doc that states `1.1.0`.

## Update-docs conclusion

Run inline by `/execute-plan` at 2026-09-18T04:47:26Z (Claude Code has no separate skill tool for it in this session). Change set: 47 tracked paths differ from `master` (`e1699fc`) plus 4 untracked, all 51 owned `exempt` by the doc map (`.agents/**`). No `(doc, section)` is owned by the diff; the sections a reader would expect to move were checked by hand.

```
DOC SYNC REPORT — default — workflow-rereview-utc vs master — 2026-09-18

| Doc | Section | Action | Summary |
|---|---|---|---|
| development-commands.md | Critical Rule / Framework Helper Tests | no change | the eight host-side helper names are unchanged by 1.2.0; `design-sync-check` and `design-sync-mark` still ship (module off, not removed); the test command is unchanged |
| TECH_STACK.md | Infrastructure (repo tree) | no change | the tree names `.agents/docs/` only; no doc states the installed framework version |
| IMPLEMENTATION_PLAN.md | roadmap | no change | framework-workflow plans carry no phase row (the parent `workflow-review-resolution` has none); nothing to mark |
| LESSONS.md | Patterns That Work / Decisions / Bug Log | no change (by this pass) | the plan's Decisions, Bug Log row and sections were written by the reviews; Step 13 added the uncommitted-doc-rows merge bullet; `## Decisions` not touched by this pass |
| TODOS.md | P3 items | no change | the four P3 items from the reviews are already present |
| AGENTS.md | Workflow / Harness Notes | no change | pointers to WORKFLOW.md and development-commands.md still correct; no command, structure, dependency or env var changed |
| config.json | modules / framework_version | no change (by this pass) | `modules.design_sync` false (§7) and `framework_version` 1.2.0 (upgrade) were set by Step 13, not by this pass; doc_map untouched |

Guard: doc-guard --staged --dry-run -> staged changes touch no documented areas
Questions: 0 asked, 0 applied, 0 declined
Unmapped: none
Placeholders: none left; the grep's one hit (`REVIEW_CHECKLIST.md:403`) is a GitHub Actions `${{ secrets.NAME }}` expression, pre-existing on `master` and outside this change set

DOC IMPACT: none - framework 1.2.0 upgrade output under .agents/ (doc-map exempt); no documented surface changed
```

Trailer for `/ship`: `Docs: n/a - framework upgrade output under .agents/ (doc-map exempt); no documented surface changed`. (Implementation review 2, 2026-09-18 UTC: made version-neutral. The report above was written at 1.2.0 and the branch now carries 1.3.0; the conclusion was re-checked on the 1.3.0 tree: `doc-guard --worktree` → `no documented areas changed`, `--staged --dry-run` → `staged changes touch no documented areas`, and no path in `git diff master` lies outside `.agents/`.)

### Ship-time run (2026-09-20T04:02:06Z)

Run inline by `/ship` step 4, from the worktree's `update-docs/SKILL.md` on disk (the report above was written at 1.2.0 with 51 paths; the branch now carries 1.3.1 and 56). Change set: 56 paths differ from `master` (`2c4e5cf`), 0 untracked; `doc-guard --explain` owns all 56 `exempt` (`.agents/**`), and `git diff --quiet master -- . ':(exclude).agents'` confirms nothing outside `.agents/` differs. No `(doc, section)` is owned by the diff; the sections a reader would expect to move were checked by hand. This pass edited no doc.

```
DOC SYNC REPORT — default — workflow-rereview-utc vs master — 2026-09-20

| Doc | Section | Action | Summary |
|---|---|---|---|
| development-commands.md | Critical Rule / Framework Helper Tests | no change | the upgrade modifies seven files under `.agents/bin` and adds, removes or renames none, so the eight host-side helper names still match; `design-sync-check` and `design-sync-mark` still ship (module off, not removed); the test command is unchanged |
| TECH_STACK.md | Infrastructure (repo tree) | no change | no doc outside LESSONS / TODOS history rows states an installed framework version (grep) |
| IMPLEMENTATION_PLAN.md | roadmap | no change | no row names this plan or its parent `workflow-review-resolution` (grep); framework-workflow plans carry no phase row, so nothing to mark now or after the registry reads `shipped` |
| LESSONS.md | Workflow And Trust Rules / Corrections Log / Bug Log / Patterns / Decisions | no change (by this pass) | the branch's +31 lines were written by the plan reviews and the code reviews; `## Decisions` not touched by this pass |
| REVIEW_CHECKLIST.md | Framework helpers | no change (by this pass) | the branch's +7 lines are the checks the code reviews added |
| TODOS.md | P3 items | no change (by this pass) | the branch's +41 lines are the four P3 items from the reviews; `/ship` step 5 found no open item this branch completes |
| AGENTS.md | Workflow / Harness Notes | no change | not in the diff; pointers to WORKFLOW.md and development-commands.md still correct; no command, structure, dependency or env var changed |
| config.json | modules / framework_version | no change (by this pass) | `modules.design_sync` false (§7) and `framework_version` 1.3.1 (upgrade) were set by Step 13; doc_map untouched |

Guard: doc-guard --staged --dry-run -> staged changes touch no documented areas
Questions: 0 asked, 0 applied, 0 declined
Unmapped: none
Placeholders: none left; the grep's one hit (`REVIEW_CHECKLIST.md:403`) is a GitHub Actions `${{ secrets.NAME }}` expression, identical on `master` and in no line this branch added

DOC IMPACT: none - framework upgrade output under .agents/ (doc-map exempt); no documented surface changed
```

## Completion

- Previous session ended 2026-09-18T04:13:49Z: STATUS BLOCKED — PR-F1 (#5) and PR-F2 (#6) open; tests so far: framework `python3 -m pytest tests payload/tests -q` → 580 passed on `rereview` (525 on `utc-dates`, 428 at `89f6aab`).
- Session ended: 2026-09-18T04:47:26Z
- STATUS: DONE_WITH_CONCERNS — every step ticked with evidence; concerns listed below.
- What was built: PR-T. This branch now carries framework 1.2.0, upgraded from framework `origin/main` at `b17c320` after #5 and #6 merged: the declared re-review gate (`--rereview`, `stale`), visible concerns (`Concern:` / `Reason:` lines), UTC everywhere (`ctx` `TIMESTAMP`, `<utc-date>` slots, `When (UTC)`), and the product-neutral design-sync helpers; the design-sync module is switched off in `config.json` and its stale marker deleted; `origin/master` (#54) is merged in by fast-forward; the plan's LESSONS and TODOS rows ride along. No file under `backend/` or `frontend/` changes.
- Tests: host `python3 -m pytest .agents/tests -q` → **449 passed** (396 on 1.1.0 before the upgrade); re-run after all edits → 449 passed. `framework doctor <worktree>` → 0 failures. Framework suite unchanged since PR-F2 (580 passed on `b17c320`'s content). Docker backend / frontend / visual suites **not run**: no app file changes on this branch, and `docker-compose` from a worktree would share the `backend` compose project name with the main checkout's stack.
- Before/after gate: `review-read --plan` dispositions identical for both in-flight registry entries on this branch (0 changes).
- Test-artifact gaps: none new. Critical paths 1–7 covered (1–6 by the framework suites in PR-F1/F2, 7 here); 8 is plan §11's operator step after PR-T merges; 9 runs only on a rollback.
- Deviations from the plan: (1) `origin/master` fast-forwarded, no merge commit; (2) two Files-table rows and the Eng Review REVIEW REPORT cell in the plan were repaired from a literal backslash-one substitution artefact — row prefixes reconstructed from §5, the Eng cell's lost first-run findings replaced by a labelled reconstruction; (3) Docker suites not run (above).
- Concerns for `/review-implementation`: (a) read the two reconstructed Files rows and the reconstructed Eng cell in the plan (each marked); (b) `plan:prod-launch-release` (M8) is not in this branch's registry, so success criterion 10 (the 15a `Reason:` line on `prod-launch-release`) is verified only after PR-T merges and `master` is merged there (plan §11); (c) the local framework checkout was moved from the deleted `rereview` branch to `main` at `b17c320` as part of asserting the upgrade source.
- State sync: `workflow_status` and `implementation_status` set to `ready-for-review` with the concern text as `reason`; the registry entry updated the same way. The index is staged (`git add -A`) for `/ship`; nothing committed.
