# Test Plan
Generated by /plan-eng-review (v2) on 2026-04-12
Branch: mealboard-main-page-updates
Repo: willdoucet/todo-app

## Affected Pages/Routes
- `/mealboard` (Meal Planner tab) — spinner loading behavior changed from conditional render to always-render + delayed overlay
- `/mealboard` (Recipes tab) — skeleton placeholder count changed from hardcoded 10 to fixed 5 (one grid row) with delayed visibility gate
- `/mealboard` (Recipes tab) — tag pill entrance animation added
- `/mealboard` (Food Items tab) — count indicator added between search bar and grid/list toggle
- `/mealboard` (Food Items tab) — spinner gated with useDelayedFlag for consistency

## Key Interactions to Verify
- Hard reload `/mealboard` on fast network — no spinner flash, day/date bar visible from first frame
- Hard reload `/mealboard` on slow network (Slow 3G) — spinner overlay appears after ~200ms, disappears when data arrives; only motion-safe users see a fade
- Hard reload `/mealboard` at mobile viewport (<768px) — same behavior via MobileDayView path
- Navigate to Recipes tab — no ghost skeletons on fast load; exactly 5 skeleton cards (one row) on slow load
- Navigate to Recipes tab — tag pills animate in with stagger on normal motion preference
- Navigate to Food Items tab — count indicator visible ("X food items"), matches Recipes tab layout symmetry
- Navigate to Food Items tab — spinner delayed by 200ms, matching Meal Planner behavior
- Search/filter on Recipes tab — count indicator shows "X of Y" format
- Toggle grid/list view on Recipes tab — both views gated behind delayed flag

## Edge Cases
- MealPlannerView with empty mealEntries — SwimlaneGrid/MobileDayView renders empty cells without crashing
- Load timing between 150-250ms — spinner either never appears or appears briefly and clears cleanly (no jarring pop)
- prefers-reduced-motion enabled — tag pill animation disabled; loading overlays on Meal Planner, Recipes, and Food Items appear/disappear instantly with NO fade
- Count indicator at count=1 — should show singular ("1 recipe", "1 food item")
- Filtered Recipes count — should show "X of Y" when search or tag filter is active

## Critical Paths
- Full reload of `/mealboard` planner tab: data fetches complete → grid renders with meals → no visible loading artifacts
- Recipe browsing flow: load Recipes tab → search → filter by tag → verify count indicator updates → clear filters → verify count returns to unfiltered format
- Dead-code verification: `npm run build` succeeds after all deletions; `npm run hygiene` (knip) shows no new unused exports
- Reduced-motion path: enable reduced-motion in devtools → verify no fade transitions on any loading overlay, no tag pill animation
